---
name: futo-keyboard-build
description: Build and maintain the user's customized fork of FUTO Keyboard for Android (package shiroikuma.futokxkb, label "白い熊 FUTO kxkb"), installable side-by-side with the official FUTO Keyboard. Use this skill any time the user mentions FUTO, FUTO Keyboard, futo-keyboard, android-keyboard, futo-org, shiroikuma.futokxkb, shiroikuma-futokxkb, futokxkb, 白い熊 FUTO kxkb, "the keyboard fork", "my plus version", asks to pull/sync a new FUTO version, rebase their changes, rebuild the keyboard, apply a change to the keyboard, works on the key-sliding / 4D-keys feature or the Special Keys page, or references their FUTO build pipeline. This fork follows the SAME model as the user's AppManager, Jami, ArcaneChat, FairEmail and SimpleX forks: a `custom` branch carrying their commits, rebased onto each upstream release tag, pushed to their GitHub fork. Default to assuming this skill applies when in doubt during a session about the FUTO Keyboard fork. Follows the shell formatting in `shell-block-formatting`; patch files are named per `patch-naming`; skill zips per `skill-export`.
---

# FUTO Keyboard — customized fork build skill

The user maintains a customized build of [FUTO Keyboard](https://github.com/futo-org/android-keyboard) on Android, run on a Huawei Mate XT alongside the official FUTO Keyboard. The fork's purpose: a distinct `applicationId` + label so it installs side-by-side, plus a growing set of feature changes the user builds, tests, and may publish as "plus" releases from their GitHub fork.

The fork model is identical to the user's other Android forks (AppManager, Jami, ArcaneChat, FairEmail, SimpleX): fork on GitHub, a mirror branch tracking upstream, a `custom` branch carrying the user's commits, rebased onto each new upstream release tag, force-pushed to the fork. The user builds locally and sideloads; builds are signed with a stable per-fork keystore so reinstalls land in place.

## Project identity

| Item | Value |
|------|-------|
| Upstream repo | `futo-org/android-keyboard` (remote `upstream`, HTTPS, fetch-only) |
| Fork repo | `git@github.com:ShiroiKuma0/shiroikuma-futokxkb.git` (remote `origin`, SSH — push here) |
| Local working tree | `~/git/shiroikuma-futokxkb` |
| Mirror branch | `master` — mirrors `futo-org/android-keyboard`, never carries our changes, fast-forward only |
| Custom branch | `custom` — carries all commits below, rebased onto each upstream release **tag** |
| Custom applicationId | `shiroikuma.futokxkb` (built on the **stable** flavor → no suffix) |
| Custom app label | `白い熊 FUTO kxkb` (`english_ime_name` in `java/res/values/strings-appname.xml`) |
| Java/Kotlin namespace (UNCHANGED) | `org.futo.inputmethod.latin` (R/BuildConfig package; never touch) |
| Flavor we build | **`stable`** (NOT `unstable` — see below) → task `:assembleStableRelease` |
| Target ABI | `arm64-v8a` only |
| Custom signing keystore | `~/.android-keystores/futokxkb.jks` (alias `futokxkb`, passphrase `futokxkb123`) |
| Output APK dir (build) | `build/outputs/apk/stable/release/` |
| Output APK dir (archive) | `~/tmp/` + on-device `/sdcard/tmp/` |
| Build host | Tuxedo OS |
| Build JDK | OpenJDK 21 at `/usr/lib/jvm/java-21-openjdk-amd64` |
| Android SDK | `/home/shiroikuma/android-sdk`, platform-35 + build-tools 35.0.0 |
| NDK / CMake | NDK `28.2.13676358` (AGP auto-downloads if absent); CMake via SDK |
| AGP / Gradle | 8.10.1 / 8.14.3 |
| Git submodules | `libs` (mozc/rime AARs, gitlab.futo.org), `voiceinput-shared/src/main/ml`, `java/assets/layouts`, `translations`, `java/assets/themes`, `java/res-large` — clone `--recurse-submodules`, re-init after each checkout/rebase |
| APK filename | `shiroikuma-futokxkb_<versionName>_arm64-v8a.apk` — the name MUST end with the versionName followed by `_arm64-v8a.apk`; **never shorten it** (do not drop the `_arm64-v8a` suffix, and use `_` not `-` before the version). versionName carries the `+N` tail, e.g. `shiroikuma-futokxkb_0.1.29+1_arm64-v8a.apk` (no datetime; an `-rcN` base rides through too, e.g. `…_0.1.29-rc1+1_…`). Same name for the `~/tmp` archive copy and the on-device `/sdcard/tmp/` push — **including when gradle is run directly in Claude Code** (mirror the build block's `apk_name`, don't improvise a shorter name like `shiroikuma-futokxkb-<version>.apk`). |

Single-module Android Gradle project, **Groovy** DSL (`build.gradle` at repo root; the app *is* the root project, `settings.gradle` only adds `:voiceinput-shared`).

Apply the `shell-block-formatting` conventions: every command in chat blocks gets a cyan `>>>` echo prefix, stderr recolored red via the **ANSI-C-quoted** `r()` helper — `r() { "$@" 2> >(sed $'s/.*/\033[1;31m&\033[0m/' >&2); }` — never the plain-single-quoted form, which GNU sed mangles (`\033` parsed as `\0`+`33`) into tripled lines and raw `33[`. Every expensive step (Gradle build) sits behind a `read -p` pause gate.

## Why build `stable`, not `unstable`

Upstream declares three product flavors on dimension `buildType`: `unstable` (upstream default, `getIsDefault().set(true)`), `stable`, `playstore`. `unstable` appends `applicationIdSuffix ".unstable"` + `versionNameSuffix "-unstable"` + overrides the label to "FUTO Keyboard [Dev Build]" (`java/unstable/res/values/strings-appname.xml`). `playstore` appends `.playstore`. Only **stable** appends nothing and inherits our base label — so it yields a clean `shiroikuma.futokxkb` / `0.1.28+1` / `白い熊 FUTO kxkb`. Always build `:assembleStableRelease`. The stable flavor's update-checking BuildConfig has `UPDATE_CHECKING_NETWORK = false`, so it will not phone home.

## Side-by-side install is safe (no collisions)

Coexistence is decided purely by `applicationId` (`shiroikuma.futokxkb` vs official `org.futo.inputmethod.latin`). The `namespace` stays `org.futo.inputmethod.latin` — it is build-time only (R/BuildConfig package) and never seen by the OS at install. All three FUTO content providers use `${applicationId}` authorities (`.font`, `.clipboard`, `.androidx-startup`), so they auto-differ; there are no hardcoded authorities and no custom `<permission>` declarations. So FUTO + the fork install as two separate keyboards. Do NOT install the fork over official FUTO (different signing keys → Android refuses); they coexist as distinct packages.

## Branch / remote model

| Branch | Purpose | Update mode |
|--------|---------|-------------|
| `master` | Mirrors `futo-org/android-keyboard`. Never carries our changes. | Fast-forward only |
| `custom` | Carries all commits below. | Rebased onto each upstream release tag |

`origin` = the user's fork (SSH, push). `upstream` = futo-org (HTTPS, fetch only). Submodules re-synced (`git submodule update --init --recursive`) after every checkout/rebase. FUTO releases are tagged bare semver (`0.1.28`, `0.1.26.2`; only old `v0.1.20..v0.1.22` had a `v`). `custom` is rebased onto the chosen release tag.

## Customization commits on `custom`

The base of the stack is two commits over the release tag, documented in full below because they're the most rebase-sensitive; feature commits (kxkb BFU default, the look, Esc, JP-label strip, the BFU 5r15c bundle + sizing, the Ctrl modifier + its highlight, the Multiling-style key icons, the per-geometry live look/size knobs — gaps, row heights, font/hint size, roundness, suggestion-bar height, split width + snap, cluster main offsets — and the per-geometry colours (the screen since renamed "Live sizing" → "Keyboard UI"), plus the 4D keys / key-sliding feature with its action-bar quick-access icons + the Special Keys reference page + the Custom layouts launcher, and the compact special-key YAML types compass / macro / chord (plus their slot shorthands) and cycle/multitap and cluster (predictive multi-key), plus the HIDE-keyboard action key, and the Spacebar-style no-preview/long-press default, plus the swipe-space layout switcher (phases 1–3, complete), plus Special Keys reference docs for the HIDE key and FUTO's native alt pages (the ABC / return-to-letters keys), plus the GNU programming "language" (a `zxx` locale shown as "GNU", registered via a custom mapping/names merge in `assets-custom`), plus per-(language·layout·geometry) Keyboard UI settings (each active-subtype × geometry combo persists and forks independently, inheriting the old per-geometry blob), plus the Jami-style grouped/cascaded Keyboard UI page (KxkbSection / KxkbSubgroup), plus the long-press "study popup" (an enlarged ~3× preview of a compass / cluster / macro / chord key's full content, held to study), plus the "App language" setting (a per-app UI locale picker at the top of Settings, independent of the system language), plus the "Next layout (same language)" action (cycles the current language's enabled layouts only), plus the "Keyboard width" slider (narrows + centres the whole keyboard, surface included), plus the scoped backup system (Settings → Miscellaneous offers the full backup plus three slim, non-destructive sets — Keyboard Settings / Learned Data / Models & Dictionaries — that partition everything the full backup holds), plus arrow keys that repeat on long-press (held arrow keeps moving the cursor, like a held key) and backspace that auto-repeats in any layout, plus the `column` special-key type (a vertical `cluster` — the mains stack top-to-bottom as the predictive band, with optional 3+3 small side keys), plus row-spanning keys (`attributes: { heightRows: N }` makes any key N rows tall, e.g. column keys flanking a multi-row nav block), plus Multiling-style cluster prediction (cluster keys constrain dict and LM suggestions to prefixes inside the tapped letter-sets, and an enumeration over per-tap mains injects valid in-set words the existing tap-decoder and LM beam both miss), plus making that cluster prediction work for accented / no-transformer languages — Czech, Russian — via diacritic-folded matching, running the constraint on the legacy dictionary path too, and a dictionary trie-walk enumeration, plus software dead keys (an accent glyph like `´`/`ˇ`/`¨` composes with the next letter — `ˇ`+`r`→`ř` — by re-dispatching it as a FLAG_DEAD event into FUTO's existing DeadKeyCombiner), plus enabling FUTO's expandable suggestions panel (the CJK-only scroll/expand candidate UI) for all languages by default + a Typing toggle + a 128-candidate pool, plus a Multiling-style "topBar" (static candidates — emoji/snippets/bracket-pairs/[Paste]/date-stamps — shown in the suggestion bar when nothing is typed, and appended after next-word predictions so both are selectable; global list editable in Settings + per-layout `topBar:` override), plus Caps-lock via a Shift **tap-cycle** (unshifted → shift → caps-lock → unshifted, timing-free) with the Shift **glyph** recoloured (default pure blue `#0000FF`, configurable per-(language·layout·geometry) in Keyboard UI) while caps-lock is engaged, plus a **YAML emitter** for the layout model (`emitKeyboardYaml`, round-trips parse↔emit) and the **visual keyboard-layout editor** built on it (Settings → Keyboard UI → "Keyboard editor": live preview, tap-a-key → dedicated per-key edit screen, structural add/move/remove/duplicate of keys & rows (incl. swapping a key up/down between rows), alt-page (sym/altGr) editing + cross-layout copy, Apply to the live keyboard / Apply-as-new, Export with copy/save, per-key appearance overrides — text/background/border colour, font + hint size, cluster spacing, character position & secondary-label offsets, with a single-key live preview), plus disabling FUTO's adjacent-identical-key merge (so duplicate keys / intentional twin keys render separately), plus cluster/column keys auto-capitalising their whole predictive band when the layout is shifted (so they needn't be wrapped in a `case` to upper-case; `shiftable: false` opts out), plus a bar-pinnable "Personal dictionary" action (deep-links to the pdict editor — for adding e.g. a Japanese kanji + hiragana reading + 品詞 that Mozc then offers as a conversion candidate), plus App-language picker fixes (offer English (US); persist the selection across restarts on API < 33), plus colon/semicolon getting period-like auto-spacing (the preceding auto-space is swapped away and one is always added after), plus the Tab key sending `KEYCODE_TAB` instead of a literal tab (so Emacs minibuffer completion / `indent-for-tab-command` work), plus a Force-auto-capitalization top-bar toggle (auto-shift after `. ? !` and after newlines even where the field — e.g. the Emacs Android port — doesn't request caps), plus the ellipsis "…" getting the same period-like auto-spacing, plus four further keyboard-editor additions — a per-layout "Suggestion bar candidates" (`topBar`) editor (pre-filled with the effective/global list for in-place editing; draft-only, persists on Apply/Export), preselecting the active layout on editor entry, a third Close button at the screen bottom, and a configurable font (family/size/weight/colour) for the editor's layout-name labels, plus per-app layout memory (each app reopens with the (language·layout) subtype it last used; last-used-wins, on by default), plus space accepting the first next-word prediction (the next-word analogue of autocorrect-on-space — when a prediction is showing and nothing is composing, space commits it like a tap; default-on toggle), …) append on top — see "Implemented features" near the end for the notable ones. As of FUTO `0.1.29` the stack is ~142 commits. All are kept small and surgical for trivial rebase conflict resolution. `namespace` is left unchanged in all of them. **The 0.1.28 → 0.1.29-rc1 rebase was the first non-trivial one** — upstream reworked several subsystems our features touch; the reconciliations are recorded in *Sync to a new upstream version* below. The subsequent **`0.1.29-rc1` → stable `0.1.29` roll-forward was small and mechanical** (4 upstream commits; 6 "keep both field" conflicts from a new `swipeLetter`/`swipeCodeOverride` field threaded through the v2keyboard model + an action-defaults reshuffle — also recorded below).

### Commit 1 — `Customize for shiroikuma side-by-side install`

Two files.

**`build.gradle` `defaultConfig`** — applicationId + arm64 abiFilter:

```diff
-        applicationId 'org.futo.inputmethod.latin'
+        applicationId 'shiroikuma.futokxkb'
         testApplicationId 'org.futo.inputmethod.latin.tests'
         testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
         vectorDrawables.useSupportLibrary = false
+        ndk { abiFilters 'arm64-v8a' }
```

Leave `namespace`, `testApplicationId`, and the flavor suffixes untouched. Upstream has no `splits`/`abiFilters`, so without the `ndk` line the build is a universal APK with every ABI's native libs (slow CMake, huge APK); restricting to arm64 keeps a single deterministic-named APK.

**`java/res/values/strings-appname.xml`** — app label:

```diff
-    <string name="english_ime_name">FUTO Keyboard</string>
+    <string name="english_ime_name">白い熊 FUTO kxkb</string>
```

(Translated app-name strings exist in the `translations` submodule's `values-*/` dirs and still say a localized "FUTO Keyboard"; not worth chasing for a personal single-locale build. The stable flavor does not override this string.)

### Commit 2 — `Quieter build logs`

Suppress the wall of build warnings **at the source** (committed), the way the AppManager / Jami forks do it. FUTO emits hundreds of clang `C/C++:` native warnings (sign-compare, cast-align, float-equal in ggml/llama/protobuf/sentencepiece), Kotlin `w:` deprecations, and Gradle/javac notices that bury the lines that matter. Everything the *compilers* emit is killed at source (below); only three *tooling*-emitted blocks with no source toggle get a narrow pipeline filter (see "Tool-emitted warnings" below).

**`build.gradle` `defaultConfig`** — silence native clang warnings (`-w`):

```diff
         vectorDrawables.useSupportLibrary = false
         ndk { abiFilters 'arm64-v8a' }
+        externalNativeBuild {
+            cmake {
+                cppFlags '-w'
+                cFlags '-w'
+            }
+        }
```

(Merges with the existing android-level `externalNativeBuild { cmake { path 'native/jni/CMakeLists.txt' } }`.)

**`build.gradle` `kotlinOptions`** — silence Kotlin `w:` warnings:

```diff
     kotlinOptions {
         jvmTarget = '1.8'
+        suppressWarnings = true
     }
```

(**Property assignment `= true`, not the method-call form `suppressWarnings true`** — the old `kotlinOptions` DSL parses the space form as a method call `suppressWarnings(true)`, which does not exist and fails configuration with "Could not find method suppressWarnings()".)

**`build.gradle`** — silence the javac deprecation notes; add this block at script top level (outside `android {}`, e.g. at the end of the file):

```groovy
tasks.withType(JavaCompile).configureEach {
    options.compilerArgs << '-Xlint:none' << '-nowarn'
    // javac's mandatory deprecation NOTES ("uses or overrides a deprecated API" /
    // "Recompile with -Xlint:deprecation for details") are NOT suppressed by -Xlint:none
    // or -nowarn (verified empirically on JDK 21 — those flags are exactly what *triggers*
    // the summary note). -XDsuppressNotes (internal javac flag) is the only thing that
    // silences them; it suppresses NOTE-kind diagnostics only, errors still print.
    options.compilerArgs << '-XDsuppressNotes'
    options.deprecation = false
}
```

**`gradle.properties`** — append:

```properties
android.javaCompile.suppressSourceTargetDeprecationWarning=true
org.gradle.warning.mode=none
```

Errors are unaffected by any of the above — `-w`/`-nowarn`/`suppressWarnings`/`-XDsuppressNotes` drop warnings and notes only, so build failures (native `error:`, Kotlin `e:`, Gradle task failures, `BUILD FAILED`) still print.

#### Tool-emitted warnings with no source toggle — narrow pipeline filter

The source-level flags above cover everything the **compilers** emit. Three remaining warning blocks come from build *tooling* (NDK/CMake, the manifest merger, AAPT2) and have **no source-level suppression** — they are not compiler diagnostics, so no `build.gradle`/`gradle.properties` flag silences them:

1. **`[CXX5304] This version only understands SDK XML versions up to 3 but an SDK XML file of version 4 was encountered.`** — NDK/CMake bridge vs a newer SDK `package.xml`. Benign; appears twice during native configure.
2. **Manifest-merger no-ops** — `…/java/stable/AndroidManifest.xml Warning:` + `uses-permission#…ACCESS_NETWORK_STATE was tagged at AndroidManifest.xml:0 to remove other declarations but no other declaration present` and `provider#org.acra.attachment.AcraContentProvider@android:authorities … to replace … but no other declaration present`. The stable flavor manifest carries `tools:node="remove"`/`"replace"` guards (to strip ACRA/network-state) that have nothing to act on in this merge → harmless warnings. (Editing the manifest to drop the guards is *not* worth it — they're intentional safety for other merge inputs.)
3. **AAPT2 resource-shrink** — `warn: removing resource shiroikuma.futokxkb:string/… without required default value.` (credits_title, version_text, several typing/size settings strings). Locale-only strings with no default value, dropped during resource processing. Benign.

Because there is no source flag, these — and **only** these — are dropped with a tight, explicit `grep -v` on the gradle output, keeping the success signal via `PIPESTATUS` so errors and `BUILD FAILED` are never hidden. This is the single sanctioned exception to "no output grep filter"; do not widen the pattern. In the build pipeline, replace the bare `r ./gradlew …` invocation with:

```bash
  # benign tool warnings with no source toggle (NDK CXX5304, manifest-merger no-op
  # remove/replace, AAPT2 locale-string removal). Tight pattern — do not broaden.
  NOISE='CXX5304|AndroidManifest\.xml Warning:|was tagged at AndroidManifest\.xml:0 to (remove|replace) other declarations but no other declaration present|^warn: removing resource .* without required default value'
  BRANCH_NAME='shiroikuma' VERSION_NAME="$our_name" VERSION_CODE="$our_code" \
    ./gradlew :assembleStableRelease --console=plain 2>&1 \
    | sed -E "/$NOISE/d"
  [ "${PIPESTATUS[0]}" = 0 ] && build_ok=1
```

`PIPESTATUS[0]` is gradle's own exit status (not `sed`'s), so success/failure detection is unaffected by the filter. Everything except the three listed blocks still prints verbatim.

## Versioning (local counter + env injection — no commit needed)

Upstream's `build.gradle` already derives the version from environment variables, falling back to git: `getVersionCode()` reads `VERSION_CODE`, `getVersionName()` reads `VERSION_NAME`. So **no build.gradle edit is needed for versioning** — we inject the env vars at the `gradlew` call, like the Jami fork injects `-PshiroikumaBuild`.

Scheme (matches AppManager/Jami):
- **Base name** = the FUTO release being tracked, currently `0.1.29` (= the rebase target tag, leading `v` stripped; an `-rcN` suffix rides through verbatim → e.g. versionName `0.1.29-rc1+1`).
- **Base code** = FUTO's own code for that release = `git rev-list --first-parent --count <tag>` (count the **tag**, not `master` — FUTO's CI counts master at release time, and master moves on afterward; counting the tag reproduces the released code, e.g. `0.1.28` → `11654`, `0.1.29-rc1` → `11686`, `0.1.29` → `11690`).
- **N** = per-build iteration, from a local counter file `~/tmp/.shiroikuma_futokxkb_build` (one line: `<base_name> <N>`). Increment on each **successful** build; **reset to 1 when the base changes** (new upstream tag). Consumed only on success.
- **versionName** = `<base_name>+<N>` → `0.1.29+1`, `0.1.29+2`, …
- **versionCode** = `<base_code> * 10000 + N` → `116900001`, `116900002`, … (9999 builds of headroom per base, always above the next upstream bump). `BRANCH_NAME='shiroikuma'` is also exported so the three `BRANCH_NAME env variable not set` fallback lines never print.
- **Downgrade caveat (rollback builds):** a newer tag's base_code is always higher, so an *older*-tag rebuild (e.g. a `0.1.28` rollback while a `0.1.29-rc1` build is installed) has a *lower* versionCode and the on-device file-manager installer refuses it as a downgrade. To make a rollback install over a newer build (keeping data), set `VERSION_CODE` explicitly just above the installed build's code (e.g. rollback at `116860002` over `0.1.29-rc1+1` = `116860001`), build with an explicit `VERSION_NAME`/`VERSION_CODE` (do NOT touch the counter), then bump the counter so the next forward build still outranks the rollback. Done this way during the 0.1.29-rc1 sync.

The build block keeps N in the counter and bumps it itself — never hardcode `buildN`. `+` is legal on ext4/sdcard FAT/`adb push`/GitHub assets; leave it unescaped.

## Signing

The release buildType has a `signingConfig` that reads `keystore.properties` (falling back to the repo's debug keystore if absent), so AGP produces an **already-signed, zipaligned** APK — no separate `apksigner`/`zipalign` step (unlike the Jami/AppManager/ArcaneChat forks). We use a dedicated keystore (created once) so the user owns the key and avoids the repo keystore's MD5withRSA cert. `keystore.properties` is **regenerated by the build block on every run** (heredoc) so it survives `git checkout`/`reset`/`clean` and is never committed (keep it out of every commit; it is local signing config).

## One-time setup

### 1. Fork on GitHub (manual)

Fork `futo-org/android-keyboard` → `ShiroiKuma0/shiroikuma-futokxkb`. Forks copy `.gitmodules` pointers, not submodule contents (fetched from their upstream URLs at clone time).

### 2. Clone, wire upstream, create the dedicated keystore

```bash
r() { "$@" 2> >(sed $'s/.*/\033[1;31m&\033[0m/' >&2); }

echo -e '\033[1;36m>>> mkdir -p ~/git && cd ~/git\033[0m'
mkdir -p ~/git && cd ~/git

echo -e '\033[1;36m>>> git clone --recurse-submodules git@github.com:ShiroiKuma0/shiroikuma-futokxkb.git\033[0m'
r git clone --recurse-submodules git@github.com:ShiroiKuma0/shiroikuma-futokxkb.git

echo -e '\033[1;36m>>> cd shiroikuma-futokxkb\033[0m'
cd shiroikuma-futokxkb

echo -e '\033[1;36m>>> git remote add upstream https://github.com/futo-org/android-keyboard.git\033[0m'
r git remote add upstream https://github.com/futo-org/android-keyboard.git

echo -e '\033[1;36m>>> git fetch upstream --tags\033[0m'
r git fetch upstream --tags

echo -e '\033[1;36m>>> mkdir -p ~/.android-keystores\033[0m'
mkdir -p ~/.android-keystores

echo -e '\033[1;36m>>> keytool -genkeypair futokxkb.jks (valid ~100y)\033[0m'
[ -f ~/.android-keystores/futokxkb.jks ] || r keytool -genkeypair -v -keystore ~/.android-keystores/futokxkb.jks -alias futokxkb -keyalg RSA -keysize 2048 -validity 36500 -storepass futokxkb123 -keypass futokxkb123 -dname "CN=shiroikuma futokxkb, O=shiroikuma, C=JP"
```

### 3. Establish the `custom` branch off the release tag

This commits the two customization layers. Claude will deliver the exact patch (or the inline edits) when the user runs this; the shape is: check out the tag onto `custom`, apply commit-1 and commit-2 edits, commit each, push.

```bash
r() { "$@" 2> >(sed $'s/.*/\033[1;31m&\033[0m/' >&2); }

target_tag="0.1.29"   # the FUTO release tag to base on

echo -e '\033[1;36m>>> cd ~/git/shiroikuma-futokxkb\033[0m'
cd ~/git/shiroikuma-futokxkb

echo -e '\033[1;36m>>> git checkout -b custom '"$target_tag"'\033[0m'
r git checkout -b custom "$target_tag"

# --- commit 1: identity ---
r sed -i "s|applicationId 'org.futo.inputmethod.latin'|applicationId 'shiroikuma.futokxkb'|" build.gradle
grep -q "abiFilters 'arm64-v8a'" build.gradle || r sed -i "/vectorDrawables.useSupportLibrary = false/a\\        ndk { abiFilters 'arm64-v8a' }" build.gradle
r sed -i 's|<string name="english_ime_name">FUTO Keyboard</string>|<string name="english_ime_name">白い熊 FUTO kxkb</string>|' java/res/values/strings-appname.xml
r git add build.gradle java/res/values/strings-appname.xml
r git commit -m "Customize for shiroikuma side-by-side install"

# --- commit 2: quieter build logs (Claude supplies the exact build.gradle edits;
#     the gradle.properties lines are appended idempotently here) ---
grep -q 'org.gradle.warning.mode=none' gradle.properties || printf '\nandroid.javaCompile.suppressSourceTargetDeprecationWarning=true\norg.gradle.warning.mode=none\n' >> gradle.properties
# (build.gradle -w / suppressWarnings / JavaCompile edits applied via Claude's patch before this commit)
r git add build.gradle gradle.properties
r git commit -m "Quieter build logs"

echo -e '\033[1;36m>>> git push -u origin custom\033[0m'
r git push -u origin custom
```

## Per-change delivery workflow (STRICT — the user tests between every step)

The user builds, sideloads and tests **every** change on-device before it is ever committed. Follow this ordering exactly, same as the AppManager / Jami / ArcaneChat forks:

1. **Deliver the patch + the apply-and-build block ONLY.** Generate the change as a `.patch` (per `patch-naming`) against `origin/custom`, present it, and give the shell block that applies it and builds/signs/sideloads. **Then stop.** Do **NOT** include any `git commit`, `git push`, `git fetch`, rebase, or `git reset --hard origin/custom` in that turn — not even "for when you're ready". Pre-empting the push is wrong every time.

   **Always paste the COMPLETE build block, every time** — including on a re-send after a fix. Never tell the user to "change only the `patch=` line" or "re-run the same block with X edited"; they copy-paste the whole thing in one go and a partial instruction invites a stale/mismatched run. Each delivery is the full, self-contained apply-and-build block with the correct `patch=` filename already filled in.
2. **Wait for the user to test.** They may report problems; iterate with more build-only patches. Fix patches can stack on the uncommitted working tree (the build block stays `git checkout custom`, no reset, so prior uncommitted patches stay in place).
3. **Only when the user explicitly says "Push"** do you provide, in that turn: the commit + push block (stage explicit source paths — never `keystore.properties`, never the submodules, never `build/`; `git commit`; `git push origin custom`), and then re-sync the sandbox on the user's next "Done". **The push block has a SINGLE y/n gate — confirm-before-commit — after which it commits and pushes in one go.** Do NOT add a second "Push to origin? (y/n)" prompt; the user had it removed (the staged-summary + `git status --short` shown before the one gate is the review point).

   **Commit messages carry NO `Co-Authored-By: Claude …` trailer** — nor any "🤖 Generated with Claude Code" / Claude-attribution line, in commit bodies OR PR bodies. 白い熊 does not want Claude attribution anywhere in this repo's history; this **overrides** the harness's default instruction to append such a trailer. End the message at the last line of the body. (This applied retroactively too: the existing stack was scrubbed of the ~79 trailers it had accumulated.)

One logical change per patch; build-only until the user says "Push". If several related uncommitted patches were tested together, they may be squashed into one commit at push time when that reads as one logical change. New feature commits append on top of commits 1–2 and survive rebases.

### Sandbox pre-patch protocol (Claude's side)

Before generating any patch, sync the sandbox clone to `origin/custom` as it exists on GitHub right now (the fork's `origin` is on github.com, reachable from the sandbox; submodules are not needed to diff tracked source):

```bash
cd /home/claude/shiroikuma-futokxkb   # or wherever the sandbox clone lives
git fetch origin '+refs/heads/*:refs/remotes/origin/*'
git reset --hard origin/custom
git clean -fd
```

Generate the patch against `origin/custom`. Never assume local sandbox state matches; never base a patch on a stale commit or a tag. Working from drift is what produces "patch does not apply" after the user reports a successful push.

### User-side apply guard

Before every `git apply`, reset to a known base and clear leftover untracked files from prior patches (a `git reset --hard` reverts only *tracked* files; an untracked file a previous patch created survives and makes the next `git apply` abort the **entire** patch atomically with `already exists in working directory`). Scope the clean so it does not nuke `build/` or the `java/assets/layouts` submodule:

```bash
r git reset --hard origin/custom      # or: git checkout custom, to keep stacked uncommitted fix patches
# Enumerate the source-bearing dirs explicitly with -fdx. Do NOT use a bare `git clean -fdx java/`:
# the `-x` would wipe the java/assets/layouts submodule contents and any build/ outputs under java/.
# Every product-flavor source root MUST be listed (java/stable, java/unstable, java/playstore), not
# just java/src — an abandoned experiment that wrote e.g. java/stable/res/ is untracked, so a reset
# leaves it behind, and if it's outside the clean scope it survives into later builds/commits and
# the artifact guard (correctly) blocks the push. (This bit us once: a dropped stable-flavor label
# override left java/stable/res/ lingering.)
r git clean -ndx java/src common/src java/res java/assets-custom java/stable java/unstable java/playstore
r git clean -fdx java/src common/src java/res java/assets-custom java/stable java/unstable java/playstore
r git apply ~/tmp/<patch-file>.patch
# post-apply sentinel grep (r does NOT abort the block on a failed apply):
r grep -n "<a string the patch introduces>" <file the patch touches>
```

## Build + sign + deploy pipeline

Canonical block, from a clean `custom` checkout (assumes commits 1–2 are on `custom`). First build pulls a large dependency set and compiles native CMake (10–20+ min); subsequent builds use the cache. During a feature iteration, prepend the apply guard above; the build half is identical.

```bash
r() { "$@" 2> >(sed $'s/.*/\033[1;31m&\033[0m/' >&2); }

export JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64
# In Claude Code the Bash tool's shell does not source the user's profile, so ANDROID_HOME is unset
# and gradle fails configuration with "SDK location not found". Export it (SDK at ~/android-sdk).
export ANDROID_HOME="$HOME/android-sdk"
export ANDROID_SDK_ROOT="$HOME/android-sdk"
export PATH="$JAVA_HOME/bin:$ANDROID_SDK_ROOT/platform-tools:$PATH"

target_tag="0.1.29"   # the FUTO release tag custom is currently based on

echo -e '\033[1;36m>>> cd ~/git/shiroikuma-futokxkb\033[0m'
cd ~/git/shiroikuma-futokxkb

echo -e '\033[1;36m>>> git checkout custom\033[0m'
r git checkout custom

echo -e '\033[1;36m>>> git submodule update --init --recursive\033[0m'
r git submodule update --init --recursive

echo -e '\033[1;36m>>> write keystore.properties (regenerated each build; never committed)\033[0m'
cat > keystore.properties <<'EOF'
keyAlias=futokxkb
keyPassword=futokxkb123
storeFile=/home/shiroikuma/.android-keystores/futokxkb.jks
storePassword=futokxkb123
EOF

echo -e '\033[1;36m>>> compute version from target_tag + local counter\033[0m'
base_name="${target_tag#v}"
base_code=$(git rev-list --first-parent --count "$target_tag")
counter="$HOME/tmp/.shiroikuma_futokxkb_build"
stored_name=""; stored_n=0
[ -f "$counter" ] && read stored_name stored_n < "$counter"
if [ "$stored_name" = "$base_name" ]; then N=$((stored_n + 1)); else N=1; fi
our_name="${base_name}+${N}"
our_code=$(( base_code * 10000 + N ))
apk_name="shiroikuma-futokxkb_${our_name}_arm64-v8a.apk"
echo -e "\033[1;36m>>> Will produce: $apk_name (versionCode $our_code)\033[0m"

read -p $'\033[1;33m>>> Continue with stable release build? (y/n) \033[0m' ans
if [[ "$ans" =~ ^[Yy]$ ]]; then
  echo -e "\033[1;36m>>> BRANCH_NAME=shiroikuma VERSION_NAME=$our_name VERSION_CODE=$our_code ./gradlew :assembleStableRelease --console=plain\033[0m"
  build_ok=0
  # drop benign tool warnings with no source toggle (NDK CXX5304, manifest-merger no-op
  # remove/replace, AAPT2 locale-string removal); PIPESTATUS keeps gradle's real exit code.
  NOISE='CXX5304|AndroidManifest\.xml Warning:|was tagged at AndroidManifest\.xml:0 to (remove|replace) other declarations but no other declaration present|^warn: removing resource .* without required default value'
  BRANCH_NAME='shiroikuma' VERSION_NAME="$our_name" VERSION_CODE="$our_code" \
    ./gradlew :assembleStableRelease --console=plain 2>&1 \
    | sed -E "/$NOISE/d"
  [ "${PIPESTATUS[0]}" = 0 ] && build_ok=1

  if [ "$build_ok" != 1 ]; then
    echo -e '\033[1;31m>>> BUILD FAILED — no APK. Paste the What went wrong / Caused by lines. Do NOT install any leftover APK.\033[0m'
  else
    echo "$base_name $N" > "$counter"   # consume the build number only on success

    echo -e '\033[1;36m>>> locate built APK\033[0m'
    built=$(ls -t build/outputs/apk/stable/release/*.apk | head -1)
    r ls -lh "$built"

    # local backup FIRST, unconditionally — a missing cable never costs the build
    echo -e "\033[1;36m>>> mkdir -p ~/tmp && cp \"$built\" ~/tmp/$apk_name\033[0m"
    r bash -c "mkdir -p ~/tmp && cp \"$built\" ~/tmp/\"$apk_name\""
    r ls -lh ~/tmp/"$apk_name"

    echo
    echo -e '\033[1;33m=================================================================\033[0m'
    echo -e '\033[1;33m>>>   CONNECT THE PHONE TO THE COMPUTER NOW                     <<<\033[0m'
    echo -e '\033[1;33m>>>   USB cable plugged in, USB debugging ON, authorise the    <<<\033[0m'
    echo -e '\033[1;33m>>>   RSA prompt on the phone if it appears.                    <<<\033[0m'
    echo -e '\033[1;33m>>>   The APK is already saved in ~/tmp regardless of push.     <<<\033[0m'
    echo -e '\033[1;33m=================================================================\033[0m'
    echo

    read -t 0.5 -n 9999 -s _flush 2>/dev/null || true   # swallow stray newline from the pasted block
    read -p $'\033[1;33m>>> Phone connected? Press ENTER to adb push, or type n to skip: \033[0m' padb
    if [[ ! "$padb" =~ ^[Nn]$ ]]; then
      echo -e '\033[1;36m>>> adb devices\033[0m'
      r adb devices
      echo -e "\033[1;36m>>> adb push \"$built\" /sdcard/tmp/$apk_name\033[0m"
      if r adb push "$built" "/sdcard/tmp/$apk_name"; then
        echo -e "\033[1;36m>>> pushed — install /sdcard/tmp/$apk_name via the phone file manager\033[0m"
      else
        echo -e "\033[1;31m>>> push failed — sideload ~/tmp/$apk_name via KDE Connect / Bluetooth instead.\033[0m"
      fi
    else
      echo -e "\033[1;36m>>> Skipped adb push. APK is in ~/tmp/$apk_name — sideload however you like.\033[0m"
    fi
  fi
else
  echo "Aborted."
fi
```

Notes:
- `storeFile` uses the absolute path `/home/shiroikuma/.android-keystores/futokxkb.jks` — `~` does not expand reliably inside gradle `file()` resolution.
- `--console=plain` so the rich console does not overwrite the real failure line (an ArcaneChat-learned trap).
- The build line pipes through a tight `sed -E "/$NOISE/d"` that drops only three benign tool-emitted warning blocks (NDK `CXX5304`, manifest-merger no-op remove/replace, AAPT2 locale-string removal) which have no source-level toggle; `PIPESTATUS[0]` preserves gradle's real exit code so errors and `BUILD FAILED` still surface. All compiler warnings are already killed at source by commit 2. Do not broaden the `NOISE` pattern.
- Two y/n gates: the build gate, then the **LOUD connect-phone gate**. The loud yellow banner is mandatory and must not be softened to a one-line note — the user has flagged its removal before. `read -t 0.5 -n 9999 -s _flush` swallows a stray trailing newline from the pasted block so it cannot auto-answer the prompt.
- Local `~/tmp/` copy is unconditional (runs before the phone gate); only `adb push` sits behind the gate. Never `adb install`/`adb uninstall` — the user installs via the device file manager.
- **Claude Code (running gradle directly, not the pasted shell block): ALWAYS ask whether to `adb push` after every successful build** — the two interactive `read` gates above don't exist when you invoke gradle/adb via the Bash tool, so the "ask before pushing" discipline moves into an explicit `AskUserQuestion` prompt (Push it / Skip) AFTER the build succeeds and the `~/tmp` copy is made, BEFORE any `adb push`. Do the `~/tmp` archive copy unconditionally (as the block does); gate only the push. This is a standing rule the user set — see the `ask-before-adb-push` memory — and applies to every keyboard build, including iterative rebuilds within a session. (Exception: if the user pre-authorizes in the same turn, e.g. "build and push it", just push and report — no need to ask again.)

## Sync to a new upstream version (rebase `custom` onto the new tag)

```bash
r() { "$@" 2> >(sed $'s/.*/\033[1;31m&\033[0m/' >&2); }

echo -e '\033[1;36m>>> cd ~/git/shiroikuma-futokxkb\033[0m'
cd ~/git/shiroikuma-futokxkb

echo -e '\033[1;36m>>> git fetch upstream --tags\033[0m'
r git fetch upstream --tags

echo -e '\033[1;36m>>> git checkout master && ff-only to upstream master (keep mirror current)\033[0m'
r git checkout master
r git merge --ff-only upstream/master
r git push origin master

echo -e '\033[1;36m>>> git checkout custom\033[0m'
r git checkout custom

echo -e '\033[1;36m>>> show what is new since the current base\033[0m'
r git log --oneline HEAD..<new-tag> | head -50

echo -e '\033[1;36m>>> git rebase <new-tag>\033[0m'
r git rebase <new-tag>

echo -e '\033[1;36m>>> git submodule update --init --recursive\033[0m'
r git submodule update --init --recursive

echo -e '\033[1;36m>>> git push --force-with-lease origin custom\033[0m'
r git push --force-with-lease origin custom
```

Then update `target_tag` in the build block to `<new-tag>` and rebuild (N resets to 1 automatically because the base changed). Conflict watch-points on rebase:
- **Commit 1:** `build.gradle` `defaultConfig` (the `applicationId` line, the `ndk { abiFilters }` line) and `strings-appname.xml` (`english_ime_name`). If upstream restructures defaultConfig, re-anchor; resolution rule is always "keep our customization values, take upstream changes around them".
- **Commit 2:** the `defaultConfig` `externalNativeBuild`/`cmake` block, the `kotlinOptions` block, the top-level `JavaCompile` block, and the two `gradle.properties` lines. If upstream itself silences these warnings or changes the version-env reads, a commit may become empty — let git auto-skip it.
- If conflicts feel non-trivial, `git rebase --abort` and re-derive each commit from this skill (each is small) rather than fighting it.

### 0.1.28 → 0.1.29-rc1 sync — reconciliation learnings (the first big rebase)

`0.1.29-rc1` reworked six subsystems our features modify; 12 conflicts over the 97-commit replay. How each was resolved (all confirmed by a clean build; the swipe one confirmed on-device):
- **Upstream added native `rowSpan`** (`4c2d5d30f`) — the same feature as our `heightRows`. **Kept `heightRows` as the author-facing `KeyAttributes` alias** (the YAML emitter, layout editor, Special Keys docs all reference it) but **route it through upstream's `rowSpan` engine**: `BaseKey.computeData` maps `rowSpan = (attributes.heightRows ?: attributes.rowSpan ?: 1)`, dropped our `ComputedKeyData.heightRows` field + our `effHeight`/`finalRowHeights` LayoutEngine logic in favour of upstream's `rowSpanProcessed` (which also auto-gaps the covered cell, so the old hand-placed `gap` cells are harmless no-ops). Don't re-add `ComputedKeyData.heightRows`.
- **Upstream split the spacebar setting** (`614d49f84`) — old single `mSpacebarMode` (0/1/2 + our `=3`) → separate `mSpacebarSwipeMode` + `mSpacebarHoldMode` (each Off/Cursor/Language). **Re-homed our layout-switcher as a new swipe-mode value `SPACEBAR_MODE_LAYOUT_MENU=3`** (renamed from `SPACEBAR_MODE_SWIPE_LAYOUT_MENU`); the PointerTracker swipe branch checks `mSpacebarSwipeMode == LAYOUT_MENU`, long-press is now upstream's `mSpacebarHoldMode` path (dropped our onLongPressed change). **GOTCHA:** upstream's legacy migration only maps old values 0/1/2, so our old `pref_spacebar_mode=3` fell back to "cursor" and the swipe menu silently stopped working — fixed by adding `SPACEBAR_MODE_SWIPE_LAYOUT_MENU_LEGACY=3` and mapping it to the `LAYOUT_MENU` swipe default in `SettingsValues` (commit `spacebar: carry the legacy swipe=layout-menu setting…`). Our Typing-list swipe toggle now coexists with upstream's dedicated Swipe Typing page (both on `PREF_GESTURE_INPUT`).
- **Upstream rewrote `repeatablyClickableAction` / `mergeDuplicates` / action map / `ComputedKeyData`** — our `mergeDuplicates`-disable kept (no-op `= row`); our appended kxkb actions stayed at the tail of `AllActionsMap` (auto-merged after upstream's new `font_typer`); `clusterMains` + the per-key colour/offset fields coexist with upstream's new fields.
- **New submodule:** `java/assets/futo-swipe` (the swipe-typing model, from huggingface.co — reachable; `git submodule update --init --recursive` pulls it).
- **Build-env:** Claude Code's Bash shell doesn't inherit the user's profile → `ANDROID_HOME` unset → gradle "SDK location not found". The build block now exports `ANDROID_HOME=$HOME/android-sdk` (see above).
- **Process:** when a sync conflict re-homes a feature onto a reworked upstream model, grep the **whole `custom` stack** for downstream commits that reference the changed symbols before deciding — several later commits (emitter, editor, Phase 3) depended on `heightRows`, which is why dropping it outright wasn't viable.
- **Cluster prediction silently broke despite a clean replay (the post-rebase follow-up).** Upstream added a new gate in `LanguageModelFacilitator.makePredictionInputValues`: while composing a word, if the new default-off `AllowTransformerOnNonQWERTYLayouts` setting is off, it returns `null` for any layout that isn't QWERTY-shaped (`mId.mKeyboardLayoutSetName == "qwerty"`, or the sorted letters spelling exactly `qwertyuiopasdfghjklzxcvbnm`). Every **cluster layout** is non-QWERTY, so this `null` (a) skipped the transformer path for languages that have a model (English → `predictionInputValues == null` → no `lmResult` → falls to the legacy branch) AND (b) made our `maybeApplyClusterConstraint`'s `makePredictionInputValues(ignorePassthrough=true)` call return null on the no-transformer legacy path (Czech/Russian) — so `processAndMergeSuggestions` (the whole cluster filter + `clusterTrieWalk` + `clusterBaseFold`) never ran and cluster layouts collapsed to the raw dict tap-decoder. **The replay was clean** — our commits were intact — but `ignorePassthrough` only bypassed the OLD `shouldPassThroughToLegacy()` guard, not this new gate. **Fix:** the gate's acceptance test now also accepts any layout carrying a cluster key (`kb.sortedKeys.any { !it.clusterMains.isNullOrEmpty() }` — the same `clusterMains` signal `clusterAllowedSets` keys on); one change fixes both paths, and upstream's gate stays intact for genuine non-QWERTY *non-cluster* layouts. Also assigned upstream's never-set `prevAcceptedLayout` so the scan caches per-layout. This is the exact regression the **`cluster-prediction-testing`** skill exists to catch — its Layer A static audit greps `makePredictionInputValues` for new layout-shape early-returns; its Layer B on-device tracer (`ClusterPredictionDebug`) shows the gate decision per keystroke. **Run that skill's two layers on every future sync** (wired into `upstream-new-version` Steps 4.5 + 6) — getValidNextCodePoints/native, the GeneralIME wiring and `Key.clusterMains` were unchanged this time, but the next sync may move any of them.

### 0.1.29-rc1 → stable 0.1.29 roll-forward — reconciliation learnings (small & mechanical)

When a stable release ships while we're on its rc, `/upstream-new-version` rolls `custom` forward off the rc onto the stable tag (the `sort -V` `~rc` rewrite ranks `0.1.29-rc1 < 0.1.29`). For `0.1.29` this delta was **4 upstream commits**, almost entirely additive, so the 124-commit replay produced only **6 conflicts, all "keep both field"/mechanical** — resolved in place, no re-derivation, confirmed by a clean build (`0.1.29+1`, versionCode `116900001`) and a CLEAN cluster static audit:
- **Upstream threaded a new `swipeLetter` / `swipeCodeOverride` field** through the v2keyboard model (`bf66214bf Add swipeLetter to fix AZERTY bug`): `BaseKey`/`CaseSelector` gain a `swipeLetter: String?`, `ComputedKeyData` gains `swipeLetter`, `Key` gains `swipeCodeOverride: Int?`, and `LayoutEngine.computedKeyToKey` sets `swipeCodeOverride = data.swipeLetter?.let { it.firstNotNullOfOrNull { it.code } }`. These additions land in the **same data classes / `ComputedKeyData` constructions** our per-key fields extend, so the 4 conflicts (`KeyData.kt`, `Key.kt`, `BaseKey.kt`, `LayoutEngine.kt`) were each just "**keep upstream's swipe field AND our field**" (our `clusterMains`, the `color`/`fontScale`/`hintScale` → `colorOverride`/`fontScaleOverride`/`hintScaleOverride` overrides, and the `heightRows ?: rowSpan` alias line). Named args → field order is irrelevant; only comma placement matters.
- **Upstream reshuffled `DefaultActionSettings`** (`9a4479281 Move MemoryDebugAction to favorites for nightly`): `PinnedKey` simplified from `if(IsDebug) MemoryDebugAction else VoiceInputAction` to just `VoiceInputAction`, and `MemoryDebugAction` moved into `Favorites` for debug builds. Our action-bar-launcher commits each *append* to `PinnedKey`, so the 3 `Registry.kt` conflicts (one per launcher commit, replayed in sequence) were resolved by **taking upstream's new base line and re-appending our pinned actions** (`KeySlidingToggleAction, LiveResizeAction, SpecialKeysAction, CustomLayoutsAction`) and **taking upstream's new `Favorites` verbatim** (our commits never intended to touch it — the conflict was only positional). `AllActionsMap` tail-appends auto-merged.
- The other rc1→stable changes (`87ef531b7` de-"alpha" strings, `8ae14c400` submodule bumps to `futo-swipe`/`layouts`) auto-merged / were pure submodule-pointer moves (`git submodule update --init --recursive` syncs them).
- **Takeaway:** a same-version rc→stable roll-forward is the *easy* case — expect only mechanical field/list conflicts where upstream extended a struct our features also extend. The static audit was green because the swipe work doesn't touch the prediction pipeline. (On-device Layer B was offered but the user pushed before testing; the trace toggles stay available for ad-hoc checks.)

## Toolchain prerequisites (diagnosing a failed build)

- **JDK 17/21** (AGP 8.10.1 needs 17+); pin `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64`. If a build fails on the wrong JVM, check `~/.gradle/gradle.properties` and `./gradle.properties` for an `org.gradle.java.home` override (it beats `JAVA_HOME`), and `./gradlew --stop` to kill a stale daemon.
- **Android SDK** platform-35 + build-tools 35.0.0; **NDK 28.2.13676358** + CMake (native JNI). **python3** (preBuild `updateLocales` runs `tools/make-keyboard-text-py/src/generate.py`).
- **Submodules initialized** — without `libs/` the build fails on the `mozc-release`/`rime-release.aar` dependencies. Most are on gitlab.futo.org (fine on the user's machine, unreachable from the sandbox).

## Sandbox hygiene

The sandbox reaches github.com (so `origin/custom` is fetchable for patch generation) but not gitlab.futo.org (so a full submodule init is impossible). Inspect upstream `build.gradle`/manifests/strings from the github clone; never run the keyboard's Gradle/CMake build in the sandbox. To verify a sed/edit, test it against the cloned `build.gradle` before delivering.

## Live look/size knobs (Multiling-style) — LIVE (core set shipped)

A Multiling-O-style per-geometry sizing screen (`KxkbSizingScreen`) with **live sliders + colour pickers** — the docked keyboard updates as the user drags. Replaces hardcoding look values on the `HighContrastYellow` theme. Shipped incrementally `c94a849` (look fields) → `195ccf075` (split snap) → `23ae74876` (per-geometry colours + geometry-overlay sync) → `9a143522c` (cluster main-position sliders + the screen rename "Live sizing" → "Keyboard UI"); see "Implemented features" for the per-commit chain. This section is the architecture map for maintenance / rebases / adding more knobs.

**Settled design decisions (kept):**
- **Per geometry**, keyed off FUTO's `KeyboardSizeSettingKind` — `Portrait`, `Landscape`, `FoldableInnerDisplay` (the Mate XT's 6 fold×orientation states collapse into these). `LastUsedSizeStateSetting` auto-tracks the active geometry.
- **One unified slider screen**, every knob live.

**The backbone is FUTO's per-kind store, now keyed per-combo.** `SavedKeyboardSizingSettings` (`@Serializable data class`, `KeyboardSizingCalculator.kt`) holds the whole blob (sizing + look + colours). Originally persisted **per geometry**: `KeyboardSettings[kind]` maps each `KeyboardSizeSettingKind` to a `SettingsKey` holding the JSON blob. **As of `7b399be0b` it is per-(active subtype × geometry):** a shared top-level `perComboSizingKey(context, kind)` folds the active subtype (the language+layout pair, via `Subtypes.subtypeToString(getActiveSubtype(context))`, sanitised) into a dynamic key `keyboard_settings__<subtype>__<kind>`; it returns `null` when no subtype resolves (BFU / direct-boot) so callers fall back to the geometry key. `getSavedSettings()` reads per-combo → **inherits** the geometry blob → `getDefaultSettingForKind`; `editSavedSettings { it.copy(...) }` reads that same effective value but **writes the per-combo key**, so an unedited combo shows the geometry baseline and **forks to its own blob on first edit** (the three original geometry blobs stay as the read-only inherited baseline — automatic migration, nothing reset). `resetCurrentMode`/`exitOneHandedMode` inherit this for free (they route through `editSavedSettings`). `fromJsonString` is lenient (`ignoreUnknownKeys`), so new fields with defaults deserialize cleanly on old blobs. **The exact same `perComboSizingKey` is used by the calculator, the screen, and the live watcher — never reconstruct the key string inline anywhere, or the three drift and edits silently miss.** That struct is the single source of truth the screen edits.

**Fields on `SavedKeyboardSizingSettings`** (all per-geometry):
- *Sizing* (Phase 0b, `89b727d`): additive `horizontalGapAddDp` / `verticalGapAddDp` (neutral `0`) + top/bottom row-height factors; the existing `heightMultiplier` / `paddingDp` / `splitWidthFraction` / `prefersSplit` are reused for keyboard height / position / split — no new position fields.
- *Look* (Phase 0a, `c94a849`, + hint later): `fontSizeMultiplier`, `labelWeight`, `keyRoundness`, `borderWidthDp`, `hintSizeMultiplier` — each defaults to sentinel **`-1` = inherit the theme**.
- *Colours* (`23ae74876`): 8 nullable packed-ARGB `Int?` (`null` = inherit) — `fontColor`, `secondaryFontColor`, `keyBackgroundColor`, `functionalKeyBackgroundColor`, `keyBorderColor`, `keyboardBackgroundColor`, `suggestionBarColor`, `suggestionTextColor`.

**Two render paths, both live:**

1. **Sizing** rides the per-kind calc rails in `LayoutEngine.kt` / `KeyboardSizingCalculator.kt`:
   - *Gaps* — additive dp: `gap + horizontalGapAddDp.dp` (neutral 0, so it works on the zero-gap HCY theme).
   - *Row heights* — `heightMultiplier` drives overall height (`singularRowHeight`). Top/bottom factors are a **redistribution within the fixed total**: `topRowIndex = rows.indexOfFirst { !isBottomRow }`, the top row is scaled `rowHeight*rowHeightPx*topF` with `topRowExtraWeight` folded into the row-height denominator; the bottom row is pinned `standardRowHeight*bottomF` (Fixed mode). **Critical:** the stock `clampHeight` (ClampHeight mode) pins keys to `standardRowHeight` and turns surplus into vertical gap → "phantom gaps" when resizing; gated off via `liveRowSizingActive = topF != 1.0 || bottomF != 1.0` so keys fill their cells. `topRowHeightFactor` threaded LayoutParams → KeyboardLayoutSetV2Params → `KeyboardSwitcher.java`.
   - *Suggestion-bar height* — the hardcoded `ActionBar.kt val ActionBarHeight = 40.dp` became `LocalActionBarHeight = compositionLocalOf { ActionBarHeight }`, provided at the keyboard root (`UixManager.ProvidersAndWrapper`) from `actionBarHeightState` + `refreshActionBarHeight()` (reads `calculateSuggestionBarHeightDp() = 40*factor`). All composable read sites repointed to `LocalActionBarHeight.current`; non-composable sites read `sizeCalculator.calculateSuggestionBarHeightDp()`. Insets (`calculateTotalActionBarHeightPx`) already matched. `refreshActionBarHeight()` runs in `onSizeUpdated` and the collector.

2. **Look** is overlaid per-build in `LatinIME.withPerKindLook(scheme)`: reads the current geometry's blob and `.copy()`s the `>= 0` fields onto `scheme.extended.advancedThemeOptions` — `keyLetterScale` ← fontSizeMultiplier, `keyLabelWeight` ← labelWeight, `keyRoundness` ← keyRoundness, `keyStrokeWidthDp` ← borderWidthDp, `keyHintScale` ← hintSizeMultiplier. `activeColorScheme` holds the **clean base** scheme; the overlay only enters the provider, so inherit stays correct. Applied at both `BasicThemeProvider` construction sites (`updateDrawableProvider` + the `getDrawableProvider` fallback). Draw sites: main label `* getKeyLetterScale()` (`KeyboardView.java:449`, auto-fit to key width); roundness `keyCornerRadius = 9.dp*roundness` (space `48.dp*roundness`, pill-clamped, `BasicThemeProvider`); **hint scale needed NEW plumbing** — `AdvancedThemeOptions.keyHintScale` → `DynamicThemeProvider`/`BasicThemeProvider` → `* getKeyHintScale()` at the on-key hint draw sites `KeyboardView.java:499 & 537` (long-press popup hint unchanged).

3. **Colours** are overlaid in the SAME `withPerKindLook(scheme)` pass (`23ae74876`): each non-null colour `.copy()`s onto `scheme.base.outline` (key border) or the **extended** scheme — `onKeyboardContainer` ← fontColor, `hintColor` ← secondaryFontColor, `keyboardContainer` ← keyBackgroundColor, `keyboardContainerVariant` ← functionalKeyBackgroundColor, `keyboardSurface`(+`keyboardSurfaceDim`, and clear `keyboardBackgroundGradient`) ← keyboardBackgroundColor. The suggestion bar gets **two dedicated `ExtraColors` fields** (`ColorScheme.kt`): `suggestionBarColorOverride` + `suggestionTextColorOverride` — separate because the bar bg otherwise shares `keyboardSurface` with the keyboard bg, and suggestion text shares `onSurface` with other chrome; `ActionBar.kt` `actionBarColor()`, the suggestion-word colour (~304) and CJK `CandidateItem` colour (~1048) prefer them when set. Compose `Color` import in `LatinIME` is **aliased `as ComposeColor`** (clashes with the existing `android.graphics.Color` used for `Color.BLACK`/`TRANSPARENT`). `lookFieldsDiffer` includes the 8 colour fields. The **Compose chrome (suggestion bar) reads the overlaid scheme**, not the clean one: `LatinIME.overlaidColorScheme` (an `activeOverlaidScheme` mutableState set inside `updateDrawableProvider`/`getDrawableProvider`) is what `UixManager` feeds to `UixThemeWrapper`, so bar/suggestion colours recompose live.

**Live-refresh wiring** (`LatinIME.kt`, the "Listen to size/look changes" `dataStore.data.collect`). Since `7b399be0b` the watcher follows the **dynamic per-combo key** (`perComboSizingKey(this, currentSizeState)`, falling back to the geometry key), NOT the three fixed geometry keys. Each emission it diffs that key + its blob against the previous: **same key + new blob** = a live edit → `invalidateKeyboard(refreshSettings = true)` (recomputes `size.value = calculateSize()` AND `recreateKeyboard()`); **key changed** = a combo switch (language/layout, or geometry after a write) → force `updateDrawableProvider` + `invalidateKeyboard`. A `lookFieldsDiffer(old, new)` check (look, colour, **and** the flick/cluster draw-offset fields) gates whether `updateDrawableProvider(activeColorScheme.value)` also runs so look/colour/offset-only edits repaint without a full rebuild (a key change always forces it). **⚠ TWO gotchas, both have bitten:** (1) **Any NEW per-geometry look/offset field MUST be added to `lookFieldsDiffer`** — it's the gate that decides whether `withPerKindLook` re-runs (the only thing that re-pushes the static offsets to the draw path); omit it and a change to that field alone updates the stored blob but never the screen (first happened with `clusterLeftOffset`/`clusterRightOffset`). (2) **The watcher MUST key off `perComboSizingKey`, not `KeyboardSettings[kind]`** — when the per-combo store landed, leaving the watcher on the geometry keys made every live slider/colour edit a no-op (the edit wrote the per-combo key the watcher wasn't watching; only a geometry rebuild, which re-reads the store directly, showed it). Driving combo switches from this watcher (rather than `onNewSubtype`) is deliberate: `DataStoreHelper`'s cache (read by `getSettingBlocking` → `perComboSizingKey` → `getActiveSubtype`) is updated by a background `dataStore.data` collector, so it trails the datastore by up to one emission; the watcher converges on whichever emission the key actually flips, whereas a cache read inside `onNewSubtype` can fire a beat early on the *old* combo. A secondary `lastProviderSubtype` gate in `recreateKeyboard` (parallel to `lastProviderSizeState`) is kept as a safety net so any recreate path rebuilds the overlay when the subtype changed.

**⚠ Per-geometry GEOMETRY-CHANGE wiring — the overlay is baked into the provider, so a rotate/fold must rebuild it (this bit us hard, `23ae74876`):** the `withPerKindLook` overlay (fonts, roundness, border AND the 8 colours) is baked into the `BasicThemeProvider` + the overlaid chrome scheme at build time. It is otherwise rebuilt only on a look-field/colour edit (collector) or theme change — **never on a pure geometry switch** — so rotating/folding kept the *previous* geometry's look+colours until the next edit (the "stuck, then jumps" symptom). Two fixes, both required:
   - **`onConfigurationChanged` must call `onSizeUpdated()`.** It's the one geometry-changing event that did NOT flow through `onSizeUpdated`, so without it `publishCurrentSizeState()` never fired on rotation → `LastUsedSizeStateSetting` stayed pinned → the settings screen's `kind` stayed on the old geometry → **edits wrote to the wrong geometry's blob** (set in landscape, appeared in portrait, lost in landscape). (Fold already republishes: the `WindowInfoTracker` collector in `UixManager` calls `invalidateKeyboard` → `publishCurrentSizeState`.)
   - **The provider must be re-synced to the live geometry at the recreate chokepoint.** `recreateKeyboard()` now rebuilds the overlay (`if (currentSizeState.name != lastProviderSizeState) updateDrawableProvider(activeColorScheme.value)`) before loading — every recreate path (rotation via `onSizeUpdated`→`invalidateKeyboard`, fold via collector→`invalidateKeyboard`) flows through it. `updateDrawableProvider` records `lastProviderSizeState`. `onSizeUpdated` also ORs `geometryChanged = currentSizeState.name != lastProviderSizeState` into `shouldInvalidateKeyboard` so a recreate is forced even if the two geometries happen to share dimensions. **Why not just rebuild in `publishCurrentSizeState`:** the orientation flag can lag the view resize a beat, so a rebuild there reads the geometry you're *leaving* (off-by-one). Keying off `lastProviderSizeState` at recreate time is timing-proof.

**Split model** (`SplitKeyboardSize` in `KeyboardSizingCalculator.kt`; positioning in `LayoutEngine.kt`):
- `splitLayoutWidth = (displayWidth * splitWidthFraction).coerceInLoosely(dp(48), displayWidth)` — cap is the **full display width** (was `9/10`), so the halves can fully close the centre gap and overlap the duplicated middle column.
- Splittable rows (`splittable = letters != null` → letter/number rows; **the bottom row is non-split**) → `splitRow()` duplicates the middle column on odd layouts → `addRowAlignLeft` (left half flush x=0) + `addRowAlignRight` (right half flush right); centre gap = `displayWidth − leftWidth − rightWidth`.
- **Snap to one column** (`195ccf075`): `splitShouldMerge = isSplitLayout && (layoutWidth + regularKeyWidth > displayWidth)` — past the "doubled-column-adjacent" point (~`maxCols/(maxCols+1)` of the display). When set, the splittable row is rendered **un-split and centred** (`addRow(idx, row.entries, offset, …)`, `offset = (displayWidth − layoutWidth)/2`); un-splitting drops the duplicated column, so the two halves collapse to one continuous column — skipping the partial-offset zone between adjacent and merged.
- **Bottom (action) row** is laid out at the **constant full width** (`unsplitLayoutWidth`), so ALL its key-width bounds must derive from that width or the keys deform as the split narrows. Three repointed to `unsplitLayoutWidth`: `bottomRegularKeyWidth = 0.1f * unsplitLayoutWidth`, `minimumBottomFunctionalKeyWidth = unsplitLayoutWidth * minimumBottomRowFunctionalKeyWidth`, and the shared (split-capped) `functionalWidth` is **rescaled** `functionalWidth * unsplitLayoutWidth / layoutWidth` for the bottom row (shift/delete take the shared functional width, otherwise capped at `layoutWidth*0.15`). All no-ops at 100% split and for non-split layouts. (The user's `kxkb.yaml` sets `minimumFunctionalKeyWidth: 0.0` + `minimumBottomRowFunctionalKeyWidth: 0.0` so functional keys render at regular width, and the spacebars are `Custom1: 0.2` — **not** Grow — which is why the slack surfaced as deformation rather than being absorbed.)

**The screen** — `KxkbSizingScreen` + `KxkbSizingResetMenu` (`Typing.kt`): reuses the stock `SettingSliderForDataStoreItem` with a **synthesized `DataStoreItem`** (`floatItem`) per knob. **As of `7b399be0b` it binds to the per-combo key:** it reads `ActiveSubtype` reactively (`useDataStore`) and computes `perComboSizingKey(context, kind)` in a `remember(activeSub, kind)` (so the sliders rebind when you switch layout/language with the screen open — `useDataStore` reads live from the cache by key, so a changing key just works), reads per-combo → geometry-blob → default for display, and writes the per-combo key via `comboItem.setValue` (forking on first edit). A "language · layout" scope line (built from `Subtypes.getName` + `getLayoutName(SubtypeLocaleUtils.getKeyboardLayoutSetName(st))`) sits above the geometry line so the edited combo is visible. `defaultSizing = getDefaultSettingForKind(kind, context)` supplies the inherit/neutral defaults the reset restores. The top-of-page explainer (`kxkb_sizing_geometry_note`) still notes the geometry scope. **Colours** are 8 expandable `ColorSetting` rows (a private composable + `ColorChannelSlider`); since `4bf0c99af` each sits in the subgroup of the element it affects (Jami-style) rather than in one "Colours" section: each row = a live swatch + an "Inherited" tag (when `null`) that expands to Alpha/Red/Green/Blue channel sliders (reusing `SettingSliderForDataStoreItem`, 0–255), seeded from the active theme via `LocalKeyboardScheme.current` (`.toArgb()`), writing the packed ARGB to the blob via a `writeColor { it.copy(...) }` helper. Reset clears the 8 colours to `null` too. Strings in `java/res/values/strings-uix.xml`. **Page structure (`4bf0c99af`):** controls are grouped by two private header composables — `KxkbSection(title)` (large `Typography.Heading.MediumMl` accent title + full-width 2dp accent `HorizontalDivider`) and `KxkbSubgroup(title) { content }` (accent title with a short underline sized to the text via `Modifier.width(IntrinsicSize.Min)` + `softWrap=false`, then its controls in an indented `Column(padding(start = 32.dp))`). Indents cascade: section start 16dp < subgroup header 24dp < controls 32dp (+ the control's own ~12–16dp). Sections: Size (Height / Spacing / Split mode) · Keys (Text / Shape & background / Hint labels) · Sliding labels (Flick / Cluster positions) · Surfaces & suggestion bar (Keyboard background / Suggestion bar) · Layout switcher. Section/subgroup titles are English literals (no string churn). **To add a knob:** drop a `SettingSliderForDataStoreItem`/`ColorSetting` inside the right `KxkbSubgroup { }` lambda — it inherits the indent automatically; don't place it outside a subgroup.

**Current live knob set** (per-combo since `7b399be0b`; was per-geometry): horizontal gap (0–10dp), vertical gap (0–10dp), keyboard height (`heightMultiplier` 0.5–2.5×), top row (0.5–2.0× redistribute), bottom row (0.2–2.0× redistribute), key font size (0.5–3.0×, inherit→1.4 on HCY), secondary/hint text size (0.5–3.0×, inherit→1.0), key roundness (0–5, inherit→0 on HCY), suggestion-bar height (0.5–2.0×), split width (40–100%), **8 colours** (key font, secondary/hint font, key bg, functional-key bg, key border, keyboard bg, suggestion-bar bg, suggestion font — each RGBA, inherit-by-default), + Reset live sizing.

**Key files:** `…/v2keyboard/KeyboardSizingCalculator.kt` (struct incl. the 8 colour fields, per-kind store, `calculate`, `SplitKeyboardSize`, defaults), `…/v2keyboard/LayoutEngine.kt` (gaps, row heights, split positioning + the merge snap, bottom-row widths), `…/latin/LatinIME.kt` (`withPerKindLook` look+colour overlay, `lookFieldsDiffer`, `overlaidColorScheme`/`activeOverlaidScheme`, `lastProviderSizeState` + `lastProviderSubtype` + the recreate-chokepoint geometry/subtype sync, `onConfigurationChanged`→`onSizeUpdated`, the `dataStore.data.collect` refresh, `refreshActionBarHeight`, `invalidateKeyboard`/`recreateKeyboard`), `…/latin/uix/ColorScheme.kt` (`ExtraColors` + the 2 suggestion overrides), `…/latin/uix/ActionBar.kt` (`LocalActionBarHeight`; suggestion bar/text colour override sites), `…/latin/uix/UixManager.kt` (provides `LocalActionBarHeight`; feeds `overlaidColorScheme` to `UixThemeWrapper`; fold collector), `…/latin/uix/theme/AdvancedThemeOptions.kt` (`keyHintScale` + the look fields), `KeyboardView.java` (letter/hint draw sites), `BasicThemeProvider`/`DynamicThemeProvider` (roundness + hint-scale plumbing; reads colour fields off the scheme), `…/latin/uix/settings/pages/Typing.kt` (`KxkbSizingScreen`, `ColorSetting`/`ColorChannelSlider`, reset), `java/res/values/strings-uix.xml`.

## 4D keys (key sliding) + action-bar quick-access + Special Keys page — LIVE

A "4D keys" feature: a short directional slide off a key types one of up to eight surrounding characters; plus the action-bar plumbing, the reference page, and the compact YAML key types (`compass`/`macro`/`chord`) built around it. Shipped `733df44b6` (toggle) → `1f7ca2ce0` (action icons + live re-apply) → `414affd43` (at-rest labels + state icon + position sliders) → `439d911b2` (Special Keys page) → `fb7b3bba8` (launcher icon + outlined close buttons) → `807432777` (Custom layouts launcher) → `0ccc3ab1f` (compass + macro types, Phase 4a) → `5f0248ab4` (chord type, Phase 4b-1) → `3b59ae18b` (compass slot shorthands, Phase 4b-2) → `05388366e` (cycle/multitap, Phase 4c) → `16d9b57a1` (cluster / predictive multi-key, Phase 4d) → `27eb58a71` (HIDE-keyboard action key + icon flick-label rendering). This section is the architecture map.

**KEY DISCOVERY — FUTO already has a flick engine; "4D keys" ARE flick keys.** `…/v2keyboard/FlickKey.kt` (`@SerialName("flick")`: a `primary` + 8 named directions). `Key.kt`: `flickKeys: Map<Direction, Key>?`, `hasFlick`, `previewLabel`. `KeyData.kt`: `enum class Direction` + a `toVector(dir): Pair<Double,Double>` extension (called from Java as `KeyDataKt.toVector(dir)`; **diagonals are ±0.707, not ±1**). `PointerTracker.java` already implements the flick gesture (gated on `mIsFlickingKey`); `KeyPreviewView.drawFlickKeys()` draws the hold-popup 3×3. So the feature is: expose a toggle, gate the existing flick handling on it, and add at-rest rendering — NOT a new gesture engine.

**Phase 1 — the toggle (`733df44b6`).** "Key sliding (4D keys)" pref, off by default, on the Typing page. `PREF_KEY_SLIDING = "key_sliding"` (`Settings.java`); `mKeySlidingEnabled` on `SettingsValues`, with `mGestureInputEnabled = readGestureInputEnabled(...) && !mKeySlidingEnabled` — **mutually exclusive with glide/swipe typing** (sliding wins; the popup says so). `PointerTracker` static `sKeySlidingEnabled` + `setKeySlidingEnabled(...)` gates `mIsFlickingKey`; `MainKeyboardView.setKeySlidingEnabled()` forwards to it; `LatinIMELegacy.updateMainKeyboardViewSettings()` feeds it on keyboard (re)load.

**Action-bar quick-access (`1f7ca2ce0`; Custom layouts `807432777`).** NEW file `…/latin/uix/actions/KxkbActions.kt` holds all kxkb-specific actions (keep them here, not scattered through stock files). Four now live there: `KeySlidingToggleAction` (icon `swipe_icon`, flips `PREF_KEY_SLIDING` directly via `PreferenceUtils.getDefaultSharedPreferences(...).edit { }`), `LiveResizeAction` (icon `aspect_ratio`, opens the Live sizing page), `SpecialKeysAction` (icon `book`, opens the Special Keys page), `CustomLayoutsAction` (icon `code`, opens Developer-settings → Custom layouts, route `devlayouteditor` — which got top/bottom OutlinedButton close buttons in `DevLayoutEditor.kt`, same as the other pages). Registered in `Registry.kt`: **appended to the END of `AllActionsMap`** (`"key_sliding_toggle"`, `"live_resize"`, `"special_keys"`, `"custom_layouts"`) — the map position is the persisted action id, so it is **append-only; never reorder** — and added to `DefaultActionSettings`' `PinnedKey` list. Strings in `strings-uix.xml`. **Because the user's action bar is already customized, new pinned actions surface in the More/… editor, not auto-pinned — the user pins manually.**

- **Deep-link from an action into a settings sub-page.** `LiveResizeAction`/`SpecialKeysAction` build an `Intent` to `SettingsActivity` with `putExtra("navDest", <route>)` and flags `NEW_TASK | SINGLE_TOP | CLEAR_TOP` (the working ThemeAction pattern — **`SINGLE_TOP`, not `RESET_TASK_IF_NEEDED`**, which reused the activity WITHOUT re-running `onCreate` → opened root). `SettingsActivity` reads the extra in a `private fun navigateToNavDest(intent)` called from `onCreate` AND from an `override fun onNewIntent(...)`, so a SECOND tap of an already-open settings activity still navigates (without `onNewIntent` the reused activity ignores the new extra → "2nd tap opens root").

- **Live re-apply of the toggle (`1f7ca2ce0`).** Flipping the pref had no live effect (the slider→view path, `updateMainKeyboardViewSettings()`, is otherwise only reached on keyboard (re)load). Extended the `LatinIME` `settingsChangedListener` (~610, which already re-applied on action-key changes) to ALSO, when `mKeySlidingEnabled`/`mGestureInputEnabled` differ AND `latinIMELegacy.mKeyboardSwitcher.mainKeyboardView != null`, call `latinIMELegacy.updateMainKeyboardViewSettings()` — **the null-guard matters**, that method dereferences the view unguarded. `Settings.onSharedPreferenceChanged` → `loadSettings` rebuilds `SettingsValues` and fires the listener.

- **State-aware action icon (`414affd43`).** `Action.kt` gained an optional trailing `activeStateProvider: (@Composable () -> Boolean)? = null`. `ActionBar.kt` `ActionItem`/`ActionItemSmall`: when active, the chip gets a FILLED `primary` background + inverted (`onPrimary`) glyph — chosen over a tint because under HighContrastYellow `primary == foreground`, so a tint would be invisible. `KeySlidingToggleAction` sets `activeStateProvider = { !keySlidingEnabledState() }` (filled = swipe active = sliding OFF, per the user's mental model).
  - **⚠ GOTCHA that crashed the whole keyboard ("Shared prefs cache was not provided!"):** `useSharedPrefsBool`/`useSharedPrefsGeneric` (`Hooks.kt`) HARD-REQUIRE a `SharedPrefsCacheProvider` (`LocalSharedPrefsCache`) that `SettingsActivity` provides but the **keyboard action bar does NOT** → they throw during compose-measure and take down the keyboard. So **action-bar composables must NEVER use `useSharedPrefs*`.** `KxkbActions` instead has a private `@Composable fun keySlidingEnabledState()` reading `SharedPreferences` directly and observing changes via `DisposableEffect` + `OnSharedPreferenceChangeListener` (cache-free).

**At-rest 4D labels (`414affd43`).** When sliding is on, the eight directional characters are drawn small on the key face so they're discoverable without holding. In `KeyboardView.onDrawKeyTopVisuals` (~425): compute `drawFlickFace = PointerTracker.isKeySlidingEnabled() && key.getFlickKeys()` non-empty; when true, SUPPRESS the corner hint label, the hint icon, AND the "…" popup-hint dot (`&& !drawFlickFace` added to all three) so they don't collide. New `private void drawAtRestFlickLabels(...)` draws each direction's `previewLabel` in a **3×3 grid**, snapping each direction to a cell with `Math.signum(vec.first / vec.second)` rather than placing on the raw `toVector` circle (the 0.707 diagonals would bunch), sized by the secondary-hint font (`kdc.getHintSize() * getKeyHintScale()`); empty slots skipped. Imports added to `KeyboardView.java`: `Direction`, `KeyDataKt`, `kotlin.Pair`, `java.util.Map`. `PointerTracker` gained `public static boolean isKeySlidingEnabled()`. `MainKeyboardView.setKeySlidingEnabled()` also calls `invalidateAllKeys()` so labels appear/vanish instantly on toggle, not on next touch.

- **4 per-geometry label-position sliders (`414affd43`) — ride the SAME live-sizing plumbing as the look knobs.** Added 4 float fields to `SavedKeyboardSizingSettings` (`flickLabelTopOffset = 0.30f`, `flickLabelBottomOffset = 0.40f`, `flickLabelLeftOffset = 0.34f`, `flickLabelRightOffset = 0.34f` — fractions of the key from centre; top/bottom and left/right independent; the lenient deserializer gives old blobs the defaults). `LatinIME.withPerKindLook` pushes them to `KeyboardView` via a NEW static `setFlickLabelOffsets(top,bottom,left,right)` (4 statics, defaults matching). Added the 4 fields to `lookFieldsDiffer`, so a drag rebuilds/redraws live. `drawAtRestFlickLabels` places cells off the statics: `sx>0`→left col `cx − left*kw`, `sx<0`→right col `cx + right*kw`, `sy>0`→top row `cy − top*kh`, `sy<0`→bottom row `cy + bottom*kh`. 4 sliders on `KxkbSizingScreen` (after Secondary text size), `SettingSliderForDataStoreItem` + `floatItem`, range 0..0.6. Strings `kxkb_sizing_flick_{top,bottom,left,right}`.

**Special Keys reference page (`439d911b2`, close buttons/styling `fb7b3bba8`).** NEW file `…/latin/uix/settings/pages/SpecialKeys.kt`: `SpecialKeysScreen(navController)` = `ScrollableList { ScreenTitle + close button + Tip + 3 CollapsibleSection (Escape `!code/key_escape`, Control one-shot `!code/key_ctrl`, 4D keys `type: flick`) + close button }`. Private `Para` (body) / `Yaml` (monospace `Surface`) helpers; content is inline English (a personal reference page — no string resources for the body, only the page title/subtitle). Route registered in `SettingsNavigator.kt` (`composable("specialKeys") { SpecialKeysScreen(navController) }`); reachable both from a `userSettingNavigationItem` (book icon) in `KeyboardSettingsMenu` (`Typing.kt`) and from the `SpecialKeysAction` launcher. Strings `special_keys_title`/`special_keys_subtitle`.
- **Close buttons + styling (`fb7b3bba8`).** Top + bottom "✕ Close — back to keyboard" buttons that `context.findActivity()?.finish()` (one-tap return to the keyboard, since the page is usually opened from the action bar) — the Special Keys page factors this into a shared private `CloseToKeyboardButton()`; the Live sizing page has two inline copies. All of them were restyled `Button` → **`OutlinedButton`** with `colors = ButtonDefaults.outlinedButtonColors(contentColor = primary)` + `border = BorderStroke(1.dp, primary)` → border + text in the accent colour, transparent fill (reads as yellow-on-black under HCY). They reuse the `kxkb_sizing_close_to_keyboard` string.

**Compact special-key YAML types — Phase 4 (SHIPPED: 4a compass+macro `0ccc3ab1f`, 4b-1 chord `5f0248ab4`, 4b-2 slot shorthands `3b59ae18b`, 4c cycle `05388366e`, 4d cluster `16d9b57a1` — vocabulary complete).** New polymorphic `AbstractKey` subtypes in NEW `…/v2keyboard/CompassKey.kt` (`AbstractKey` is a `sealed interface` → subtypes auto-register via `AbstractKey.serializer()`; the layout config uses `EmptySerializersModule`, so there is **no manual subclass list** to update — just add the `@SerialName` class in the `v2keyboard` package):
- **`compass`** (`@SerialName("compass")`) — sugar over flick: a `primary` key + a compact `slide:` string (one codepoint per slot, order **up, down, left, right, up-left, up-right, down-left, down-right**; a space skips a slot, a short string leaves the rest empty) PLUS optional per-direction overrides (`up`/`down`/…/`downRight`, each any `Key`; overrides win). **Crucially compass does NOT delegate to FlickKey** — FlickKey casts each direction `as? BaseKey` and silently drops non-base slots, so compass builds the `ComputedFlickData` map itself (BaseKey slots via `computeDataWithExtraAttrs`+extraAttrs, any other key via `computeData`), letting macro/chord slots work. Compact-string slots are made literal (`useKeySpecShortcut=false`, `moreKeyMode=OnlyExplicit`) so `$`/`^`/`{` stay literal. Slot order maps U/D/L/R/UL/UR/DL/DR → North/South/West/East/NorthWest/NorthEast/SouthWest/SouthEast. Diagonal slides with empty diagonals snap to the nearest filled cardinal (FUTO's `computeDirectionsFromDeltaPos` dot-product fallback — user fine with it).
- **`macro`** (`@SerialName("macro")`) — types a literal string (optional shorter `label`). Builds its ComputedKeyData directly (like GapKey) with `code = CODE_OUTPUT_TEXT` + `outputText = text`, bypassing the keyspec parser so the text needs no escaping (`|`, `\`, leading `!`, commas all literal). Standalone or in a compass slot.
- **`chord`** (`@SerialName("chord")`) — emits modifier+keystroke chords as real key events, `keys: "C-x C-s"` (Emacs notation: each space-separated step is MOD-…-BASE, MOD ∈ `C`/`M`/`S`/`s`, BASE a single char (`C-x`, `C--`) or named TAB/RET·ENTER/SPC·SPACE/ESC/DEL/UP/DOWN/LEFT/RIGHT/HOME/END/PGUP/PGDN/F1–F12; **Shift must be explicit**). **Carrier trick:** the spec rides in `outputText` behind a leading **`\u0000` sentinel** (impossible in real text); `InputLogic.onTextInput` checks `rawText.startsWith("\u0000")` at the very top and calls `performChord(...)`, which flushes composition (`commitTyped` + `mConnection.send()`) then parses each step and emits via `sendDownUpKeyEvent(keyCode, meta)` — reusing the Ctrl-one-shot's `androidKeyCodeForCodePoint` for char bases + new `keyCodeForChordBase` (named keys) + `chordModifierMeta` (Emacs letter → Android meta). **No new code constant, no listener/PointerTracker change.** `M-` → Android **Alt** meta (works if the app treats Alt as Meta; else switch `M-` to ESC-prefix encoding). Field is named `keys` (user OK'd). Standalone or in a compass slot.
- Both macro (`CODE_OUTPUT_TEXT`) and chord (the `\u0000` sentinel inside outputText) route through the existing `PointerTracker.detectAndSendKey → onTextInput` path, so they fire identically whether tapped as a standalone key or activated as a flick/compass direction.

Documented on the Special Keys page (compact + explicit `compass`, `macro`, `chord` sections). **Gotcha:** a bare inline map without `type:` in a slot fails kaml with *"Property 'type' is required but it is missing"* — until the shorthand lands, slots must use the full `type: macro` / `type: chord` form.

**Phase 4b-2 — compact slot shorthands (SHIPPED `3b59ae18b`).** The eight compass direction fields changed from `Key?` to `Slot?`, where `typealias Slot = @Serializable(with = CompassSlotSerializer::class) CompassSlot` (mirrors FUTO's `typealias Key = @Serializable(with = KeyPathSerializer::class) AbstractKey`). `CompassSlot` is a plain `sealed interface` (NOT `@Serializable` — the custom serializer handles it) with `KeySlot(key: Key)` and `ModSlot(mod, on?, label?)`. `object CompassSlotSerializer` peeks at `(decoder.beginStructure(descriptor) as YamlInput).node` (the `ClassOrScalarsSerializer` pattern): if it's a `YamlMap` whose keys (`node.entries.keys.map { it.content }`) **lack `type`** and contain `mod`/`chord`/`macro`, it decodes a private `@Serializable data class` (`ModShorthand`/`ChordShorthand`/`MacroShorthand`) → `ModSlot` / `KeySlot(ChordKey(keys))` / `KeySlot(MacroKey(text))`; otherwise it falls back to `KeySlot(decodeFromYamlNode(KeyPathSerializer, node))` (scalar, list, or `type:`-tagged map = a normal key). chord/macro desugar immediately (they don't need the primary); **`mod` defers** to a `ModSlot` because it needs the primary's character, which only `CompassKey` knows. `CompassKey.computeData` now extracts `primaryChar` from the primary's `ComputedKeyData` (`code > 0 → String(Character.toChars(code))`, else its `label`) and `resolveSlots(primaryChar)` converts each `Slot` via `slotToKey`: `KeySlot → key`; `ModSlot → ChordKey(keys = modPrefix(mod) + "-" + (on ?: primaryChar), label)`. `modPrefix`: ctrl/control→`C`, alt/meta/opt/option→`M`, shift→`S`, super/win/cmd/gui→`s` (also accepts the single letters). So on primary `o`: `{ mod: ctrl }` = `C-o`, `{ mod: alt }` = `M-o`, `{ mod: ctrl, on: x }` = `C-x`; plus `{ chord: "C-x C-s" }` and `{ macro: "etc. " }`, all taking an optional `label`. **GOTCHA (now fixed by this commit):** before 4b-2 a typeless inline map in a slot failed kaml with *"Property 'type' is required"*; the shorthands now resolve that — but a standalone (non-slot) special key still needs the full `type:` form (the shorthand serializer is only on compass slots). Documented on the Special Keys page (compass slot shorthands). **Remaining at 4b-2:** see Phase 4c below.

**Phase 4c — `cycle` (multitap) key (SHIPPED `05388366e`).** New `CycleKey` (`@SerialName("cycle")`) in `CompassKey.kt`. Repeated taps cycle through a set, each tap deleting the previously-committed entry and committing the next (wrapping); the multitap window lapsing, the cursor moving, or any other key ends the cycle so the next tap starts fresh. **`taps`** is a string (one tap per codepoint) OR a list (multi-character entries), via `@Serializable(with = TapsSerializer::class) data class Taps(items: List<String>)` whose serializer peeks the node (`YamlScalar` → `content.codePoints()` each → one tap; `YamlList` → each entry a tap). **Carrier:** like macro/chord the tap-set rides in `outputText`, here behind a leading **`\u0001` sentinel with entries joined by `\u0001`** (both impossible in real text). `InputLogic.onTextInput` intercepts `startsWith("\u0001")` (beside the chord `\u0000` check) → `performCycle`, which keeps **state** (`mCycleTaps`/`mCycleIndex`/`mCycleTime`/`mCycleAnchor`): a tap continues only if the SAME tap-set is re-tapped within the window AND the caret is still where the last commit left it (`getExpectedSelectionStart() == mCycleAnchor`, start==end); else fresh. Continuing deletes `mCycleTaps[mCycleIndex].length()` UTF-16 units (`deleteTextBeforeCursor` → `deleteSurroundingText(n,0)`, so `.length()` is the right count even for astral/multi-char entries) then advances `(index+1)%n` and `commitText`s. Commits directly via the connection (composer flushed via `commitTyped` first); **no composer/suggestions involvement, no new code constant, no listener/PointerTracker change.** **The multitap window is a SETTING:** `Settings.PREF_MULTITAP_TIMEOUT = "kxkb_multitap_timeout"` + `readMultitapTimeout(prefs)` (default 800ms) → `SettingsValues.mMultitapTimeout` (read in `loadSettings`, so it re-applies live on pref change) → read by `performCycle`. Exposed as a `SettingSliderSharedPrefsInt(key = "kxkb_multitap_timeout", default = 800, range 300..1500ms)` in a new **Cycle section on the Special Keys page**. 5 files: `CompassKey.kt`, `InputLogic.java`, `Settings.java`, `SettingsValues.java`, `SpecialKeys.kt`. **Invalidation is implicit** (caret-anchor + timeout) — no hot-path reset hook; the only false-resume is manually moving the caret back to the exact anchor within the window, transient and harmless.

**Phase 4d — `cluster` (predictive multi-key) (SHIPPED `16d9b57a1`).** New `ClusterKey` (`@SerialName("cluster")`) in `CompassKey.kt` — the old Multiling `3+2`. A wide key carries a band of N **main** glyphs (`main: "aev"`, left→right); a plain tap commits the **centre** main, but the real touch position is decomposed across ALL the band's letters so the language model picks one from word context. Unlike chord/macro/cycle it does **NOT** intercept `onTextInput` — the tap is an ordinary centre-main keypress; the predictive behaviour is entirely a spatial-model injection. **A cluster IS a compass whose middle row is the band:** the desugar builds a compass-shaped `ComputedKeyData` with `code` = centre main (= `main[n/2]`), flick `West` = first main / `East` = last main (precise side access by a slide), and the six diagonal/vertical slots = "extras" (any key or `{ mod }`/`{ chord }`/`{ macro }` shorthand — the SAME `Slot`/`CompassSlotSerializer` machinery as compass). `left`/`right` are reserved (the side mains); only `up`/`down`/`upLeft`/`upRight`/`downLeft`/`downRight` are author-settable. It tags the `ComputedKeyData` with **`clusterMains: List<ClusterMain>`** (`ClusterMain(codePoint, column, columnCount)` — defined in `KeyData.kt`), carried onto `keyboard.Key` in `computedKeyToKey`.

**The prediction hook (the whole point) is pure Java in `ProximityInfo.createNativeProximityInfo` — NO native/C++ change.** FUTO's transformer decoder (on by default, `PREF_KEY_USE_TRANSFORMER_LM`) disambiguates each tap via the native `proximity_info.h::decomposeTapPosition(x,y)`, which is purely geometric: it overlaps a tap-radius circle (`MOST_COMMON_KEY_WIDTH*0.292`) with every key's rect and weights `getKeyCodePoint(key)` by the overlap, feeding the top `NUM_TOKEN_MIX` (=4) letters as a per-position distribution to `PredictCorrection` (the literal typed char is just an a–z gate). Those native key arrays (`keyXCoordinates/keyCharCodes/…`) are **built in Java and the native side consumes whatever list it's handed** — so `clusterSubKeys(key)` emits **one tiled sub-rect entry per LETTER main** (column `c` of `N` → `x = keyX + c*keyW/N`, full key height, code = that main) INSTEAD of the key's single full-width entry, wired into `getProximityInfoKeysCount`, the coordinate-array fill, AND the sweet-spot loop in lockstep (so the arrays stay length-aligned with `keyCount`). A tap then decomposes across the band: centre weighted highest (tap centred on it), sides live, and leaning left/right nudges the weights — exactly Multiling. **Letter-mains-only:** non-letter mains (`(`, `;`, digits) still commit (centre-tap or slide) but get no proximity entry — the transformer ignores non-letters anyway, and skipping them keeps the entry count under `MAX_KEY_COUNT_IN_A_KEYBOARD = 64` (the hard native cap; mind it for big cluster layouts). The real `(x,y)` reaches the composer only because `hasProximityCharsCorrection(code)` is true — v2 layouts set `mProximityCharsCorrectionEnabled = true` unconditionally and the gate also passes for any letter / the whole alphabet element; on a non-letter-only path it would send `NOT_A_COORDINATE` and the spatial decomposition would not run.

**Rendering** (`KeyboardView.onDrawKeyTopVisuals` → new `drawClusterMains`): the band draws at the PRIMARY letter size on the centre line — the centre main at the key centre, the left/right outer mains stepped out by **per-geometry sliders** `clusterLeftOffset`/`clusterRightOffset` (fraction of key width per column-step, default `0.333f` = evenly tiled `1/6·1/2·5/6`; pushed by `LatinIME.withPerKindLook` → `KeyboardView.setClusterMainOffsets` → statics `sClusterLeftOff`/`sClusterRightOff`). These offsets are **display only** — the proximity sub-rects (prediction) stay uniform thirds, so moving the glyphs never changes typing. NB the first cut tiled uniformly with NO knobs; the sliders were added next (commit `9a143522c`). **GOTCHA that bit us:** the sliders moved the stored value but not the glyphs, because the live-apply gate `LatinIME.lookFieldsDiffer(oldBlob,newBlob)` (decides whether a sizing-screen change rebuilds the drawable provider, which is the ONLY thing that re-runs `withPerKindLook` and re-pushes the static offsets) enumerates look-affecting fields explicitly — any NEW per-geometry look/offset field MUST be added to `lookFieldsDiffer` too, or a change to it alone is treated as "nothing relevant changed" and never reaches the draw path. The single label is suppressed for clusters; `drawAtRestFlickLabels` skips `West`/`East` for a cluster (they're the band, drawn at primary size) and draws the ≤6 extras small via the existing top/bottom/left/right "4D label" offset sliders. Authoring: give the key a wide width (e.g. a `Custom2` override) so the band fits, and key sliding ON to reach the side mains + extras (prediction itself works regardless of the sliding toggle). 9 files across the two commits: `CompassKey.kt`, `KeyData.kt`, `Key.kt`, `LayoutEngine.kt`, `ProximityInfo.java`, `KeyboardView.java`, `SpecialKeys.kt` (Phase 4d `16d9b57a1`); `KeyboardSizingCalculator.kt`, `LatinIME.kt`, `Typing.kt` + the above `KeyboardView.java`/`SpecialKeys.kt`/`strings-uix.xml` (sliders + rename `9a143522c`). Empirical knob if prediction reads too loose: widen the centre sub-rect so it dominates unless you lean. Documented on the Special Keys page (Cluster section). **The per-geometry sizing screen was renamed "Live sizing" → "Keyboard UI"** in `9a143522c` (`action_live_resize` = "Keyboard UI", `kxkb_sizing_title` = "Keyboard UI (kxkb)"); a few internal code comments still say "Live sizing" (cosmetic, left).

**Locked vocabulary COMPLETE** — compass / macro / chord / cycle / cluster all shipped. Verbose `type: flick` remains the lowest-level escape hatch; native C++ is the escape hatch only if a future need exceeds the 64-key cap or the 4-candidate (`NUM_TOKEN_MIX`) ceiling.

**Key files:** `…/v2keyboard/FlickKey.kt` + `Key.kt` + `KeyData.kt` (the flick model — `Direction`/`toVector`/`ComputedKeyData`), `…/v2keyboard/CompassKey.kt` (NEW — the `compass`, `macro`, `chord` key types), `…/v2keyboard/BaseKey.kt` (the `Key` polymorphic serializer — scalars → `BaseKey`), `PointerTracker.java` (`sKeySlidingEnabled` gate + `isKeySlidingEnabled()`; `detectAndSendKey` routes `CODE_OUTPUT_TEXT` → `onTextInput`), `MainKeyboardView.java` (`setKeySlidingEnabled` + `invalidateAllKeys`), `KeyboardView.java` (`drawAtRestFlickLabels`, the suppress-hint gating, `setFlickLabelOffsets` statics), `Settings.java` / `SettingsValues.java` (`PREF_KEY_SLIDING`, `mKeySlidingEnabled`, glide mutual-exclusion), `LatinIME.kt` (the live re-apply listener extension; `withPerKindLook` pushing flick-label offsets; `lookFieldsDiffer`), `LatinIMELegacy.java` (`updateMainKeyboardViewSettings`; `onTextInput`), `inputlogic/InputLogic.java` (the chord `\u0000` intercept + the cycle `\u0001` intercept in `onTextInput`; `performChord`/`emitChordStep`/`chordModifierMeta`/`keyCodeForChordBase`; `performCycle` + the `mCycle*` multitap state; alongside the Ctrl one-shot `handleCtrlComboEvent`/`androidKeyCodeForCodePoint`/`sendDownUpKeyEvent`), `Settings.java` / `SettingsValues.java` (also `PREF_MULTITAP_TIMEOUT`/`readMultitapTimeout` → `mMultitapTimeout` for cycle), `…/latin/uix/actions/KxkbActions.kt` (the 4 kxkb actions + `keySlidingEnabledState`), `Registry.kt` (append-only registration + PinnedKey), `Action.kt` / `ActionBar.kt` (`activeStateProvider` + filled-bg active state), `SettingsActivity.kt` (`navigateToNavDest` + `onNewIntent` deep-link), `…/settings/pages/SpecialKeys.kt` (the page — Esc/Ctrl/compass/macro/chord/cycle/cluster/Hide-keyboard/alt-pages), `…/settings/pages/DevLayoutEditor.kt` (Custom layouts page + its close buttons), `SettingsNavigator.kt` (routes), `…/settings/pages/Typing.kt` (Special Keys nav item + the 4 flick-label sliders), `KeyboardSizingCalculator.kt` (the 4 `flickLabel*Offset` fields), `strings-uix.xml`.

## Backlog / later

- **`MoreKeysBuilder` crash on all-non-counting letter rows — fix at source (found via the layout-conversion work).** `MoreKeysBuilder.kt` `getNumForCoordinate` (~line 21) and `symsForCoord` (~line 41) index `keyCoordinate.measurement.numColumnsByRow[keyCoordinate.regularRow]` **directly**, so a letter row whose keys are all *non-counting* (clusters / compass) — i.e. `regularRow` past the end of `numColumnsByRow` — throws `IndexOutOfBounds` and crashes layout build, unless every such key is `moreKeyMode: OnlyExplicit` (which skips the auto-num/sym coordinate path entirely). Compass keys already default to `OnlyExplicit` (`CompassKey.kt`), but the crash still forces converted layouts to carry the workaround on other non-counting keys. **Fix:** use `numColumnsByRow.getOrNull(regularRow)` and early-`return ""` on null, mirroring `symsForCoord`'s existing `QwertySymbols.getOrNull(...) ?: return ""` guard one line up. One-file change; lets layouts drop the per-key `OnlyExplicit` workaround. NB the **lever that suppresses auto-moreKeys is `moreKeyMode` (→ `OnlyExplicit`), not `useKeySpecShortcut`** — the latter only governs the keyspec shortcut table (`$`→currency); see `BaseKey.kt` `getEffectiveAttributes` (~line 370) and the `multiling-futo-conversion` skill, which now documents both levers correctly.

- Cog/settings key isn't placed in the `kxkb` layout yet (the `kxkb_settings` icon is ready for when it is).
- **Live-knob wishlist not yet exposed as sliders.** The `labelWeight` (bold) and `borderWidthDp` (border width) look fields exist on `SavedKeyboardSizingSettings` and `withPerKindLook` already overlays them — they just lack a slider on `KxkbSizingScreen`. Adding one is trivial: synthesize a `floatItem` like the other knobs. Also still on the global path, not per-geometry: the hints/"symbol above letter" toggle (stock `KeyHintsSetting`), and X/Y/W/H position (stock `paddingDp` via the drag-resizer rather than sliders).
- The key **font-size** knob (`a146bbd27`) drives `keyLetterScale` for *text*; it does **not** re-couple **icon** size (icons stay the honest key-fraction `1.0f` from the icon work, so they can't overflow). Bounded re-coupling of icon size to the font knob is a possible future tweak.
- **Deferred Multiling "Size|Pos" extras (out of live-knobs v1 by choice):** the Multiling page also had Wallpaper, "ABC", and "Show symbols" toggles, plus drag-the-corner move/resize on the live preview. Intentionally excluded. Revisit only if the user asks.

## Swipe-space layout switcher (Multiling-style, COMPLETE — phases 1–3 shipped, latest `dfcd1002a`)

A Multiling-style fast switcher: swipe off the spacebar to open a panel covering the keyboard, slide the finger onto a cell, release to switch — all in one motion. It is a 3-column grid: left = settings shortcuts, middle = other languages, right = current-language layouts, each column with a header on a shared top row ("Keyboard UI" → the sizing screen, "Keyboards…" → the system IME picker, "Custom Layouts" → the Custom-layouts editor). The panel is **compact** (anchored near the swipe, fitted to the keyboard width), not full-width, so short reaches work in the wide unfolded geometries. Phasing (all done): (1) gesture + overlay + the **layouts column** `796423b8c`; (2) the **languages** column + **recency tracking** `2e08b1cbf`; (3) the **header row** + **configurable left-column shortcuts** `dfcd1002a`. This section is the architecture map.

**The model that makes the single-motion work — `MoreKeysPanel` as an overlay.** A Compose dialog (like `showLanguageSwitcher`) can't do single-motion because it's a separate window that wouldn't receive the *continuing* spacebar touch. FUTO's own more-keys-panel infra already does "open mid-gesture → track finger → release-to-select", so `LayoutSwitcherView` (`…/keyboard/LayoutSwitcherView.java`, NEW) implements `MoreKeysPanel` and `PointerTracker` drives it for free: once `mMoreKeysPanel` is set, `PointerTracker.onMoveEvent`/`onUpEventInternal` translate the touch and forward `onMoveEvent`/`onUpEvent` to the panel, then dismiss after up. To open it, `PointerTracker.openLayoutSwitcherPanel()` mirrors the long-press `showMoreKeysKeyboardOnPointer` path: `sDrawingProxy.showLayoutSwitcher(this)` (new `DrawingProxy` method, impl in `MainKeyboardView`) builds + shows the view, then `onDownEvent` + assign `mMoreKeysPanel`.

**The identity-coordinate trick (the bit that's easy to get wrong otherwise).** `PointerTracker` forwards keyboard-local coords. Rather than do the `MoreKeysKeyboardView` container/window-origin/`getDefaultCoordX` translation dance, `LayoutSwitcherView` is sized to **exactly cover the MainKeyboardView** and positioned at its window origin inside `mDrawingPreviewPlacerView` (via `showInParent`, `setTranslationX/Y(mWinX/mWinY)` from `parentView.getLocationInWindow`). So keyboard-local == panel-local and `translateX/Y` are the **identity**. All drawing + hit-testing happen in keyboard-local coords; the view renders the **N columns side by side** (left shortcuts, middle languages, right layouts). Each column is sized to its own longest label but **every column is capped to an equal `avail/nCols` share so all three always fit the keyboard width** (text is `clipRect`-clipped per cell so a long label can't overflow), and the block is centred on the swipe x (clamped on-screen). Within a column, entries stack **upward** from a baseline just above the finger (index 0 = bottom = nearest, so the most-recent is closest); the **header sits on a single shared top row** across all columns (row index = max entry count), so headers line up even when columns differ in length. A dim scrim (`0xB0000000`) backs it; colours come from the provider (`keyboardColor`/`keyColor`/`moreKeysTextColor`), the highlighted cell inverts (text-colour bg + keyboard-colour text), **header cells fill with `keyboardColor`** to read as section tops vs the `keyColor` entry cells, and the active subtype is marked with a bullet. `onShowMoreKeysPanel` must run **after** `mKbWidth/mKbHeight` are set (they feed the `showInParent` LayoutParams).

**The trigger — a new spacebar mode.** `Settings.SPACEBAR_MODE_SWIPE_LAYOUT_MENU = 3` (alongside the existing 0/1/2; picker + label in `Typing.kt` / `strings-uix.xml`). In `PointerTracker`, the spacebar sliding branch (the `mIsSlidingCursor && CODE_SPACE` block in `onMoveEventInternal`): when mode == LAYOUT_MENU and `!mSpacebarLongPressed`, a real swipe (`abs(x-mStartX) > sPointerStep` past the swipe-ignore time) calls `openLayoutSwitcherPanel()` and returns (no cursor move); once the panel is open, `onMoveEvent`'s `isShowingMoreKeysPanel()` branch takes over so this code isn't re-reached. **Long-press → cursor**: LAYOUT_MENU is added to the same `onLongPressed` branch as `SWIPE_LANGUAGE` (sets `mSpacebarLongPressed`, `onMovingCursorLockEvent(true)`), after which the sliding branch falls through to `onMovePointer`. Net: swipe → menu, long-press+drag → cursor, exactly as chosen.

**Subtype model (how cells map to switches).** A FUTO subtype IS a `(language, layout)` pair stored as `locale:KeyboardLayoutSet=<layout>:<langtag>`; the enabled `SubtypesSetting` list is the user's languages-and-layouts, `ActiveSubtype` is current. Every cell (layout OR language) carries a target subtype string; selecting it just calls `Subtypes.switchToSubtypeString` → writes `ActiveSubtype` → `LatinIME` observes it and performs the actual switch. Helpers in `Subtypes.kt`: `getActiveSubtypeString`; `getCurrentLanguageLayouts` (enabled subtypes whose language matches the active one — `langOf(sub)` = `getLocale(sub.split(":")[0]).language` — ordered **active-first → recency → enabled**, capped 4, as `(subtypeString, getLayoutName)` pairs) for the layouts column; and `getOtherLanguageEntries` for the languages column (distinct **other** languages among enabled subtypes, ordered recency-then-enabled, capped 4; each entry targets that language's **last-used layout** — first recency hit for the language, else its first enabled subtype — labelled with the native name via `getLocaleDisplayName(locale, locale)`). Since phase 3 the panel **always opens** (the headers + at least one shortcut are always present), so there is no longer a no-switch no-op guard.

**Recency tracking (phase 2).** `LayoutRecency = SettingsKey(stringPreferencesKey("kxkb_layout_recency"), "")` stores recently-active subtype strings newline-joined, most-recent first (a subtype string can't contain `\n`). It is updated from the **central** `LatinIME` `ActiveSubtype` observer (`onNewSubtype`, the `dataStore.data.collect`), so EVERY switch is captured — panel, language dialog, swipe-cycle, settings — not just panel switches. `Subtypes.pushLayoutRecency` is `suspend` (called from that observer coroutine; reads via `getSetting`, writes via the `suspend` `setSetting` — no `runBlocking` nesting), prepends + dedups + caps at 16. **No recursion:** it writes `LayoutRecency`, not `ActiveSubtype`, and `currentSubtype` is already updated, so the re-triggered collect is a no-op. Before any recency accumulates, both helpers fall back to enabled order, so it works from first launch.

**Command cells + the header row (phase 3).** A cell's target is now either a subtype string (switch, as above) **or a command**, dispatched in the panel's `onUpEvent` → `activate(target)`: `!nav/<route>` builds an `Intent` to `SettingsActivity` with `putExtra("navDest", route)` (+ `NEW_TASK|SINGLE_TOP|CLEAR_TOP`) and `startActivity` — exactly the `CustomLayoutsAction` pattern; `!ime` calls `((InputMethodManager) getSystemService(INPUT_METHOD_SERVICE)).showInputMethodPicker()`. Routes are the registered Compose-nav routes in `SettingsNavigator.kt` (`kxkbSizing`, `languages`, `addLanguage`, `themes`, `specialKeys`, `resize`, `home`, and **`devlayouteditor`** = the Custom-layouts editor — *not* `devlayouts`). `setColumns` keeps the same `(target, label)` Pair shape (commands are just special targets) but gained a parallel `List<Pair> headers` (one per column, nullable) + an `isHeader` flag on `Cell`. The three headers: left `!nav/kxkbSizing` "Keyboard UI", middle `!ime` "Keyboards…", right `!nav/devlayouteditor` "Custom Layouts" (renamed from "Layouts…" in `b51800df5`).

**Configurable left-column shortcuts (phase 3).** The left column is no longer hardcoded — it's built from a central catalog in `Subtypes.kt`: top-level `data class SwitcherShortcut(id, label, target, defaultOn)` + `val LayoutSwitcherShortcutCatalog` (add_layout/custom_layouts/themes/special_keys/resize/languages/settings; add_layout+themes+settings default-on) + `fun layoutSwitcherShortcutPrefKey(id) = "kxkb_switcher_shortcut_$id"`. `Subtypes.getLayoutSwitcherShortcuts(context)` filters the catalog by a **SharedPreferences boolean per id** and maps to `(target, label)`; `MainKeyboardView.showLayoutSwitcher` calls it for the left column. The toggle UI is a section ("Layout switcher shortcuts") at the bottom of `KxkbSizingScreen` ("Keyboard UI"), one `SettingToggleSharedPrefs(title, key=layoutSwitcherShortcutPrefKey(id), default=defaultOn)` per catalog entry. **Store choice is deliberate:** `SettingToggleSharedPrefs`→`useSharedPrefsBool` and the panel's `PreferenceUtils.getDefaultSharedPreferences(context).getBoolean(...)` hit the *same* default SharedPreferences (the proven key-sliding round-trip), so UI and panel agree without a DataStore hop — don't "fix" this to a `SettingsKey`/DataStore toggle or the two stores diverge. Changes apply next time the panel opens (no rebuild). Labels are English literals (consistent with the hardcoded header labels); localise later if wanted.

**Key files:** `…/keyboard/LayoutSwitcherView.java` (the panel — multi-column via `setColumns(columns, headers, …)`, the `Cell` model + `isHeader`, fit-to-width layout with the shared header row, and `activate()` command dispatch — imports `Intent`, `InputMethodManager`, `SettingsActivity`), `…/keyboard/PointerTracker.java` (`openLayoutSwitcherPanel` + the swipe hook + the LAYOUT_MENU long-press branch), `…/keyboard/MainKeyboardView.java` (`showLayoutSwitcher` — builds the 3 columns + 3 headers, left column from `getLayoutSwitcherShortcuts`, pulls theme colours, anchors at the last touch), `…/keyboard/internal/DrawingProxy.java` (the `showLayoutSwitcher` method — only `MainKeyboardView` implements `DrawingProxy`, so no other impl to update), `…/latin/Subtypes.kt` (`LayoutRecency` key + `getLayoutRecency`/`pushLayoutRecency`; the switcher helpers `getCurrentLanguageLayouts`/`getOtherLanguageEntries`/`getActiveSubtypeString`/`switchToSubtypeString` + `langOf`/`layoutOf`; the `SwitcherShortcut` catalog + `layoutSwitcherShortcutPrefKey` + `getLayoutSwitcherShortcuts`; imports `PreferenceUtils`), `…/latin/LatinIME.kt` (the recency hook in the `ActiveSubtype` observer), `…/latin/settings/Settings.java` (the mode constant), `…/latin/uix/settings/pages/Typing.kt` (the spacebar-mode picker option + searchTag, **and** the "Layout switcher shortcuts" toggle section in `KxkbSizingScreen` — imports `SettingToggleSharedPrefs` + the catalog symbols), `java/res/values/strings-uix.xml` (the mode label).

## Implemented features (reference, beyond commits 1–2)

The `custom` stack has grown well past the two base commits documented above; the feature commits append on top and survive rebases. The Ctrl modifier is the most intricate and is recorded here in full because it spans the input pipeline and the render path:

- **Ctrl modifier — one-shot, functional + visual** (commits `179ce28` functional, `1eeed9e` highlight). A one-shot latch, not a held modifier. `CODE_CTRL = -26` in `Constants.java`; `"key_ctrl"` appended (index 26, parallel) to `ID_TO_NAME`/`DEFAULT` in `KeyboardCodesSet.java`. In `InputLogic.java`: a `private boolean mCtrlActive` field; a `setCtrlActive(boolean)` helper that sets the field **and** calls `KeyboardSwitcher.getInstance().setCtrlActive(active)` so the view can highlight; a `case Constants.CODE_CTRL` in `handleFunctionalEvent` that toggles via `setCtrlActive(!mCtrlActive)` (arm / cancel-if-armed); and an interception at the very top of `onCodeInput` — before `mWordComposer.processEvent` / `beginBatchEdit` — that, when armed and the event is a printable codepoint (`>= CODE_SPACE`, so `\n`/`\t` fall through and just cancel), returns `handleCtrlComboEvent(...)`. That helper disarms, flushes any composing word, maps the codepoint to an Android `KEYCODE_*` via `androidKeyCodeForCodePoint` (a–z, 0–9, and the terminal symbols `[ ] \ / - = ; ' , . ``), and calls `sendDownUpKeyEvent(keyCode, META_CTRL_ON | META_CTRL_LEFT_ON)`. A second Ctrl press or any functional key cancels the latch. The bundled `kxkb.yaml` Ctrl key uses `spec: "Ctrl|!code/key_ctrl"`.
  - **Visual highlight** (no full keyboard reload): `KeyboardSwitcher.setCtrlActive(boolean)` forwards to `MainKeyboardView`; `KeyboardView` holds a `mCtrlActive` flag + a `setCtrlActive` that `invalidateKey`s every key whose `getCode() == CODE_CTRL`. In `KeyboardView.drawKey`, when `mCtrlActive` and the key is `CODE_CTRL`, the `KeyDrawingConfiguration` is rebuilt with the `KeyVisualStyle.StickyOn` descriptor's `backgroundDrawable` + `foregroundColor` (the same bright style shift-lock uses) — i.e. one key is restyled at draw time rather than switching the whole keyboard the way shift state does. A `val visualStyle` is immutable per `Key`, which is why the swap happens at draw time, not by mutating the key.

- **Space accepts the first next-word prediction** (commit `b39c25383`, built `0.1.29+15`, versionCode `116900015`). The next-word analogue of autocorrect-on-space: while composing, space already commits the top correction; this extends the same gesture to the **idle / next-word** state — when nothing is being composed and the suggestion bar's first candidate is a real next-word prediction, pressing space commits that prediction instead of typing a space, so you can chain through predicted words with repeated space presses (Multiling-style). E.g. Czech `dob`+space→`Dobrý`, then space→`den`, then space→the next prediction. **Where** (`GeneralIME.kt`): a helper `nextWordPickForSpace(event)` returns the prediction to pick (or null to let space behave normally), and the `onEventInternal` keypress branch routes a hit through `inputLogic.onPickSuggestionManually(...)` — the **same call the tap path uses** (the `EVENT_TYPE_SUGGESTION_PICKED` branch), so it behaves exactly like tapping that candidate: commit + `insertOrSetPhantomSpace` trailing space + `postUpdateSuggestionStrip` re-predict + shift update. It fires only when (a) the new `SpaceCommitsNextWordPrediction` setting is on (default **true**), (b) `event.mCodePoint == CODE_SPACE` and `!mWordComposer.isComposingWord`, and (c) `inputLogic.mSuggestedWords.getInfo(0)` is a genuine prediction and **NOT** a static topBar default — discriminated by reference identity against `topBarInfos` (the same `it === picked` test `handleTopBarPick` uses), since `showPredictionsWithTopBar` builds `mSuggestedWords` as `preds + topBarInfos`, so index 0 is a prediction iff `preds` is non-empty. So an idle bar showing only the pinned topBar defaults still types a plain space, and composing (autocorrect-on-space) is untouched (the `!isComposingWord` guard). To **ignore** a prediction, type the next word's first letter (starts composing, bypassing the space-accept). **Trade-off while on:** double-space→`. ` doesn't trigger when a prediction is showing (the second space accepts the prediction). The setting key lives next to `UseExpandableSuggestionsForGeneralIME` in `GeneralIME.kt`; the Settings → Predictive Text toggle is in `Typing.kt` (strings in `strings-uix.xml`). 3 files (`GeneralIME.kt`, `Typing.kt`, `strings-uix.xml`), no native change.

- **Per-app layout memory — restore each app's last-used layout on focus** (commit `c7e6b9aa6`, built `0.1.29+14`, versionCode `116900014`). Remembers the (language·layout) subtype last used in each app and switches to it automatically when that app's text field is focused; **last-used-wins** — any layout switch made while an app is focused (re)binds that app — and **on by default** (opt out under Settings → Keyboard & Typing → Keyboard → "Remember layout per app", with a "Forget remembered layouts" action). 白い熊's use case: GNU layout in Termux, an English layout in the Emacs Android port, each restored on focus. **Data** (`Subtypes.kt`): `PerAppLayoutEnabled` (`booleanPreferencesKey kxkb_per_app_layout_enabled`, default **true**) + `PerAppLayoutMap` (`kxkb_per_app_layout_map` — `package⇥subtype` bindings, one per line, tab-separated/newline-joined, same no-newline-in-subtype guarantee `LayoutRecency` relies on), with `getAppLayout` (blocking cache read), `recordAppLayout` (suspend; no-op when unchanged), `clearAppLayouts`. **Behaviour** (`LatinIME.kt`): *restore* in `onStartInput` — `maybeApplyPerAppLayout(EditorInfo)` captures `attribute.packageName` into `@Volatile currentInputPackage` (null for our own package, so the settings UI / layout editor never bind a layout), and if the feature is on and the app has a binding still in `SubtypesSetting` differing from the active subtype, writes `ActiveSubtype` so the existing `onNewSubtype` observer performs the live switch (same path as a manual switch). *Record* in that observer — on every `ActiveSubtype` change with an app focused, `recordAppLayout(currentInputPackage, nowActive)`. *Clear* `currentInputPackage` in `onFinishInput`. Every switch path (swipe-space switcher, "Next layout" action, system picker, editor Apply) funnels through `ActiveSubtype`, so both hooks cover them uniformly. **GOTCHA — the cold-start hang (cost a broken build):** the restore must write `ActiveSubtype` **off the main thread** — `lifecycleScope.launch { setSetting(ActiveSubtype.key, remembered) }`, NOT the blocking `Subtypes.switchToSubtypeString` / `setSettingBlocking` (which does `runBlocking`). While the feature was off-by-default the blocking write only ran after init and was harmless; the moment it became default-on it fired on the very first `onStartInput` at cold start and stalled the IME's main thread, so the keyboard **never drew in any app** (process alive, no crash, no ANR in the buffer — the tell of a hung UI thread). Diagnosed by `adb logcat` (no FATAL) + `adb shell ps`/`dumpsys` (process up, set as default IME). The reads stay inline (cheap in-memory `DataStoreHelper` cache lookups; only the write must not block). **Settings** (`Typing.kt` `KeyboardSettingsMenu`, strings in `strings-uix.xml`): the opt-out toggle + the forget action. 4 files (`Subtypes.kt`, `LatinIME.kt`, `Typing.kt`, `strings-uix.xml`), no native change.

- **Layout editor — configurable font for layout-name labels** (commit `e5fbc77ad`). New Settings → Keyboard UI → "Keyboard editor font" (`EditorLabelFont.kt`; route `keyboardeditorfont`; a `NavigationItem` under "Keyboard editor" in `KxkbSizingScreen`) controlling the font of the layout-name LABELS throughout the visual editor: family (Monospace / Default / Sans-serif / Serif), size (8–28 sp), weight (Light…Extra-bold), and an optional colour override (a `SettingToggleDataStoreItem` + the editor's `ColorSetting` RGBA picker; off = inherit the theme content colour), with a live preview + "Reset to defaults". Five `SettingsKey`s (`kxkb_editor_label_font_family`/`_size`/`_weight`/`_color_on`/`_color`) feed one `@Composable editorLabelStyle()` that returns `MaterialTheme.typography.bodySmall.copy(...)` with the four knobs overlaid (colour `Color.Unspecified` unless the override is on, so the label inherits its container's content colour exactly as the old hardcoded `mono` did). Repointed every editor `mono` LABEL site to `editorLabelStyle()`: `KeyboardEditorScreen` (the layout list, Rows, alt-page list, append-from-another-layout list), `KeyEditScreen` (its file-level `private val mono` removed; all 6 sites call `editorLabelStyle()` inline), `KeyboardEditorExportScreen`, `AltPagePreviewScreen`. Defaults reproduce the old look exactly (Monospace, 12 sp, normal, theme colour) — nothing changes until a knob moves. Where a `Text` passes an explicit `color =` (the muted "(none)" / "(building preview…)" hints), that wins over the style colour, so the override colours the real labels while the hints stay muted. 8 files (1 new `EditorLabelFont.kt`, + `SettingsNavigator.kt`, `Typing.kt`, the 4 editor screens), no native change.

- **Layout editor — preselect the active layout on entry** (commit `8a2adf4b7`). On a FRESH entry into the editor it loads the custom layout currently active in the keyboard (the one entered from), not whatever was last edited. `SubtypeLocaleUtils.getKeyboardLayoutSetName(Subtypes.getActiveSubtype(context))` gives the active subtype's `KeyboardLayoutSet` name; a `custom<N>` value → the custom-layout index. Gated by `var didPreselect by rememberSaveable { mutableStateOf(false) }` + `LaunchedEffect(Unit)`: `rememberSaveable` is restored as `true` on a pop-back (key-edit / export sub-screen → back) but starts `false` on a new nav back-stack entry, so the preselect fires only on a genuine fresh entry and never clobbers in-progress edits. Only switches when the active layout is a custom layout that exists in the list and isn't already `sourceIdx`. 1 file (`KeyboardEditorScreen.kt`), no native change.

- **Layout editor — Close button at the bottom of the screen** (commit `e048304c8`). A third "Close Keyboard editor" `OutlinedButton` (→ `context.findActivity()?.finish()`) at the very bottom of the main editor screen, under the candidates editor, mirroring the top + above-preview ones so the editor closes without scrolling back up. 1 file (`KeyboardEditorScreen.kt`), no native change.

- **Layout editor — per-layout "Suggestion bar candidates" editor** (commit `af18f1610`). A section on the editor's main screen to edit a layout's own `topBar` list (the per-layout override of the global Settings → Typing → "Suggestion bar candidates"). The `Keyboard.topBar` field + `emitKeyboardYaml` already round-tripped it, but there was no UI — a per-layout list could only be set by hand-authoring the YAML. New `KeyboardEditorSession.setTopBar(entries)` writes the list into the working DRAFT (`working = kb.copy(topBar = entries)`, dirty), so it reaches the real/live layout only on Apply / Apply-as-new / Export like every other edit — NOT on keystroke (白い熊 flagged this; the only persistence paths are the button handlers). The box stores `it.split("\n").filter { it.isNotBlank() }`, so an all-blank box becomes `[]` (not `[""]`) and `resolveTopBarEntries` falls back to the global list. The box is **pre-filled with the effective list** — `working.topBar` if non-empty, else the global list via `useDataStoreValue(TopBarEntriesSetting)` — for in-place editing; the pre-fill is display-only (a local `remember(sourceIdx)` text state), and the model's `topBar` is written/forked from global ONLY on an actual `onValueChange`, so an untouched layout keeps tracking the global list and clearing the box reverts to tracking global. Same entry syntax as `TopBarEditor` (plain text/emoji, `A…B` caret-pairs, `[Paste]`, `{{date` stamps). 2 files (`KeyboardEditorScreen.kt`, `KeyboardEditorSession.kt`), no native change.

- **Ellipsis "…" gets period-like auto-spacing** (commit `3e13557fc`). The single ellipsis character (U+2026) left the preceding auto-space in place (`hello …`) because, although it's a `symbols_word_separators` member, it was absent from `symbols_followed_by_space` in `java/assets/spacing-and-punctuations/default.xml`. Added it there → `isUsuallyFollowedBySpace('…')` true → `trySwapSwapperAndSpace` strips the preceding space and commits `…` + a trailing space (`hello… `), exactly like `.`. The three-dot `...` already did this (each `.` is a swapper). NOT added to `symbols_sentence_terminators`, so it doesn't auto-capitalize the next word (an ellipsis is usually mid-thought). `default.xml` only — covers en + the cs/ru fallbacks; the fr/fr-rCA/hy/fa/ar overrides keep their own punctuation-spacing. 1 file (`default.xml`), no code/native change.

- **Tab key sends `KEYCODE_TAB`, not a literal tab** (commit `7dacd5161`). A Tab key (`CODE_TAB`=`'\t'`, e.g. `!code/key_tab` in the GNU/`kxkb.yaml` layout) flowed through `InputLogic.sendKeyCodePoint` into `commitText("\t")`, inserting a literal tab — so in editors that bind Tab (Emacs minibuffer completion, `indent-for-tab-command`) nothing useful happened. Now `sendKeyCodePoint` special-cases `CODE_TAB`: `mConnection.send()` to flush pending edits, then `sendDownUpKeyEvent(KeyEvent.KEYCODE_TAB, 0)`, falling back to `commitText` only if the connection can't take a key event (mirrors the digit-key path directly above). The field then receives a real Tab key event like other soft keyboards. 1 file (`InputLogic.java`), no native change.

- **Force auto-capitalization toggle (caps after . ? ! and after newlines)** (commit `19c921b8e`). Off-by-default bar-pinnable toggle "Force auto-capitalization on/off" (`ForceAutoCapsToggleAction` in `KxkbActions.kt`, icon `shift`, pref `PREF_FORCE_AUTO_CAPS` → `Settings.readForceAutoCaps` → `SettingsValues.mForceAutoCaps`; registered append-only in `Registry` → lands in *More*; reactive `activeStateProvider` highlight; rebuilds `SettingsValues` live on pref change — the Terminal-prediction toggle pattern). It exists because some fields report a text inputType WITHOUT `TYPE_TEXT_FLAG_CAP_SENTENCES` — notably the **Emacs Android port**'s buffers — so the keyboard, faithfully honouring the field, never auto-caps there (no keyboard does). When on, `InputLogic.getCurrentAutoCapsState` ORs `TYPE_TEXT_FLAG_CAP_SENTENCES` into the inputType it feeds `getCursorCapsMode` (that bit == `CAP_MODE_SENTENCES`, which the caps lookup masks against), so `.`/`?`/`!` capitalize. Still gated by the global Auto-capitalization master switch (`mAutoCap`). **Newlines:** Emacs reports its text-before-cursor *without* the trailing newline (the committed-text cache AND a fresh `getTextBeforeCursor` both come back without it — diagnosed from "caps works after a period, and after a newline only when the prior line had one"), so no text inspection can ever see the line start. The reliable, editor-independent signal is that the last thing *we* input was Enter: a new `mLastInputCodePoint` field, set at the top of `handleNonFunctionalEvent` (both Enter and ordinary characters route through it, so the next typed character overwrites it), and `getCurrentAutoCapsState` returns `CAP_MODE_SENTENCES` when force-caps is on, the cache lookup says OFF, and `mLastInputCodePoint == CODE_ENTER` — so only the new line's first letter capitalizes. (An earlier attempt added a fresh-editor-read `getFreshCursorCapsMode` to `RichInputConnection`; reverted once the hidden-newline behaviour was understood, since a fresh read also lacks the newline.) 6 files (`Settings.java`, `SettingsValues.java`, `InputLogic.java`, `KxkbActions.kt`, `Registry.kt`, `strings-uix.xml`), no native change.

- **Layout editor — swap a key up/down between rows** (commit `300891473`). The editor could move a key only left/right within its row; the key Edit screen's "Position in row" section now also has **▲ Move up / Move down ▼**, the vertical analogue of ◀ Move / Move ▶. New session op `KeyboardEditorSession.swapKeyVertical(page, row, col, delta)` **swaps** the key with the one directly above/below — same column index, clamped to the adjacent row's key count — so both rows keep their key counts (a pure exchange, no overflow; `withKeys` preserves each row's kind, so a key can swap across letters/bottom rows). Up disabled on the top row, down on the last; works per page (base + alt); pops back to the preview after the swap. **Swap, not relocate** (matching the horizontal Move, which is itself a swap-with-neighbour). 2 files (`KeyboardEditorSession.kt`, `KeyEditScreen.kt`), no native change.

- **Colon and semicolon get period-like auto-spacing** (commit `18fa9a032`). When auto-spacing was on, `:`/`;` kept an auto-space *before* them (`hello :`) with no reliable space after — unlike `.` `,` `?` `!`. They were the **entire** `symbols_optionally_preceded_by_space` set (`;:`) in `java/assets/spacing-and-punctuations/default.xml` (the only file defining it; no locale overrides), which marked them emoticon-style. That set's **only** two consumers are the two `:/;` special-cases in `InputLogic.handleSeparatorEvent`: the `// Don't swap space for : or ; ever, see #1436` guard in `tryStripSpaceAndReturnWhetherShouldSwapInstead` (~L1996) that disables the preceding-auto-space **swap**, and the conditional trailing-space branch (~L1468, `codeShouldBeFollowedBySpace = !spacePrecedesCursor`). **Fix: emptied the set** (one declarative line, no code change) → `isOptionallyPrecededBySpace` always false → `:`/`;` follow the ordinary followed-by-space path exactly like `.` `,` `?` `!`: the preceding auto-space is swapped away and a space is always added after, so `hello :` → `hello: `. **Emoticons mostly survive** because `)` is itself a swapper (`hello` → `hello ` → `:` → `hello: ` → `)` → `hello:) `); a *manually* typed space before `:` is preserved unless the most aggressive auto-space mode is on (same nuance `.` has). `12:30` unaffected (digit context suppresses auto-spacing); GNU/`zxx` unaffected (`current_language_has_spaces=false` disables the whole auto-space machinery). 1 file (`default.xml`), no code/native change.

- **App language: offer English (US) + persist the selection** (commit `3073e993e`). Two fixes to the in-app UI-locale picker (Settings → "App language"; the base feature is in the prose list above — NOT keyboard typing subtypes). (1) **English (US) was missing** — the offered list is build-generated by the `generateAppLocales` task from translation `values-*` dirs, and the base `values/` (the project's English source) isn't a `values-*` dir, so en/en-US was never emitted (only en-GB/en-AU/en-CA/en-IN/en-XC). The task now adds `en-US` explicitly (build log: 92 locales); selecting it resolves through values-en-rUS → values-en (neither exist) → base `values/`. (2) **The selection didn't persist** across a cold start on **API < 33** — AppCompat's per-app-locale auto-storage only re-reads the saved locale when an `AppCompatActivity` is created, but `SettingsActivity` is a plain `ComponentActivity`, so `AppCompatDelegate.getApplicationLocales()` was empty on a fresh process and the existing `SettingsActivity.attachBaseContext` override fell back to the system language. Fix in `AppLanguage.kt`: mirror the chosen tag into our own pref (`setAppLocale` writing `kxkb_app_locale`) and re-apply it at app startup from `CrashLoggingApplication.onCreate` (`restoreAppLocale`, guarded; a no-op on API 33+ where the framework restored it, and on "System default" = empty tag), in **both stable + playstore flavors**. 4 files (`build.gradle`, `AppLanguage.kt`, both `CrashLoggingApplication.kt`), no native change.

- **Bar-pinnable "Personal dictionary" action** (commit `7c83dfa91`). `PersonalDictionaryAction` in `KxkbActions.kt` (icon `icon_spellcheck`) opens the Personal Dictionary editor (Settings → Predictive Text → "Edit Personal Dictionary") directly via the `SettingsActivity` "navDest" extra → "pdict" route — the same launcher pattern as Live sizing / Special keys / Custom layouts. Registered in `Registry.AllActionsMap` as `"personal_dictionary"`, in no default category → auto-lands in *More* so the user pins it themselves. This is the entry point for adding custom words, incl. **Japanese kanji entered with a hiragana reading + part-of-speech (品詞)** which Mozc then offers as a conversion candidate: the in-app `JapaneseWordPopupDialog` (`pages/pdict/PersonalDictionaryJa.kt`) encodes it into Android's `UserDictionary` as word=kanji, shortcut=`"furigana\tposIndex"`, and `JapaneseIME.refreshMozcDictionaries()` re-injects all `ja` personal words into Mozc on the `ReloadPersonalDict` IME message. 3 files (`KxkbActions.kt`, `Registry.kt`, `strings-uix.xml`), no native change.

- **Multiling-style key icons** (commit `c3fc0df`). Replaced FUTO's outline icons with solid filled glyphs matching the user's Multiling O keyboard. **Icon system:** `KeyboardIconsSet` defines names (`NAME_SHIFT_KEY="shift_key"`, `NAME_DELETE_KEY`, `NAME_ENTER_KEY`, `NAME_TAB_KEY`, `NAME_SETTINGS_KEY`, …); `getIconDrawable(name)` → `provider.getIcon(name)`. `BasicThemeProvider.getIcon` resolves names via an `addIcon(name, R.drawable.X, tint)` block (~lines 391–411) that is **unconditional** (runs for every theme/provider instance). Arrow keys are *actions*: `CursorActions.kt` `ArrowUp/Down/Left/RightAction` each carry `icon = R.drawable.X`, registered into the provider via a `Direction.entries.forEach` loop (also unconditional). **The change:** added 9 vector drawables `java/res/drawable/kxkb_*.xml` — `arrow_up/down/left/right` (solid triangles), `shift` (solid up-arrow + detached bar), `tab` (arrow into a vertical bar), `enter` (hooked ↵), `delete` (left pentagon with an `evenOdd` knockout X), `settings` (gear with an `evenOdd` center hole). All `fillColor="#FFFFFF"` (theme-tinted), intrinsic `96dp` over a `24×24` viewport. Repointed the 6 `BasicThemeProvider` addIcon lines (shift + shiftshifted → `kxkb_shift`, delete, settings, enter, tab) and the 4 `CursorActions` arrow icons. The bundled `kxkb.yaml` references each by name (`$shift`, `!icon/tab_key`, `$delete`, `!icon/enter_key`, `!icon/action_up|!code/action_up`, …), so they resolve to the new drawables. Shared app-UI icons (`R.drawable.shift`/`delete`/`settings`/`chevron_*`) were left untouched — new names + repointing only, so nothing else in the app UI changed.
  - **Icon sizing** lives in `KeyboardView.onDrawKeyTopVisuals` (~line 551). Stock: `size = kdc.getTextSize() * 1.75f`, then `iconWidth = min(icon.getIntrinsicWidth(), keyWidth)` capped at `size`, `iconHeight = iconWidth` (square, centered). Because the intrinsic was a small `24dp`, icons stayed tiny regardless of key/letter size. Changed to `final float size = Math.min(keyWidth, keyHeight) * 1.0f;` — a direct fraction of the shorter key side (bumping the vector intrinsic to `96dp` so it's never the limiter), so glyphs fill the key. The `1.0f` is the live dial. **Per-icon relative tuning** is done *in the vector*, not the code: a centered `<group android:scaleX="N" android:scaleY="N" android:pivotX="12" android:pivotY="12">` wrapping the glyph paths — currently arrows `×1.2`, enter `×1.1`. (Earlier the global `size` was multiplied by `getKeyLetterScale()`; that was dropped so the number is an honest key-fraction and can't overflow — a future Font-Size knob can re-couple it deliberately, bounded.)
  - **BFU propagation is automatic** — these icons already render on the before-first-unlock keyboard with no extra work: the addIcon repointing + arrow registration are unconditional shared code, the size change is in shared `KeyboardView`, the direct-boot defaults map (`DefaultDataStore` in `Settings.kt`) pins `THEME_KEY="HighContrastYellow"` + `KeyBordersSetting`/`KeyHintsSetting` + the `1.4` height, and the bundled `kxkb.yaml` names every key. Verified by the user.

- **Per-geometry look-knob plumbing — Phase 0a of the live look/size knobs** (commit `c94a849`). Inert foundation, zero visible change. See the "Live look/size knobs" section below for the full feature plan; what landed: `SavedKeyboardSizingSettings` (in `KeyboardSizingCalculator.kt`) gained 4 per-geometry look fields, each defaulting to sentinel `-1` = "inherit the theme" — `fontSizeMultiplier`, `labelWeight`, `keyRoundness`, `borderWidthDp`. `LatinIME.withPerKindLook(scheme)` reads the current geometry's saved settings and `.copy()`s those onto `scheme.extended.advancedThemeOptions` (`keyLetterScale`/`keyLabelWeight`/`keyRoundness`/`keyStrokeWidthDp`) only when `>= 0`; applied at both `BasicThemeProvider` construction sites (`updateDrawableProvider` and the `getDrawableProvider` fallback). All-inherit by default → overlay is a no-op until a knob is set.

- **Per-geometry live look/size knobs — the rest (LIVE)** (commits `89b727d` → `195ccf075`). The full live-sizing screen built on top of Phase 0a; architecture in the "Live look/size knobs" section above. Commit chain, oldest first:
  - `89b727d` — Phase 0b: sizing fields on `SavedKeyboardSizingSettings` (additive gap dp, top/bottom row-height factors), inert/neutral defaults — still visually identical.
  - `56bfb207b` — gaps + keyboard/top/bottom row-height sliders live (includes the `liveRowSizingActive` clamp-gating fix that kills the "phantom gap" when redistributing row heights).
  - `a146bbd27` — key font-size slider (`fontSizeMultiplier` → `keyLetterScale`).
  - `71cd381f4` — key roundness slider (range 0..5, raised from 1.0 at the user's request).
  - `e2c82b7b0` — secondary (hint) text-size slider; required NEW `keyHintScale` plumbing through `AdvancedThemeOptions` → providers → the on-key hint draw sites.
  - `6c92a5433` — suggestion/action-bar height made dynamic + live via `LocalActionBarHeight` (replaced the hardcoded `ActionBarHeight = 40.dp`).
  - `7a356f61c` — per-geometry split-width slider + gap-close/overlap (split cap raised from `9/10` to full width) + the three bottom-row width fixes (regular width, functional minimum, and functional-value rescale all repointed to `unsplitLayoutWidth`).
  - `195ccf075` — snap split overlap to a single merged column (`splitShouldMerge` → render the row un-split and centred, skipping the partial-offset zone).

- **Per-geometry colours + geometry-overlay sync (LIVE)** (commit `23ae74876`). 8 per-geometry colour overrides (key font, secondary/hint font, key bg, functional-key bg, key border, keyboard bg, suggestion-bar bg, suggestion font) as nullable packed-ARGB `Int?` on `SavedKeyboardSizingSettings`; overlaid in `withPerKindLook` onto `base.outline` + the extended scheme, with 2 dedicated `ExtraColors` overrides for the suggestion bar so it's independent of the shared surface/onSurface; Compose chrome reads the overlaid scheme (`overlaidColorScheme`) so the bar recolours live. UI: a "Colours" section of expandable RGBA `ColorSetting` rows on `KxkbSizingScreen` + a per-geometry explainer header. **Also fixed the per-geometry tracking this exposed** (latent for the look knobs too): `onConfigurationChanged` now republishes the size state (rotation never reached `publishCurrentSizeState`, so the settings screen wrote to the wrong geometry), and `recreateKeyboard` re-syncs the baked-in overlay to the live geometry (`lastProviderSizeState`) so rotate/fold no longer carries the previous geometry's look/colours forward. Full architecture + the gotcha in the "Live look/size knobs" section above.

- **Cluster main-position sliders + "Keyboard UI" rename (LIVE)** (commit `9a143522c`). Two per-geometry sliders on the sizing screen — `clusterLeftOffset`/`clusterRightOffset` (`SavedKeyboardSizingSettings`, default `0.333f`) — set how far a cluster key's left/right outer mains sit from the centre, pushed via `withPerKindLook` → `KeyboardView.setClusterMainOffsets` and read in `drawClusterMains`; **display only** (prediction sub-rects unaffected). Added to `lookFieldsDiffer` so a cluster-only drag repaints live — the bug on first try was omitting exactly that (value stored, glyphs frozen). Also renamed the screen/action "Live sizing" → **"Keyboard UI"** (`action_live_resize`, `kxkb_sizing_title`). 6 files: `KeyboardSizingCalculator.kt`, `KeyboardView.java`, `LatinIME.kt`, `Typing.kt`, `SpecialKeys.kt`, `strings-uix.xml`. Architecture in the "Live look/size knobs" and Phase 4d sections above.

- **HIDE-keyboard action key + icon flick-label rendering (LIVE)** (commit `27eb58a71`). A `HideKeyboardAction` (in `KxkbActions.kt`, icon `chevron_down`) whose `simplePressImpl` calls a NEW `KeyboardManagerForAction.requestHideSelf()` (interface in `Action.kt`; sole impl `UixActionKeyboardManager` in `UixManager.kt` → `latinIME.requestHideSelf(0)`) — the old Multiling `[HIDE]`. Registered `"hide_keyboard" to HideKeyboardAction` in `Registry.kt` (appended; **not** in any action-bar category — keys only). **Reusable pattern for "an Action on a key":** register it in `AllActionsMap` (append, indices stay stable), then reference on a key/flick slot as `!code/action_<id>` — `ActionRegistry.parseAction` strips the `action_` prefix → the registry id → `CODE_ACTION_0 + index`, and pressing the key runs the action's `simplePressImpl` (same path the arrow keys use). The icon `!icon/action_<id>` auto-registers from the action's `icon` drawable (`KeyboardIconsSet` adds `action_<id>` names), so no extra icon wiring. **Also closed a latent rendering gap:** `KeyboardView.drawAtRestFlickLabels` only ever drew a flick target's *text* `previewLabel`, so an icon-only flick/compass target (like HIDE) showed nothing on the key face; it now falls back to the target's icon (`getPreviewIcon(mKeyboard.mIconsSet)`, tinted with the hint colour via `setTint`, drawn at hint size centred on the cell). **Caveat:** the hold-popup `KeyPreviewView.drawFlickKeys` is still text-only, so an icon flick target won't show in the long-press preview (untouched — the key-face was the reported issue). 6 files: `Action.kt`, `UixManager.kt`, `KxkbActions.kt`, `Registry.kt`, `KeyboardView.java`, `strings-uix.xml`.

- **Spacebar style ⇒ no key-preview + long-press enabled (LIVE)** (commit `a005dce4c`). `BaseKey.getEffectiveAttributes` now defaults a `Spacebar`-styled key to `showPopup = false` and `longPressEnabled = true`, matching `TemplateSpaceKey`. One-file fix in `BaseKey.kt`: `val isSpacebar = resolve(attrs){it.style} == KeyVisualStyle.Spacebar`, then `showPopup = resolve(configured){it.showPopup} ?: !isSpacebar` and `longPressEnabled = resolve(configured){it.longPressEnabled} ?: isSpacebar`, where `configured = attrs.dropLast(1)` drops the trailing `DefaultKeyAttributes` so an explicit per-key/row/keyboard value still wins; non-spacebar keys are byte-for-byte unchanged (`!isSpacebar`=true and `isSpacebar`=false are exactly the old `DefaultKeyAttributes` values). **GOTCHA this fixed:** the canonical spacebar is `TemplateSpaceKey` (`TemplateKeys.kt`, the `$space` template) which sets `showPopup=false`+`longPressEnabled=true`, but the kxkb layouts (in-repo `kxkb.yaml`, BFU, the test layout) write the spacebar **longhand** as `{ type: base, attributes: { width: Custom1, style: Spacebar }, spec: "!icon/space_key|!code/key_space" }`, which inherited the base defaults `showPopup=true` (→ a wide black-box key-preview popup on every space touch — `noKeyPreview` is `!showPopup`, set in `LayoutEngine` `actionsFlags`) and `longPressEnabled=false` (→ space long-press never fired, so no Languages dialog; `PointerTracker.onLongPressed` → `onCustomRequest(CUSTOM_CODE_SHOW_INPUT_METHOD_PICKER)` → `UixManager.showLanguageSwitcher()` only runs for the space code when long-press is enabled **and** the spacebar mode isn't a swipe mode). Fixing the style default repairs every longhand spacebar at once with no YAML change. Not a regression from the cluster/HIDE work — none of that touches the preview or long-press path. 1 file: `BaseKey.kt`.

- **GNU "language" — custom locale registration via an `assets-custom` mapping/names merge (LIVE)** (commit `faa330790`). Adds a **GNU** language — a no-dictionary, non-predictive "language" for programming/Emacs layouts, kept distinct from English so English stays predictive. **Mechanism (rebase-safe, no submodule fork):** the stock `layouts/mapping.yaml` + `layouts/names.yaml` live in the `java/assets/layouts` submodule; `LayoutManager.init` now also merges two **optional** files shipped from `assets-custom` on top — `parseMappingsOrEmpty("layouts/mapping-custom.yaml")` into `localeToLayoutsMappings` and `parseNamesOrEmpty("layouts/names-custom.yaml")` into `localeNames` (both wrap the parser in `try/catch` → empty if the file is absent; `+` means a custom entry wins on key clash). `assets.srcDirs` already includes `java/assets-custom`, and the custom filenames differ from the submodule's, so there's no asset collision. The two new data files register `zxx` (ISO "no linguistic content") → layout id `[kxkb]` (the bundled default, also BFU's) and `zxx: { en: "GNU" }`. **Why that's all it takes:** (1) the Add-language picker lists `localeToLayoutsMappings.keys` (`AddLanguage.SelectLanguageScreen`), so "GNU" now appears, seeded with `kxkb`; (2) `Subtypes.getLocaleDisplayName` → `LayoutManager.getExceptionalNameForLocale` is consulted first (outer key prefix-matches, so a `zxx` entry also names `zxx_US`; the inner display-locale falls back to the first entry for any UI locale), so it reads "GNU"; (3) **no dictionary ships for `zxx`, so word prediction is off there automatically** — this is the "non-predictive layout" need, solved at the language level (there is NO per-layout suggestions flag in the v2 `Keyboard` schema). **Adding GNU layouts needs no rebuild:** the Custom-Layout editor's `updateCustomLayoutsAndSyncSubtypes` (`DevLayoutEditor.kt`) writes `Subtypes.makeSubtype(language, "customN")` straight into `SubtypesSetting`, so typing **`zxx`** in its Language field + pasting a converted YAML creates a real, switchable GNU subtype on the spot. Use plain `zxx` (the override name is verbatim "GNU"); `zxx_US` would only read "GNU (US)" if the override literally said so. 3 files: `LayoutManager.kt`, `java/assets-custom/layouts/mapping-custom.yaml`, `java/assets-custom/layouts/names-custom.yaml`. **Companion:** the `multiling-futo-conversion` skill produces the GNU layout YAMLs; to bundle one under GNU instead of Dev-importing it, drop the `.yaml` into `assets-custom/layouts/` and add its id to the `zxx` list in `mapping-custom.yaml`.

- **Per-(language·layout·geometry) Keyboard UI settings (LIVE)** (commit `7b399be0b`). The Keyboard UI store was keyed by geometry alone; now it's keyed per-(active subtype × geometry) so every language·layout·geometry combination has its own sizing/look/colours. Full architecture is in *Live look/size knobs* above; in brief: a shared `perComboSizingKey(context, kind)` (in `KeyboardSizingCalculator.kt`) folds the active subtype into the key; `getSavedSettings`/`editSavedSettings` read per-combo→geometry-blob→default and **write per-combo**, so unedited combos inherit the existing per-geometry tuning and **fork on first edit** (no migration/reset). The Keyboard UI screen (`Typing.kt`) rebinds to that key reactively off `ActiveSubtype` and shows a "language · layout" scope line. **The subtle bit (and the regression that bit during this work):** the live `dataStore.data` watcher in `LatinIME` that drives slider/colour updates must follow the **same dynamic per-combo key** — v1 left it on the fixed geometry keys, so every live edit was a no-op (the store was correct, so only a geometry rebuild revealed the change). The watcher now reacts to same-key/new-blob (edit) and key-changed (combo switch), which also makes switches robust to the `DataStoreHelper` cache lag. 3 files: `KeyboardSizingCalculator.kt`, `LatinIME.kt`, `Typing.kt`.

- **Keyboard UI page grouped Jami-style with cascading indentation (LIVE)** (commit `4bf0c99af`). The flat slider/colour run became a 5-section hierarchy via two private composables in `Typing.kt`: `KxkbSection` (big accent header + full-width rule) and `KxkbSubgroup(title) { content }` (accent header + short underline, controls indented one step deeper). Cascade: section 16dp < subgroup 24dp < controls 32dp (+ the control's own padding). Sections Size / Keys / Sliding labels / Surfaces & suggestion bar / Layout switcher; each colour now sits beside the element it affects (the old single "Colours" section is gone). Pure presentation — every control keeps its binding/range/inherit logic. See *Live look/size knobs → The screen* for the structure and how to add a knob. 1 file: `Typing.kt`.

- **Long-press "study popup" for complex keys** (commit `037794c63`). Holding a compass / cluster / macro / chord key pops an enlarged (~3×) preview of its full content above the key, to study the layout while held; a normal tap is unaffected. 6 files. **Trigger:** `Key.isStudyable` (Kotlin `val`, Java `isStudyable()`) = `flickKeys` non-empty (compass) OR `clusterMains` non-empty (cluster) OR `outputText != null` (macro/chord). **The gotcha that bit first:** these keys are `OnlyExplicit` with no more-keys, so `Key.isLongPressEnabled` is false and `PointerTracker.startLongPressTimer` returned early → `onLongPressed` never fired (no popup, and release still typed). Fix: `startLongPressTimer` now also starts the timer when `key.isStudyable()`. In `onLongPressed`, a studyable key is consumed (never opens a more-keys panel) and pops the study popup only when `mFlickDirection == null` (a stationary hold; mid-slide is left alone). **Show path:** `MainKeyboardView.showStudyKeyPreview` / `dismissStudyKeyPreview` (two new `DrawingProxy` methods — MainKeyboardView is the only implementor) force the preview even for no-preview keys, via a new `studyMode` overload of `KeyPreviewChoreographer.placeAndShowKeyPreview`. `placeKeyPreview(studyMode=true)` sizes a square box `min(baseWidthCap×3, keyboardWidth×0.86)` (≥120dp), keeps the normal preview's bottom edge and grows upward, and clamps X into the keyboard width and Y off the top. **Drawing** (`KeyPreviewView`, gated on `mStudyMode`): in study mode `drawStudyExtras` runs FIRST (cluster band — glyphs evenly across the width; or macro/chord — `getLabel()` title + cleaned `getOutputText()` below, stripping the chord's `\u0000`/multitap `\u0001` sentinels), then `drawFlickKeys` (compass, just scaled up), so a cluster key that also carries flick side-mains shows its band rather than the flick layout. **Release semantics** (`onUpEventInternal`): when `mStudyPopupShown`, dismiss the popup and — if `mFlickDirection == null` (no direction was ever crossed; `flickDirection(0,0)` is null at rest) — `return` BEFORE the flick branch, so a stationary hold types nothing; a slide leaves `mFlickDirection` set and falls through to the flick branch, which types the slid target. `mStudyPopupShown` is reset on down (`onDownEventInternal`) and on cancel (`onCancelEventInternal`). **Known cosmetic nit (left as-is):** the study popup reuses the same pooled `KeyPreviewView` as the small press-preview, so the 1×→3× relayout costs a one-frame blink as the small preview vanishes before the big one paints; showing it `withAnimation=true` (scale-up) was tried on-device and made no difference, so it ships with the instant show. A clean fix would be a dedicated always-big overlay view (deferred — not worth it for one frame). 6 files: `Key.kt`, `DrawingProxy.java`, `KeyPreviewView.java`, `KeyPreviewChoreographer.java`, `MainKeyboardView.java`, `PointerTracker.java`.

- **"App language" setting — per-app UI locale** (commit `70a41185a`). New **App language** entry at the TOP of Settings (`Home.kt`, `HomeScreenLite.settings`, first item, `navigateTo = "appLanguage"`, globe icon) → `AppLanguageScreen` (`pages/AppLanguage.kt`, route registered in `SettingsNavigator`). Lists **System default** + every language the app is translated into, each shown in its own name (autonym via `Locale.getDisplayName(locale)`), with an English subtitle and a check on the active one. Selecting pins the app's own UI locale via `AppCompatDelegate.setApplicationLocales(LocaleListCompat.forLanguageTags(tag))`; "System default" passes `getEmptyLocaleList()` to follow the phone again. **This is the app/UI locale only — keyboard *typing* languages stay under "Languages & Models" (`language_settings_title`), which is untouched.** Works because `androidx.appcompat:appcompat:1.7.0` is already a dep. On API 33+ the framework persists the choice and recreates the activity into the chosen language automatically; on API < 33 a `SettingsActivity.attachBaseContext` override wraps the base context with the stored locale (`AppCompatDelegate.getApplicationLocales()` → `Configuration.setLocales`) and the picker calls `Activity.recreate()` itself (the framework won't auto-recreate a plain `ComponentActivity`). **The offered list is build-generated, not hardcoded:** a new `generateAppLocales` gradle task (wired via `preBuild.dependsOn`) scans the translation submodule's `values-<locale>` dirs (`translations/core`, `translations/core-ign`) + `java/res`, converts Android qualifiers to BCP-47 (`-rXX`→`-XX`, `b+sr+Latn`→`sr-Latn`), and writes `res/values/kxkb_app_locales.xml` (`<string-array name="kxkb_app_locales">`) into `$buildDir/generated/appLocales/res` (added to the main `res.srcDirs`). The task logs `generateAppLocales: NN app UI locales: …` — handy to confirm what shipped. The picker reads `R.array.kxkb_app_locales` and falls back to `resources.assets.locales` (the resource table) if that array is empty. NOTE the translations submodule must be checked out (`git submodule update --init --recursive`, already in the build pipeline) for the task to find anything. Deferred: a `localeConfig` + `AppLocalesMetadataHolderService` would add the system per-app-language menu entry and < 33 cross-restart persistence — not added (device is 33+). 6 files: `build.gradle`, `strings-uix.xml`, `SettingsActivity.kt`, `SettingsNavigator.kt`, `Home.kt`, and new `pages/AppLanguage.kt`.

- **"Next layout (same language)" action** (commit `5b14e9781`). A new action that cycles forward through the *enabled* layouts of the **current language only** (in enabled order, wrapping), with no language change — no-op if that language has one layout. Backed by `Subtypes.switchToNextLayoutInLanguage(context)` (filters `SubtypesSetting` by `langOf(active)`, steps `(idx+1) % size`, writes `ActiveSubtype` via `switchToSubtypeString`). Defined as `NextLanguageLayoutAction` in `KxkbActions.kt` (`simplePressImpl` → that fn; `icon = R.drawable.keyboard`), registered in `Registry.kt` as `"next_language_layout"` **appended last** (action indices must stay stable). Referenceable in a layout as `!icon/action_next_language_layout|!code/action_next_language_layout`, and documented as a new Special Keys section. **Key pattern worth remembering** (the cog use-case): to give a key a *tap action while keeping its long-press popup*, write a base key with the action as its code and the popup as explicit `moreKeys`, pinned with `attributes: { moreKeyMode: OnlyExplicit }` so no auto moreKeys are added — e.g. the bottom-row cog became `{ type: base, attributes: { moreKeyMode: OnlyExplicit }, spec: "!icon/settings_key|!code/action_next_language_layout", moreKeys: [...] }` (the `key_settings`/`CODE_SETTINGS` it replaced is obsolete/no-op — settings now live in the action system, e.g. `!code/action_settings`). The cog YAMLs are the user's imported custom layouts, not in-repo. 5 files: `Subtypes.kt`, `KxkbActions.kt`, `Registry.kt`, `SpecialKeys.kt`, `strings-uix.xml`.

- **"Keyboard width" slider — narrow + centre the whole keyboard** (commit `d5c5edfa4`). A new per-(language·layout·geometry) size knob in a **Width** subgroup of the Keyboard UI "Size" section (`Typing.kt`, after Height, before Spacing; `SettingSliderForDataStoreItem` bound `floatItem({ it.widthFraction }, { s, v -> s.copy(widthFraction = v) })`, `default = 1f`, `range = 0.5f..1.0f`, indicator `"%.0f%%"`). Backed by a new `val widthFraction: Float = 1.0f` on `SavedKeyboardSizingSettings` — old blobs deserialize to the default (`SizingSettingsJson` is `ignoreUnknownKeys`, and Kotlin fills the missing property from its default), so it's forward/backward-safe. **Applies to `KeyboardMode.Regular` only** (Split has `splitWidthFraction`; one-handed its own rect). **It shrinks the keyboard SURFACE itself, not just the keys** (the user explicitly wanted the background to narrow so the app shows at the edges): in `KeyboardSizingCalculator.calculate()` the Regular branch sets `width = (fullWidth * frac)` (frac coerced 0.3..1.0) and `RegularKeyboardSize` now carries `val sideOffset: Int = 0` = `(fullWidth - narrowedWidth) / 2` (also added to its `equals`). `UixManager.NonFloatingKeyboardWindow` reads `(size as? RegularKeyboardSize)?.sideOffset` and passes `Offset(xOffset, 0f)` to `OffsetPositioner` (which `absoluteOffset`s the surface), centring the narrowed surface. Because `KeyboardSurface` is `requiredWidth(size.width)` with its own `pointerInput` block, the exposed side strips fall *outside* the surface Box → the app shows through and stays touchable at both edges. `widthFraction = 1.0` → `sideOffset = 0`, byte-identical to stock (no regression). Recomputes live via the existing size watcher (`invalidateKeyboard(refreshSettings = true)` on blob change → `calculate()`). **NOTE the surface width is `size.width`**: anything that should narrow with the keyboard rides along automatically (action bar, suggestion strip, background image scaling all key off `computedSize.width`); modes other than Regular are untouched. 4 files: `KeyboardSizingCalculator.kt`, `UixManager.kt`, `Typing.kt`, `strings-uix.xml`.

- **Scoped backup system — full + three slim sets** (commits `bc80aed62` then `bd7f75633`, which generalised it). `Misc.kt` (Settings → Miscellaneous) has the original full "Settings Backup" plus three partial sections: **Keyboard Settings Backup**, **Learned Data Backup**, **Models & Dictionaries Backup**. Driven by a top-level `enum class BackupScope { Full, Settings, Learned, Models }` in `SettingsExporter.kt`. The three partial scopes partition exactly what Full covers, no overlap: **Settings** = DataStore (every `SettingsKey` — theme, toggles, suggestion options, layout/language setup, App language, AND every per-(language·layout·geometry) Keyboard UI blob) + entire SharedPreferences + custom theme files; **Learned** = personal dictionary + clipboard (file + dir) + UserHistory dicts + mozc + rime typing data; **Models** = `ext/` (voice `.bin` models + `.dict` dictionaries — both land in `getExternalFilesDir`) + `transformers/` (`filesDir/transformer-models`, `.gguf`). **Export**: `exportSettings(context, out, scope = BackupScope.Full)` computes `includeConfig/includeLearned/includeModels` and gates each collection section; for a partial scope it writes a marker entry via `scopeMarkerName(scope)` (`FUTOKeyboardSettings_SettingsOnly` / `_LearnedOnly` / `_ModelsOnly`) right after the version entry. (The old `includeHeavyResources` flag was removed — only the full export ever used it, always true.) **Import auto-detects scope**: `getCfgFileMetadata` scans the WHOLE zip (it used to early-return on the version entry) and sets `CfgFileMetadata.scope`; `ImportResourceActivity` runs `loadSettings(destructive = item.v.scope == BackupScope.Full)`, so a partial backup restores only its own category and NEVER deletes the others — safe even via the main import button. `SettingsImportScreen` shows a scope-specific note (`resource_importer_cfg_backup_{settings,learned,models}_only`) instead of the destructive warning; only Full stays destructive. **Wiring**: routes `exportingkbcfg`/`exportinglearnedcfg`/`exportingmodelscfg` → `ExportingMenu(nav, scope)`; `triggerExportSettings(context, scope)` sets `SettingsActivity.exportScope` (read by `onActivityResult` → `exportSettings(..., exportScope)`, reuses `EXPORT_SETTINGS_REQUEST`); filenames tagged `_KB`/`_Learned`/`_Models`. All three import buttons reuse `triggerImportSettings` (any backup auto-detects its own scope). **Notes**: clipboard is grouped under Learned (accumulated user data); themes live in Settings (carry custom colours); import REPLACES (not merges) the categories it contains, so e.g. a Settings import replaces the whole settings store — only file deletion of other categories is skipped. `BackupScope` is same-package for `SettingsExporter`/`ImportResourceActivity` (no import) but needs importing in `SettingsActivity`/`SettingsNavigator`. The `item` smart-cast in `ImportResourceActivity` holds because `InnerScreen()` binds it via `itemBeingImported.value.let { item -> when(item){…} }` (a local val). 6 files: `SettingsExporter.kt`, `ImportResourceActivity.kt`, `SettingsActivity.kt`, `SettingsNavigator.kt`, `Misc.kt`, `strings-uix.xml`.

- **Arrow keys repeat on long-press** (commit `18b9a7caf`). Holding an arrow key now repeats the cursor move like a held physical key; a tap still moves once. The repeat machinery already existed and is shared with Delete: a key with `isRepeatable()` starts a repeat timer on press (`PointerTracker.startRepeatKey` → `startKeyRepeatTimer`, cadence = `mKeyLongpressTimeout*4/3` then `*5/30`), and each `onKeyRepeat` tick calls `callListenerOnCodeInput(..., isKeyRepeat=true)` → `InputLogic` re-runs `triggerAction(actionId)` (another DPAD event). Tap-fires-once is preserved by the release path (`onUpEventInternal`: if the key was repeating — `code == currentRepeatingKeyCode` — it returns without re-sending). The arrows just weren't repeatable. Fix: added `val repeatable: Boolean = false` to the `Action` data class (`Action.kt`), set `repeatable = true` on the four `Arrow{Up,Down,Left,Right}Action` (`CursorActions.kt`), and `BaseKey.computeData` now sets `repeatable = (attributes.repeatableEnabled ?: false) || isRepeatableActionCode(code)`, where the new top-level helper `isRepeatableActionCode(code)` returns true when `code` is in `Constants.CODE_ACTION_0..CODE_ACTION_MAX` and `AllActions.getOrNull(code - CODE_ACTION_0)?.repeatable == true`. So any key whose resolved code is a repeatable action auto-repeats — plain `!code/action_left|up|right|down` work everywhere with NO layout edits (and no per-key `repeatableEnabled`). Indexing is consistent: `!code/action_up` → `KeyboardCodesSet` → `CODE_ACTION_0 + ActionRegistry.parseAction("action_up")` = `CODE_ACTION_0 + AllActions.indexOf(ArrowUpAction)`, the same index the helper reads back and that `triggerAction` uses. **Build gotcha (hit once)**: `isRepeatableActionCode` is a top-level fun and must be placed ABOVE `BaseKey`'s `/** */` doc + `@Serializable @SerialName("base")` annotations — inserting it *between* the annotations and `data class BaseKey(` makes the annotations bind to the function ("annotation not applicable to target 'top level function'"). 3 files: `Action.kt`, `CursorActions.kt`, `BaseKey.kt`.
  - **Backspace always repeats too (commit `bfe244fbb`).** Same machinery: the `$delete` template sets `repeatableEnabled = true`, but **longhand** delete keys (`!code/key_delete` without the attribute) didn't repeat — they deleted once. `BaseKey.computeData`'s `repeatable` now also ORs `code == Constants.CODE_DELETE`, so every delete key repeats when held regardless of the layout attribute. 1 file: `BaseKey.kt`.

- **Caps-lock via Shift tap-cycle + blue Shift-glyph indicator (LIVE)** (feature commit `f99c2f2f1`, preceded by the trivial `.gitignore` newline fix `030bb4bb8`). Multiling-style caps-lock without a long-press or a double-tap *timer*. **Tap-cycle:** the `CODE_SHIFT` press in `KeyboardState.onPressKey` now calls the new `cycleShift()` instead of `toggleShift(manually = true)` + `onShiftTapForShiftLockTimer()`. `cycleShift()` reads `currentLayout.page` and steps `ManuallyShifted → lockShift()` (caps-lock), `ShiftLocked`/`Shifted → toggleShift(to = false)` (off), else `→ toggleShift(to = true, manually = true)` (one-shot shift); non-alphabet layouts just `toggleShift(manually = true)`. So a single tap shifts, a second reaches caps-lock, a third clears — and because only a *manual* shift advances to caps-lock, an auto-capitalised `Shifted` start still cancels on the first tap (expected). The old `onShiftTapForShiftLockTimer`/`shiftTime` double-tap path is left in place but no longer driven from the press. **Visual indicator (render-path, no extra invalidation):** caps-lock toggling reloads the keyboard into the `ELEMENT_ALPHABET_SHIFT_LOCKED` / `…_SHIFT_LOCK_SHIFTED` element, so `KeyboardView.onDrawKey` detects it at draw time — for a `CODE_SHIFT` key in either element it rebuilds the `KeyDrawingConfiguration` with `textColor = sCapsLockColor`, leaving the *background* untouched. The shift **glyph** turns blue because the icon is tinted with `kdc.getTextColor()` (and labels are painted with it) — recolouring the shape, not a background highlight (the user explicitly rejected the background-tint version). `sCapsLockColor` defaults to pure blue `0xFF0000FF` and is set via the static `KeyboardView.setCapsLockColor(int)`. **Per-(language·layout·geometry) config:** `SavedKeyboardSizingSettings.capsLockColor: Int? = null` (in `KeyboardSizingCalculator.kt`); `LatinIME.withPerKindLook` pushes `KeyboardView.setCapsLockColor(saved.capsLockColor ?: 0xFF0000FF.toInt())` (right after `setClusterMainOffsets`), and `lookFieldsDiffer` ORs in `a.capsLockColor != b.capsLockColor` so a change re-looks live. UI: a "Caps lock" `KxkbSubgroup` on the Keyboard UI screen (`Typing.kt`) with a single `ColorSetting` ("Caps-lock Shift colour", string `kxkb_color_caps_lock`) whose `inherited` is seeded with the blue default (so an unset value shows blue, not a theme colour). No native change. 6 files: `KeyboardState.kt`, `KeyboardView.java`, `KeyboardSizingCalculator.kt`, `LatinIME.kt`, `Typing.kt`, `strings-uix.xml`.

- **YAML emitter for the layout model** (commit `683f47083`). `emitKeyboardYaml(kb: Keyboard): String` in `v2keyboard/KeyboardYamlEmitter.kt` — a **hand-written** YAML emitter (NOT kaml encode). KEY CONSTRAINT discovered while planning the editor: the codebase only *parses* yaml (`parseKeyboardYamlString` → kaml `decodeFromString` in `LayoutManager.kt`); there is **no encoder**, and the custom serializers throw on `serialize()` — `CompassSlotSerializer` + `TapsSerializer` (`CompassKey.kt`) literally `throw SerializationException(...)`, and the `SpacedListSerializer`s `TODO()`. So a model→YAML path must be bespoke. Strategy (gives a faithful round-trip — `parseKeyboardYamlString(emitKeyboardYaml(kb)) == kb`, verified on every kxkb layout): build a tiny `Y` node tree (`YScalar`/`YSeq`/`YMap`) and render block-style; **emit every key in the explicit `{ type: … }` object form** (sidesteps the throwing shorthand slot/taps paths — a `{type: chord}` map still decodes as a `KeySlot(ChordKey)`); a key structurally equal to a `TemplateKeys` value emits as its `$name` scalar (reverse-map); a *pure* single-spec `BaseKey` (`== BaseKey(spec)`, spec not `$…`) emits as a bare quoted scalar; **omit any field equal to its default** so an absent field re-parses to that default; row key-lists / `moreKeys` / `languages` emit as YAML *lists* (the `YamlList` branch, sidestepping the space-split scalar form). Double-quotes scalars with escaping; enum/number/bool unquoted. Two supporting model tweaks shipped with it: **`Keyboard.rows` made public** (was `private`) so the emitter/editor read the AUTHORED rows (use `getEffectiveRows` for the runtime rows that add the number/bottom rows); **`GapKey` made a `data class`** (was a plain `class`) so two gaps with equal attributes compare equal (the template reverse-map + round-trip equality rely on structural key equality). 3 files: `KeyboardYamlEmitter.kt` (new), `Keyboard.kt`, `BaseKey.kt`.

- **Visual keyboard-layout editor — Phase 1, per-key editing (LIVE)** (commit `cf4b89047`, on the emitter `683f47083`). Edits the user's imported custom layouts in-app, key by key, with a live preview; replaces hand-editing the YAML. Reached at **Settings → Keyboard UI → "Keyboard editor"** (a `NavigationItem` on `KxkbSizingScreen`) and from the space-swipe switcher's Keyboard UI column. **Decisions (locked with the user):** model-based editing + the new emitter (NOT raw-text); per-key editing first (structural ops are Phase 2); preview→explicit-Apply; full-screen settings page with embedded preview; per-key edits open a **dedicated sub-screen** (not an inline panel). Files (all under `uix/settings/pages/`):
  - **`KeyboardEditorSession.kt`** — the spine. A singleton holding the shared working `V2Keyboard` (Compose `mutableStateOf`) across the editor's navigation, plus `EditPath(row, col, fields)` addressing and recursive get/replace of keys and **slots** (compass/cluster/column directions are `CompassSlot` = `KeySlot`|`ModSlot`; case branches / compass `primary` / flick dirs are plain `Key`). `buildPreview(context, widthPx)` builds a runtime keyboard from the working model **at the exact render width** (scale 1) by emitting it and feeding it through the `ForceCustomLayoutYamlB64` private-IME-option seam into `KeyboardLayoutSetV2` (same path `KeyboardLayoutPreview` uses). **Tap→source mapping (the load-bearing fact, validated in a throwaway spike):** a tapped runtime `Key.row`/`Key.column` index the **authored `kb.rows` directly** — the layouts set `numberRowMode: AlwaysDisabled`, so the engine does NOT lay out the auto-added number row, and the top authored row is `key.row = 0`. (Going via `getEffectiveRows(0)` is WRONG: it prepends a number row at index 0, shifting every row up one — the top row read as a phantom number row → "auto-generated". This was the bug the user caught.) Also `setOverrideWidth(KeyWidth, Float?)`.
  - **`KeyboardEditorScreen.kt`** — layout picker → live preview (`EditorPreview`: a real `KeyboardView` in an `AndroidView`, sized to native px (built at the container width via `BoxWithConstraints`, so 1:1, identity tap mapping) with a transparent `pointerInput` overlay; **hit-test against each key's VISUAL bounds `x..x+width`, NOT `key.hitBox`** — the hitBox is inflated by the gap and overlaps neighbouring rows, which selected the wrong row). Tap → `pathForRuntimeKey` → navigate to the edit screen. Apply (emit → `updateCustomLayoutsAndSyncSubtypes` → `Subtypes.switchToSubtypeString` so the live keyboard reloads), Export (→ export screen), Revert, a "Custom key widths" section editing `overrideWidths` Custom1–4 as %, and two **"Close Keyboard editor"** buttons (`findActivity()?.finish()`) — one at top, one directly above the preview.
  - **`KeyEditScreen.kt`** — the per-key form. A **Type** picker converts between base/compass/cluster/column/macro/chord/cycle/case/gap (seeded from the current key). Type-specific scalar fields inline; an **Attributes** editor (width / style / moreKeyMode via dropdowns, heightRows, and the boolean flags as tri-state `(inherit)/true/false`). The width picker shows **meaningful labels** ("Regular (1 key)", "Functional", "Grow (fills the row)", "Custom1 — 9% of keyboard" resolved from `overrideWidths`) instead of the bare token. Nested keys are **rows that drill in** (navigate to `KeyEdit(childPath)`); a `{mod}` direction slot gets inline mod/on/label fields, an empty slot an "Add key", a `KeySlot` an "Edit"/✕. **Gotcha fixed:** compass/cluster/column/flick `attributes` is `KeyAttributes?` — when null the attributes section was skipped entirely (no width control); `keyAttributes()` now coalesces null→`KeyAttributes()` for those types so the section always shows.
  - **`KeyboardEditorExportScreen.kt`** — shows the emitted YAML with **Copy** (`LocalClipboardManager`) and **Save to file** (`ActivityResultContracts.CreateDocument`, prefilled with the layout name; the name keeps Unicode — only `/\` + control chars are replaced, NOT all non-ASCII, so Japanese names survive).
  - Routing: string routes `keyboardeditor` / `keyboardeditorexport` + typed `Route.KeyEdit(path)` in `SettingsNavigator.kt`. The space-swipe catalog (`Subtypes.kt` `LayoutSwitcherShortcutCatalog`) was reworked: **removed "Themes"**, added **"Keyboard editor"** (last in the list = top of the column, under the "Keyboard UI" header — the column renders bottom-up), and moved "All settings"/"Special keys" to the front (nearest the finger). No native change.
  - **Next:** Phase 2 is COMPLETE (add/move/remove/duplicate keys & rows, alt-page editing + cross-layout copy — all below). Phase 3 = engine-backed **per-key colour** and **per-key cluster spacing** (need new `KeyAttributes` fields first — like the caps-lock-colour commit — then an editor control). A validation spike (`KeyboardEditorSpike.kt`) was used to prove the emitter round-trip + tap mapping, then deleted.

- **Adjacent identical keys render separately — `mergeDuplicates` disabled** (engine commit `e049a4ea6`). Upstream FUTO's `LayoutEngine.mergeDuplicates` (in the 0.1.28 base, from the v2 "Refactor layout definition system") folded any two ADJACENT `LayoutEntry.Key`s with equal `ComputedKeyData` into one key of combined width — called unconditionally in `buildLayoutRow`, so it affected the REAL keyboard, not just previews. Two identical neighbours therefore always collapsed into a single double-width key (this is what made the editor's "Duplicate key" look broken — and means no editor-only trick could show twin keys, since Apply re-merges). Changed `mergeDuplicates` to `= row` (no-op). Now adjacent identical keys render as separate keys; **wide keys must be made with width tokens (`Custom*`/`Grow`), never by duplicating a key.** Split-layout middle-key duplication is unaffected (that's `splitRow` at render-time, after `buildLayoutRow`). 1 file.

- **Layout editor — Phase 3 Stage 1: per-key text appearance (LIVE)** (engine `29de03ec3` + editor `6e2652da3`). Per-key visual overrides, plumbed end-to-end. `KeyAttributes` gains `color: Int?` (label/icon ARGB), `fontScale: Float?` and `hintScale: Float?` (multipliers on the computed primary / hint text size; null = inherit theme) — resolved in `getEffectiveAttributes`/`plus`, emitted by the YAML emitter (`attrsNode`). They reach `ComputedKeyData` via `BaseKey.computeDataWithExtraAttrs` (covers base + cluster/column primary, since those pass the key's attributes as `extraAttrs` to the centre key) and via `CompassKey`/`MacroKey`/`ChordKey`/`CycleKey`'s own `computeData` (compass applies its OWN `attributes` in the final copy, since the primary is computed without them); then onto the runtime `Key` (`colorOverride`/`fontScaleOverride`/`hintScaleOverride`, set in `computedKeyToKey`); then `KeyboardView.onDrawKey` rebuilds the `KeyDrawingConfiguration` with `textColor = colorOverride`, `textSize ×= fontScale`, `hintSize ×= hintScale` (like the caps-lock-colour block — the icon tint + label paint both honour textColor). **Editor UI** — the key Edit screen's "Appearance (live)" section: the reused RGBA `ColorSetting`/`ColorChannelSlider` (made `internal` in `Typing.kt`) + a "Use theme colour" clear button, and a `SizePercentRow` (a `Switch` for theme/override + a `Slider` shown 20–300%, storing the multiplier). Above them a **single-key live preview at REAL size**: `KeyboardEditorSession.buildPreview` builds the full page keyboard at `displayMetrics.widthPixels`, then `SingleKeyCrop` (`renderKeyBitmap`) draws that keyboard into a `Bitmap` and crops it to the key's exact pixel rect (`drawX, y, drawWidth, height`) shown as an `Image` — a deterministic 1:1 crop (the earlier `offset`+`requiredSize`+`clipToBounds` approach kept landing on the wrong key, so this bitmap crop replaced it). For a `CaseSelector` (shift) the appearance writes to all shift-state branches (`caseWithUnifiedAttributes`). **Gotcha:** the single-key preview must build with `moreKeyMode: OnlyExplicit` — a lone non-counting key (cluster/compass) otherwise crashes FUTO's auto-moreKeys builder and the build falls back to the error layout. 11 files.

- **Layout editor — Phase 3 Stage 2: per-key background + border colour (LIVE)** (commit `13491a707`). Same plumbing for `KeyAttributes.backgroundColor` / `borderColor` (ARGB, null = theme) → `ComputedKeyData` → `Key.backgroundColorOverride` / `borderColorOverride`. In `KeyboardView.onDrawKey`, when either is set, build a `GradientDrawable` (RECTANGLE) matching the theme: **corner radius and existing fill are read from the key's current `GradientDrawable`** (`getCornerRadius()` / `getColor()`, both API 24 — minSdk is 24), and the theme's **border colour + stroke width are pushed per-geometry** from `LatinIME.withPerKindLook` via `KeyboardView.setKeyDrawDefaults(bg, border, strokePx)` (the bg/border statics, defaulted from `scheme.extended.keyboardContainer` / `scheme.base.outline` / `overlaid.keyStrokeWidthDp`). So override-background-only keeps the existing border; override-border-only keeps the existing fill (and synthesises a ~2dp border when the theme is borderless). Editor "Appearance (live)": Background + Border RGBA `ColorSetting`s with clear buttons, reflected in the single-key crop preview. **Next:** per-key **cluster spacing** (the global `clusterLeftOffset`/`clusterRightOffset` pushed via `setClusterMainOffsets` → make per-key). 9 files.

- **Layout editor — Phase 3 (completion): per-key cluster spacing, character position & secondary-label offsets (LIVE)** (commit `255811886`). Finishes Phase 3's per-key geometry overrides on the same model→render→editor plumbing as the colour/size overrides. `KeyAttributes` gains `clusterLeftOffset`/`clusterRightOffset` (cluster band insets, fraction of key width), `labelOffsetX`/`labelOffsetY` (character position, ±keyWidth/keyHeight) and `flickTopOffset`/`flickBottomOffset`/`flickLeftOffset`/`flickRightOffset` (the secondary 4D / cluster side-label band) — all nullable (null = theme), resolved in `getEffectiveAttributes`/`plus`, emitted by `attrsNode`. They reach `ComputedKeyData` (via `computeDataWithExtraAttrs` for base + cluster/column primary, and the compass/macro/chord/cycle `computeData` final copies) → runtime `Key` (`clusterLeftOffsetOverride`/`clusterRightOffsetOverride`, `labelOffsetX`/`labelOffsetY`, `flickTopOffset`/…/`flickRightOffset`, set in `computedKeyToKey`). In `KeyboardView`: `drawClusterMains` reads the per-key cluster insets (falling back to theme `sClusterLeftOff`/`sClusterRightOff`); `onDrawKeyTopVisuals` shifts the label, icon and the `drawClusterMains` call by `charOffX/Y = labelOffset × keyWidth/keyHeight`; `drawAtRestFlickLabels` positions the four side-label bands per-key (falling back to `sFlickTopOff`/`sFlickBotOff`/`sFlickLeftOff`/`sFlickRightOff`). **Editor** "Appearance (live)": `OverrideSliderRow(label, value, range, default, onChange)` controls — Cluster left/right spacing (0..0.6, default 0.333), Horizontal/Vertical position (−0.5..0.5, default 0), Secondary top/bottom/left/right (0..0.6, defaults 0.30/0.40/0.34/0.34) — each Switch-off = theme, shown live on the single-key REAL-size crop preview; the secondary-band rows are gated to `CompassKey`/`ClusterKey`/`ColumnKey`/`FlickKey`. **Phase 3 complete.** 8 files, no native change.

- **Cluster/column auto-capitalise when shifted** (commit `21e1da0f2`). A cluster/column now upper-cases its WHOLE predictive band when the layout is shifted, so it no longer needs wrapping in a `case` purely to capitalise. `ClusterKey`/`ColumnKey.computeData` detect the shifted element (`ELEMENT_SYMBOLS_SHIFTED`, `_ALPHABET_MANUAL_SHIFTED`, `_AUTOMATIC_SHIFTED`, `_SHIFT_LOCKED`, `_ALPHABET_SHIFT_LOCK_SHIFTED`) **gated on the resolved `shiftable` attribute** (`(attributes ?: KeyAttributes()).getEffectiveAttributes(row, keyboard).shiftable == true`, default true via `DefaultKeyAttributes`) — mirroring `BaseKey.computeData`'s existing auto-shift — and build `effMain = main.uppercase(params.mId.locale)`. `effMain` then drives every output: the codepoints of the tap-commit primary (centre main) and the West/East precise side flicks, the drawn `label`, and the `clusterMains` codepoints fed to the proximity model. **Prediction is unaffected** because `LanguageModelFacilitator.clusterBaseFold()` lowercases (`Character.toLowerCase`) before set-membership against the lowercase dict trie, so an upper-cased band still matches. `attributes: { shiftable: false }` opts a band out — matching base keys and compass keys (whose primary is itself an auto-shifting `BaseKey`), which have always auto-capitalised. The **Special Keys** settings page gains a "Case (per-shift-state key)" section explaining what `case` is for (per-state glyph / key-type / presence differences, caps-lock vs one-shot) and that clusters/columns no longer need it just to capitalise. 2 files (`CompassKey.kt` + `SpecialKeys.kt`), no native change.

- **Layout editor — Phase 2 duplicate key / duplicate row (LIVE)** (commit `0d71fd01d`, depends on `e049a4ea6`). `KeyboardEditorSession.duplicateKey(page, row, col)` inserts a copy of the key right after it; `duplicateRow(page, row)` inserts a `row.copy()` right after the row (both via `mutateRowsFor`; keys/rows are immutable so sharing the instance is safe). UI: a **"Duplicate key"** button in `KeyEditScreen`'s "Position in row" section (inserts the copy then navigates to edit it — `EditPath(row, col+1, page)`) and a **"dup"** TextButton per row in `KeyboardEditorScreen`'s Rows section. Works on base + alt pages. **Note:** duplicating a key in an already-full fixed-width row (e.g. 4 cluster keys = 1.0) overflows the row (5 × Custom = 1.25) — expected; delete/replace another key to rebalance. 3 files.

- **Layout editor — alt-page editing + cross-layout copy + Apply-as-new (LIVE)** (commit `320bc8074`). Extends the editor to the alt pages and adds cross-layout management. **Per-page editing:** `EditPath` gained a `page` field (−1 = base rows, 0..N = `altPages[page]`); `KeyboardEditorSession.keyAt`/`replaceKey` and the structural ops are page-aware (`mutateRowsFor(page)` + a `withRowsForPage` extension); `pageRows(page)` exposes a page's rows. A **page selector** (Base / alt0 (sym) / alt1 (altGr) / …, from `altPageCount()`) on the main screen drives the preview, the Rows section and tap-to-edit. An alt page is previewed by building a **standalone keyboard whose base rows ARE that page's rows** (`base.copy(rows = altPages[p], altPages = emptyList())`) so it renders + tap-maps exactly like the base; the page's switch / `← ABC` keys just render as plain keys. `buildPreview` was refactored to delegate to a private `buildPreviewFor(context, widthPx, kb, language)`. The key-edit breadcrumb shows the page (`alt0 · row 2, col 3`). **Alt-page management ("Alt pages (max 3)"** — `MAX_ALT_PAGES = 3`, because the engine only has `key_to_alt_0/1/2_layout` → `KeyboardLayoutKind.Alphabet1/2/3`): each current page has ◀/▶ (`moveAltPage`, the selected `page` follows the swap) and ✕ (`deleteAltPage`, clamps `page`); `appendAltPageFrom(src, srcPage)` appends a single alt page copied from another custom layout (no-op at cap / if missing). The "Append a page from another layout" list shows **one row per (layout · altN)** with **Preview** → `AltPagePreviewScreen` (`Route.AltPreview(srcIdx, page)`, renders that page full-width via `buildAltPagePreview` with its own Append button) and **Append**. **"Apply as a new layout":** an `AlertDialog` (Language + name, prefilled from the current layout) emits `working.copy(name=…)`, appends it as a new `CustomLayout` via `updateCustomLayoutsAndSyncSubtypes` (same path as "Create new layout", so it registers as a subtype), refreshes the picker, and `KeyboardEditorSession.load`s the copy (reopens the editor on it). The action area is now `Apply | Export YAML` over `Apply as a new layout | Revert changes`. **`setPage`→`selectPage` gotcha:** a public `fun setPage(Int)` clashes with the `var page`'s generated JVM setter (`setPage(I)V`) — renamed to `selectPage`. 5 files (1 new: `AltPagePreviewScreen.kt`) + `Route.AltPreview`. No native change.

- **Layout editor — Phase 2: add/move/remove keys & rows (LIVE)** (commit `7796125ce`). Structural editing on the base rows. `KeyboardEditorSession` gains `insertKey`/`removeKey`/`moveKey`/`addRow`/`removeRow`/`moveRow`, all routed through a private `mutateRows { rows -> … }` helper (copies `kb.rows`, sets `dirty`, rebuilds the live preview) + a `Row.withKeys(list)` helper that preserves the row kind (numbers/letters/bottom). **Guards:** `removeKey` keeps ≥1 key per row (delete the row instead); `removeRow` keeps ≥1 row and never deletes the last letters row; moves clamp to bounds. **UI:** `KeyEditScreen` shows a "Position in row" section for **top-level row keys only** (`path.fields.isEmpty()`) — ◀ Move / Move ▶ (swap with neighbour, then `popBackStack()` to the preview), Insert ◀ / Insert ▶ (insert a `BaseKey("a")` and `navigate` into editing it), Delete key (disabled when the row has 1 key). `KeyboardEditorScreen` has a "Rows" section listing every row (`[i] type ·count`) with ▲/▼ (`moveRow`), +key (`insertKey` at end + navigate to edit it), ✕ (`removeRow`), and an "Add row" button (`addRow(indexOfLast letters)`); it lives OUTSIDE the preview's null-guard so an invalid intermediate arrangement (preview fails to build → null) is still recoverable. **Also fixed here:** a `CaseSelector` (e.g. the shift key) has no attributes of its own — width/style live on its per-state branches — so the Edit screen now shows a **unified Attributes editor** reading `key.normal`'s attributes and writing the edited set to every attribute-bearing branch via `caseWithUnifiedAttributes` (was: no attributes section at all for shift keys). 3 files: `KeyboardEditorSession.kt`, `KeyEditScreen.kt`, `KeyboardEditorScreen.kt`. No native change.

- **`column` key type — a vertical `cluster`** (commit `c0730836c`). The compact special-key YAML type `column` (`@SerialName("column")`) is the vertical sibling of `cluster`: `{ type: column, main: "abc", left: …, right: …, upLeft: …, upRight: …, downLeft: …, downRight: … }`. The `main` chars stack TOP-TO-BOTTOM as the predictive band (sub-rects split the key HEIGHT, letters feed the decoder exactly like cluster), the centre main is the tap-commit primary, and North/South flicks give precise access to the top/bottom main (mirroring cluster's West/East). The six author slots become the optional 3+3 small side keys — left column `upLeft`/`left`/`downLeft`, right column `upRight`/`right`/`downRight` — reached by flick and drawn small by the *existing* at-rest flick-label machinery (so key-sliding should be on, same as cluster slots); `up`/`down` aren't author-settable (they're the band ends). Behaviour is otherwise identical to cluster (letter-only prediction, `{ mod }`/`{ chord }`/`{ macro }` slot desugaring, study popup, `showPopup`). Implementation, 6 files: `KeyData.kt` — `ClusterMain` gains `val vertical: Boolean = false` (when set, `column`/`columnCount` are read as row/rowCount). `CompassKey.kt` — new `ColumnKey` data class, a near-clone of `ClusterKey` with North=`cps[0]`/South=`cps[n-1]` band ends, `West/East/NorthWest/NorthEast/SouthWest/SouthEast` ← the six slots, and `clusterMains` built with `vertical = true`; it's a sealed-interface sibling so kotlinx auto-registers it (NO manual `subclass()` needed). `ProximityInfo.java` — `clusterSubKeys` splits by `key.getHeight()` when `m.getVertical()`. `KeyboardView.java` — `drawClusterMains` has a vertical branch that centres each main in its 1/n height slice and sizes it `min(slice * CLUSTER_VERTICAL_FILL(0.62f), primaryLetterSize)` so the stack stays tidy and proportionate at any row height (an earlier step/scale approach drew the glyphs oversized + loose — the slice-fit replaced it); `drawAtRestFlickLabels` detects a vertical cluster (`cMains.get(0).getVertical()`) and skips North/South instead of West/East. `KeyPreviewView.java` — stacks the mains vertically in the study popup. `SpecialKeys.kt` — added a "Column (vertical predictive multi-key)" reference section. **Layout note (not a code bug)**: a row of `column` keys only fills the width if its key count equals the layout's widest letter row — otherwise FUTO insets it with side gaps (`LayoutEngine` adds gaps to short letter rows; `regularKeyWidth = layoutWidth / max letter-row key count`). Fix in the *layout*: either give the keys `attributes: { width: Grow }` (splits the row evenly; not for split layouts) or make every letter row the same width (e.g. trim the number row to match) so the grid is uniform and all fill at `Regular`. 6 files: `KeyData.kt`, `CompassKey.kt`, `ProximityInfo.java`, `KeyboardView.java`, `KeyPreviewView.java`, `SpecialKeys.kt`.

- **Row-spanning keys via `heightRows`** (commit `7e3636632`). Any key can declare `attributes: { heightRows: N }` to span N rows — it takes its own row's height plus the next (N−1) rows' and bypasses the height clamp. Built to let a tall key (e.g. a `column` key) flank a multi-row block: the test layout `kxkb en colspan 5r13c` has five left + five right column keys with `heightRows: 3` enclosing the centre 3×3 nav block (`[↑] / ←~→ / Esc↓Ctrl`), capped by `( $ )` from the top row. **Key architectural fact**: the layout is strictly row-by-row with NO column grid (`LayoutEngine.addKeys` stacks full-width strips, `currentY += row.height`; keys flow left-to-right by accumulating `widthPx`). So a tall key does NOT auto-push the rows below it aside — the author must reserve the covered cells in each spanned row with `gap` keys (`type: gap`, Regular width by default) and keep widths uniform so columns line up. Each spanned row still describes the full width (e.g. row C = `5 gaps · ← ~ → · 5 gaps` = 13 cells, so the centre 3 land under the top row's `( $ )`). The renderer (`KeyboardView`) and hit-tester (`ProximityInfo`) already work off each `Key`'s own `x/y/w/h`, so a tall key needed no change there — the work was confined to the engine. Implementation, 4 files: `BaseKey.kt` — `KeyAttributes` gains `val heightRows: Int? = null`, resolved in both `getEffectiveAttributes` and `plus`, with `DefaultKeyAttributes.heightRows = 1`. `KeyData.kt` — `ComputedKeyData` gains `val heightRows: Int = 1`. `BaseKey.computeData` sets `heightRows = (attributes.heightRows ?: 1).coerceAtLeast(1)` (and the `column`/`cluster`/etc. types inherit it through `primaryData.copy(...)`). `LayoutEngine.kt` — `addKeys` captures `finalRowHeights = rows.map { it.height }` (a member); in `computedKeyToKey`, a key with `data.heightRows > 1` gets `effHeight = finalRowHeights.subList(row, min(row+heightRows, size)).sum()`, used for the key height, hitbox bottom, and the flick-key recursion; the `rowHeightMode.clampHeight` branch is gated with `data.heightRows <= 1` so a spanning key isn't clamped back (the default `ClampHeight` mode would otherwise turn the extra rows into vertical gap). **Horizontal multi-cell spanning was deliberately NOT added** — it's already available via the `Custom` width tokens; a `widthCols` sugar could be added later for symmetry but would entangle the row width-distribution math. 4 files: `BaseKey.kt`, `KeyData.kt`, `LayoutEngine.kt`, `SpecialKeys.kt`.

- **Multiling-style cluster prediction** (commit `3bb85d1a1`). The `cluster` and `column` special keys each represent N letters per tap (the predictive mains band), so each tap commits to a SET of letters rather than a single letter. This adds the matching prediction pipeline: dictionary and LM suggestions get constrained to prefixes that stay inside the tapped letter-sets, and a Multiling-style enumeration over the cartesian product of per-tap mains injects valid in-set words that the existing tap-decoder and LM beam both miss (people, simple, …). Built and tuned over several on-device test rounds against the user's `kxkb_en_3+2_5r9c` layout. **Five layers**:
  - **Spatial flatten** (`ProximityInfo.java`). Every main of a cluster gets the SAME key rectangle (`CLUSTER_PREDICTION_SPREAD = 1.0f`), so all mains of a cluster share identical `keyX/keyY/keyWidth/keyHeight`. No ordinary key has that property — it's the cluster signature used by every downstream layer (native dict, native LM, Kotlin merge) to recognise a tap as cluster vs single-letter; detection is pure rectangle equality, no separate metadata to plumb.
  - **Native dict decoder exact-set classifier** (`proximity_info_utils.h`, `proximity_info_state.{h,cpp}`). `calculateProximities` gains an `int *outIsExactSet` out-param. When a tap's primary key shares its rect with the cell's other mains (cluster detected), the proximity list collapses to ONLY the co-located mains and `outIsExactSet[i] = 1`. `ProximityInfoState` carries `int mInputIsExactSet[MAX_WORD_LENGTH]`; `getProximityType` consults it at the top, returning MATCH for any listed main and **UNRELATED** (not SUBSTITUTION) for off-band keys — locks the dict's tap exploration in-set. Threaded through `initializeProximities` in `proximity_info.h`. Entirely internal C++; **no JNI signature changes**.
  - **Native LM mix purification + NUM_RESULTS bump** (`org_futo_inputmethod_latin_xlm_LanguageModel.cpp`). After `decomposeTapPosition` builds the per-tap token mix (top-`NUM_TOKEN_MIX=4` letter tokens weighted by tap-circle overlap), find the dominant key (proportion > 0.5), get its `(keyX, keyY)` via NEW public `AK_FORCE_INLINE` `getKeyX/getKeyY` accessors on `ProximityInfo` (added in `proximity_info.h`), count co-located keys (same x,y) with positive proportion. If ≥ 2 → cluster → zero all non-co-located proportions so the per-tap token mix stays in-set. Gesture is automatically skipped (Kotlin sets `partialWord=""` for batch mode → mix loop runs 0 times). `#define NUM_RESULTS 10` (was 3) so the Kotlin merge sees enough beam candidates to work with.
  - **`DictionaryFacilitator.getFrequency` exposed** (`DictionaryFacilitator.java` + `DictionaryFacilitatorImpl.java`). The private `getFrequency(word)` is promoted to public + `@Override`; matching `int getFrequency(final String word)` entry added on the interface. Used by the enumeration layer to rank in-set valid words by dict frequency.
  - **Kotlin merge — cluster-aware filter + bias + enumeration** (`LanguageModelFacilitator.kt`):
    - `clusterAllowedSets(values)`: returns `List<Set<Int>>?` of per-tap allowed lowercase codepoints. Per tap: `keyboard.getNearestKeys(x,y).firstOrNull{ it.isOnKey(x,y) }.clusterMains.map{ toLowerCase(it.codePoint) }.toHashSet()`. Returns `null` for gesture (`composedData.mIsBatchMode`), missing keyboard, or layouts with no cluster keys (filter doesn't apply). Non-cluster keys constrained to `{their own code}`; unresolved taps get an empty set (left unconstrained at that position).
    - `prefixInClusterSets(word, sets)`: prefix-not-exact-length so `simp → simple/simply` works (`word.length >= sets.size`, every position with a non-empty set must match by `Character.toLowerCase`). Applied to BOTH the LM suggestion list (filter call before the bias map) and the dict suggestion list (`mapNotNull` filter on `suggestedWordsDict.mSuggestedWordInfoList`, exempting `suggestedWordsDict.typedWordInfo`).
    - `clusterLengthBias(len, tapCount)`: `2.0f` for exact-length, `Math.pow(baseFactor, len-tapCount)` for completions, where **`baseFactor = if (tapCount == 4) 0.7 else 0.55`** — softer specifically for 4-tap prefixes so `simp → simple/simply` actually surfaces in the alternative slots (shorter inputs get exact-length matches injected by enumeration and don't need completion help; longer inputs are usually full-word typing where completions are noise rather than intent).
    - `clusterEnumerateValid(clusterSets)`: cartesian product of per-tap letter mains (symbols filtered via `Character.isLetter`), **capped at 2000 combinations** so per-keystroke cost stays bounded (typical 6-tap clusters are ~150 combinations). Each candidate checked via `dictionaryFacilitator.isValidSuggestionWord(word)`, returned as `List<Pair<String, Int>>` with `dictionaryFacilitator.getFrequency(word).coerceAtLeast(0)`.
    - Enumeration injection in `processAndMergeSuggestions` (after the bias `mapNotNull` builds `suggestedWordsDictList`): **dual freq threshold** — when the LM also picked the word it only needs `freq >= 30` (contextually confirmed by the LM); otherwise `>= 100` to cull proper-noun/abbreviation noise (`Diu`, `yip`, `doz`, …). On warmup with empty LM, a single `>= 50` floor. Survivors not already in the dict list are added as `KIND_CORRECTION` with score `(100M + freq*1M) * 2` — the 2× exact-length boost pre-applied since these are exact-length matches by construction (the bias `mapNotNull` runs only over the dict's original output, so injected entries skip it).
  - **Trade-offs settled with on-device testing.** Several rounds were tried over the user's battery (`the / you / yes / vet / cat / people / simple / simp`):
    - *Round A* — single `freq >= 50` filter, no LM signal. `vet` survives, but `cab > cat` in dict freq so the wrong word wins.
    - *Round B (shipped)* — dual `>= 30/>= 100` threshold. `cat > cab` works (cab fails the non-LM-confirmed gate), at the cost of `vet` disappearing in some contexts (not LM-confirmed there, freq below 100). User explicitly preferred this.
    - *Round C (tried, reverted)* — single `>= 50` floor + LM-confirmation BOOST on score (4× vs 2×) + proper-noun heuristic (skip if `getFrequency(capForm) > getFrequency(word)`). Brings `vet` back but the boost wasn't strong enough to flip `cab/cat` (cab also gets enumerated and freq-ranks ahead), and the merge re-ranking caused `simple` to lose its slot in the `simp` test.
    - The flat dict-frequency layer is the fundamental ceiling here; further wins would need either rank-aware LM boosting or bypassing dict freq for ranking entirely.
  - **Two known small anomalies left in place.** (1) `den` occasionally appears for the `gas/yes` cluster sequence even though `n` isn't in the third cluster's mains — most likely an edge tap snapping to the adjacent cluster, not a code bug (no source of `den` survives the prefix filter unless the actual `clusterSets[2]` contains `n`). (2) `Diu` (capitalised) can still appear from the dict's own native tap-decoder; the proper-noun-style filtering only ever applied to our enumeration's lowercase `diu` injection (and Round C's heuristic, since reverted). Both judged acceptable.
  - **Where to extend next** if this needs more work: rank-aware LM boost (top-N LM picks weighted more than tail), exposing `Dictionary.getMaxFrequencyOfExactMatches` separately from user-history freq, or per-position confidence weighting in the cartesian enumeration (tap centre vs edge).
  - 9 files: `ProximityInfo.java`, `DictionaryFacilitator.java`, `DictionaryFacilitatorImpl.java`, `LanguageModelFacilitator.kt`, `org_futo_inputmethod_latin_xlm_LanguageModel.cpp`, `proximity_info.h`, `proximity_info_state.cpp`, `proximity_info_state.h`, `proximity_info_utils.h`.

- **Cluster prediction for accented / no-transformer languages (Czech, Russian) (LIVE)** (commit `bc1cd9e7c`). The cluster prediction above only worked for **English**. Two root causes, both found by on-device debugging (Huawei suppresses app logcat, so diagnostics were written to a file in `getExternalFilesDir` and pulled with `adb pull /sdcard/Android/data/shiroikuma.futokxkb/files/…` — the working pattern when logcat is dead). All fixes are **language-agnostic, Kotlin-only (2 files), no native change**:
  - **Root cause 1 — the whole pipeline was transformer-gated.** The cluster filter + enumeration live in `LanguageModelFacilitator.processAndMergeSuggestions`, which `GeneralIME` only calls when the transformer LM returns results (`!lmResult.isNullOrEmpty()`). Czech/Russian have a **dictionary but no transformer model** (`ModelPaths.getModelOptions` has no entry → `runLanguageModel` sets `skipLanguage` → `shouldPassThroughToLegacy()` true → `makePredictionInputValues` returns null → the dict-only legacy branch runs, with **none** of the cluster code). English worked only because it *has* a transformer model. Fix: **`maybeApplyClusterConstraint(inputStyle, dictResult)`** — called from `GeneralIME`'s `dictResult != null` (legacy) branch — builds values via `makePredictionInputValues(…, ignorePassthrough = true)` (new param that skips the passthrough guard), and if the layout is a cluster layout routes the raw dict result back through `processAndMergeSuggestions` with an **empty** LM list. Returns null (caller keeps the untouched dict result) for non-cluster layouts, so ordinary legacy typing is unaffected.
  - **Root cause 2 — accent-blindness.** Cluster mains are base-Latin/Cyrillic but the dictionary stores accented words, and `isValidSuggestionWord`/`getFrequency` are **accent-exact**; the dict decoder only ever surfaced wrong neighbours (`těžce`, never `těžko`). Fix: **`clusterBaseFold(cp)`** — `Character.toLowerCase` then (non-ASCII only) NFD-decompose + drop combining marks → base letter (`ě/é→e`, `ž→z`, `ů→u`; Cyrillic `ё→е`, `й→и`), mirroring the native `toBaseLowerCase`/`BASE_CHARS`. Used in `clusterAllowedSets` (folds the mains) and `prefixInClusterSets` (folds the candidate char). ASCII fast-path → **English byte-for-byte unchanged**.
  - **The enumeration is now a dictionary trie-walk, not a cartesian product** — `clusterTrieWalk(clusterSets, accentVariants)`. KEY DISCOVERY: `Dictionary.getValidNextCodePoints(ComposedData)` already exists (aggregated across dicts by `DictionaryFacilitator.getValidNextCodePoints`, which was **already on the interface** — no new method needed) and returns the valid next code points after a prefix. The walk: at each tap, get the dict's valid continuations of the prefix built so far and follow only those whose `clusterBaseFold` is in that tap's cluster set. This **prunes to real dictionary paths** (the 10-tap `jednoduché` returns only the 2 valid in-set words — no blow-up), yields the **accented spelling straight from the trie** (no per-language accent tables), and has no length cap. **GOTCHA:** `getValidNextCodePoints("")` returns nothing (the query needs a non-empty prefix), so **position 0 is seeded** from the cluster's own letters + their accent variants (harvested from the dict's near-candidates) rather than a dict query; positions ≥1 use the dict. The old cartesian+accent-harvest enumeration (`clusterEnumerateValid` + `accentVariantsFromDict`) is **kept as a fallback** when the walk returns nothing.
  - **Confident-cluster auto-commit.** On a cluster layout the "typed word" is the meaningless centre-letter commit (`teuwi` for `těžko`), so AOSP's normalized-score-vs-typed-word autocorrect test in `Suggest.obtainNonBatchedInputSuggestedWords` always fails (huge edit distance) → the predicted word sat in slot 2 and space committed the literal. Fix: for cluster layouts with a confident in-set word AND **≥ 4 taps** (shorter inputs stay suggest-only to avoid mis-commit), pass `firstSuggestionExceedsConfidenceThreshold = true` to the `SuggestionResults` constructor (computed up front since that flag is constructor-set/final), so the top in-set word becomes the auto-commit. **Tuning knobs if needed:** the `>= 4` tap threshold (commit aggressiveness) and the `>= 30/100/50` enumeration freq floors.
  - **Why the earlier "fix" only half-worked:** `těžko`/`daleko` (short/medium) succeeded once the fold + legacy-path + accent enumeration landed; `jednoduché` (10 taps) failed until the trie-walk replaced the cartesian (which exceeded its size cap). A regression round where the trie-walk returned empty for *every* word traced to the seed-position-0 gotcha above.
  - Verified on-device for Czech (`těžko`, `daleko`, `jednoduché`) and Russian. 2 files: `LanguageModelFacilitator.kt` (`clusterBaseFold`, `clusterTrieWalk` + `clusterWalkPointers`, `accentVariantsFromDict`/`clusterEnumerateValid` fallback, `maybeApplyClusterConstraint`, `makePredictionInputValues` `ignorePassthrough`, the hoisted enumeration + `clusterConfidentAutocorrect`), `GeneralIME.kt` (the legacy-branch wiring).

- **Cluster-prediction non-QWERTY-gate fix + on-device tracer (LIVE; built `0.1.29-rc1+4`, pending Push).** Restores the two cluster-prediction features above after the 0.1.29-rc1 rebase broke them, and adds a permanent testing harness. **The break:** upstream's new gate in `makePredictionInputValues` returns null for non-QWERTY layouts (default-off `AllowTransformerOnNonQWERTYLayouts`); cluster layouts are non-QWERTY → the whole pipeline was bypassed (full analysis in §"Sync to a new upstream version → 0.1.29-rc1 sync learnings"). **The fix** (1 changed method): the gate now accepts `kb.sortedKeys.any { !it.clusterMains.isNullOrEmpty() }`, restoring both the transformer-merge path (English) and the legacy `maybeApplyClusterConstraint` path (cz/ru); upstream's never-set `prevAcceptedLayout` cache is now assigned. **The tracer** (`ClusterPredictionDebug.kt`, new): a default-off dev toggle (Settings → Developer → "Cluster prediction debug") writing a staged per-update trace — the `makePredictionInputValues` gate decision (incl. a "REFUSED by QWERTY gate" line if the regression returns), the per-tap folded `clusterSets`, and the `dict.raw` / `lm(filtered)` / `enum.trieWalk` / `enum.selected` / `FINAL` candidate lists — to `cluster_pred_debug.log` in the app external files dir (`adb pull /sdcard/Android/data/shiroikuma.futokxkb/files/cluster_pred_debug.log`), the file-based pattern that works where logcat is suppressed. A companion "Isolate" toggle blanks the idle next-word/topBar bar so only while-composing cluster candidates show. Instrumented in `GeneralIME.kt` (path-decision trace + the isolate gate in `showPredictionsWithTopBar`) and `LanguageModelFacilitator.kt` (gate-refusal + `maybeApplyClusterConstraint` + `processAndMergeSuggestions` traces). The whole testing procedure — Layer A pre-build static audit (seven integration points) + Layer B on-device cycle + the en/cz/ru corpus + the quality bar — lives in the **`cluster-prediction-testing`** skill, invoked from `upstream-new-version` Steps 4.5/6. Verified on-device (0 gate refusals, English `thih→this` via transformer-merge, Czech `pravděpodobně` via legacy, both top + confident-autocorrect, FINAL strictly in-set). 4 files edited + `ClusterPredictionDebug.kt` + the new skill, no native change.

- **Software dead keys — diacritic composition (LIVE)** (engine commit `45bba2f80`, layouts commit `bfb0a429c`). A diacritic accent key (`´` acute, `ˇ` caron, `¨` diaeresis, `¯` macron, `˚` ring-above, plus `˙ ¸ ˛ ˘ ˜ ˝`) emitted by a cluster/compass/etc. slot now acts as a **dead key**: press it, then a letter, and they compose into the precomposed character (`ˇ` then `r` → `ř`) — instead of typing the bare accent literally (the old "ˇr" bug). The point is literal entry of accented words when the dictionary doesn't surface them. **KEY DISCOVERY:** FUTO already ships `DeadKeyCombiner` (`event/DeadKeyCombiner.java`), and it's **already first in every layout's combiner chain** (the layout default `combiners = [CombinerKind.DeadKey]`, `Keyboard.kt`; `CombinerKind.DeadKey → DeadKeyCombiner`). It does the full NFC composition via its `sAccentToCombining` table (accent display-form `ACCENT_CARON=U+02C7` etc. → combining mark) + `Normalizer.NFC`. It just **never fired**, because it only triggers on `FLAG_DEAD` events, and AOSP only ever set that flag for *hardware* dead keys (`Event.createDeadEvent`'s own TODO: "if we ever create a software layout with dead keys"). So the whole fix is to turn an accent keypress into a dead event:
  - `DeadKeyCombiner.isDeadAccentCodePoint(cp)` (new public helper): `cp >= 0x80 && Data.sAccentToCombining.indexOfKey(cp) >= 0`. **The `>= 0x80` guard is essential** — `sAccentToCombining` also holds ASCII approximations / pre-L legacy entries (`` ` `` U+0060, `^` U+005E, `~` U+007E, and `ACCENT_HORN='`'`, `ACCENT_DOT_BELOW='.'`, `ACCENT_STROKE='-'`), which have literal uses (backtick/caret/tilde are everywhere in the GNU/Emacs layouts). All the *dedicated* accent display-forms are `>= U+00A8`, so the guard cleanly includes them and excludes the ASCII ones.
  - `InputLogic.onCodeInput` (right after the Ctrl-one-shot intercept): if the event isn't already dead, isn't functional, and `DeadKeyCombiner.isDeadAccentCodePoint(event.mCodePoint)`, **re-dispatch** `onCodeInput(... Event.createDeadEvent(cp, keyCode, null) ...)`. The combiner then buffers it (consumed event, shows `_ˇ` composing feedback via `getCombiningStateFeedback`) and composes with the next letter. The `!event.isDead()` guard terminates the one-level recursion. Composed chars flow through the word composer normally, so **prediction still works** (`ˇ r e k a → řeka`). A dead accent then **space** (or pressed twice) emits the literal accent (the combiner's existing whitespace/same-key rule).
  - **No new code constant, no native change, no combiner-chain change.** 2 engine files: `event/DeadKeyCombiner.java` (the helper), `inputlogic/InputLogic.java` (the re-dispatch + import).
  - **Layout side (commit `bfb0a429c`, the user's kxkb archive, not bundled):** the Czech `3+2` (5r4c, 5r9c) compass + `´lˇ` cluster and the GNU/Emacs (5r13c, 5r15c) apostrophe compass were updated so their diacritic-intended slots emit the *dedicated* accent glyph (so they compose). cz: `upLeft → ¯` (macron), `upRight → ¨` (diaeresis), `downLeft → ˚` (ring/kroužek → ů). gnu: `downLeft → ¯` (macron). ASCII slots that were genuinely literal (`" ' + = % ’`) were left literal — the engine never touches them (the `>= 0x80` guard). Each bumped to **1.3.5** (name + filename). Imported via Custom Layouts; the engine build is what makes them compose.
  - **To make any other slot a dead key:** just emit the dedicated accent display-form glyph (`´ ˇ ¨ ¯ ˚ ˙ ¸ ˛ ˘ ˜ ˝`), NOT the ASCII lookalike — the engine recognises the former and composes; the latter stays literal by design.

- **Expandable suggestions panel — Multiling-style, default-on (LIVE)** (commit `2cfffdc45`). The user wanted Multiling's "swipe a candidate → overlay the keyboard with ALL candidates, paged/scrollable" so they can find a word that isn't in the top 3 instead of deleting and re-entering by slide. **KEY DISCOVERY: FUTO already ships this UI** — `ActionBarWithExpandableCandidates` in `…/uix/ActionBar.kt`: a horizontally-scrollable inline candidate row (`InlineCandidates`, a `LazyRow`) plus a `▲` toggle that animates a `Surface` over the keyboard area (`suggestionsExpansion` 0→1 drives `keyboardOffset`) holding the **full** `wordList` in a lazy `LazyFlowRow`. It was wired only for **CJK** and gated behind a default-off **developer** setting. The path is chosen by `ExpandableSuggestionBarConfiguration(useExpandableUi, addExtraSpaceForFirstEntry)` (`engine/IMEHelper.kt`) passed to `UixManager.setSuggestions`; `useExpandableUi=true` → `ActionBarWithExpandableCandidates`, false → the plain 3-slot `CollapsibleSuggestionsBar` (`SuggestionItems`, always exactly 3). `GeneralIME` (the Latin/Cyrillic path, incl. clusters) already had `expandableExpandableCfg = ExpandableSuggestionBarConfiguration(true, false)`, selected in `onStartInput` iff `getSetting(UseExpandableSuggestionsForGeneralIME)` — a `SettingsKey` (default **false**), exposed only in DevSettings ("Here be dragons", "Use expandable suggestions UI for all languages"). So the whole feature was one setting away. Changes:
  - **Defaulted `UseExpandableSuggestionsForGeneralIME` to `true`** (`GeneralIME.kt`) so every language gets the expandable bar.
  - **Surfaced it as a first-class toggle** in Settings → Typing — `userSettingToggleDataStore(R.string.keyboard_settings_expandable_suggestions, …, UseExpandableSuggestionsForGeneralIME)` in `Typing.kt` (the same menu list as "Show action/suggestions bar"), so it's reachable without developer mode. Still also in DevSettings (same `SettingsKey`, harmless). Strings in `strings-uix.xml`.
  - **Raised the candidate pool from 14 → 128** in `LanguageModelFacilitator.processAndMergeSuggestions`: `SuggestionResults(14→128, …)` (the TreeSet enforces capacity via `pollLast`, so the constructor arg is the real cap) and the dict-list `.take(10)→.take(128)`. The cluster **trie-walk already finds far more in-set words** than 14 (capped at 200 internally); they were just truncated before display. The 3-slot strip (panel off) still renders only 3, and autocorrect uses `suggestionResults.first()`, so nothing else changes; for ordinary typing the dict simply produces fewer than 128 (it's a cap, not a forced count). NB this raises the pool for the **transformer + cluster (`processAndMergeSuggestions`) path**; the pure-legacy `Suggest` path has its own (smaller) `SuggestionResults` if a future need wants it bumped too.
  - Gesture is FUTO's `▲` toggle on the bar + horizontal bar-scroll (not Multiling's swipe-down-on-a-candidate); same outcome. 4 files: `GeneralIME.kt`, `LanguageModelFacilitator.kt`, `Typing.kt`, `strings-uix.xml`.

- **Multiling-style "topBar" — static candidates shown when nothing is typed (LIVE)** (commit `432f70487`). FUTO only shows suggestions after you type; Multiling let each layout define a `topBar` of fixed entries (emoji, snippets, bracket pairs, paste, date stamps) shown in the empty/neutral bar, so you can drop a ❤ or `(…)` without typing. Added as **global default + per-layout override** ("Both"):
  - **Source** — `engine/general/TopBar.kt`: a global `TopBarEntriesSetting` (`stringPreferencesKey`, newline-separated, seeded with `DEFAULT_TOPBAR_ENTRIES` ≈ the user's Multiling list). A layout may set `topBar: List<String>` on the v2 `Keyboard` (`v2keyboard/Keyboard.kt`); when non-empty it **overrides** the global list. `resolveTopBarEntries(layoutTopBar, globalRaw)` picks layout-else-global; GeneralIME resolves the active layout via `LayoutManager.getLayoutOrNull(ctx, SubtypeLocaleUtils.getKeyboardLayoutSetName(Subtypes.getActiveSubtype(ctx)))?.topBar`.
  - **Entry forms** (`parseTopBarEntry`): plain text/emoji → inserted as-is; **`A…B`** (contains `…` U+2026) → inserts `AB` with the caret between (`(…)`→`()`); **`[Paste]`** → `performContextMenuAction(android.R.id.paste)`; **`{{<pattern>`** → current date/time via `SimpleDateFormat` (the label is a preview; re-evaluated at tap via `dateText()`).
  - **Display (combined bar, since `f25c887eb`):** when **not composing a word** (idle / next-word state), `GeneralIME.showPredictionsWithTopBar(predWords)` shows `[next-word predictions…] + [topBar defaults…]` — the defaults appended after any predictions, so both are selectable (defaults wrap after predictions in the expandable panel); no predictions → just the topBar; no topBar → just the predictions; neither → blank. **While composing a word**, `showSuggestionStrip` keeps **pure** correction/cluster candidates (no defaults appended — they'd otherwise pollute the 3-slot correction strip). Both `setNeutralSuggestionStrip()` and `showSuggestionStrip()` route here (the latter only diverts when not composing), which also fixed the **first-cut flashing/blank bar** (the empty state is reached via both overrides + the async prediction update; handling only the neutral override left it inconsistent). Entries are cached as `topBarInfos` (KIND_CORRECTION `SuggestedWordInfo`s) per active layout (`topBarLayoutKey`, reset each `onStartInput` so date previews / setting changes refresh).
  - **Pick:** intercepted in `onEventInternal`'s `EVENT_TYPE_SUGGESTION_PICKED` branch — `handleTopBarPick(info)` matches the picked `SuggestedWordInfo` by `topBarWords.indexOf(...)` and inserts via the raw input connection (`commitText(before,1)+commitText(after,0)` for the caret pair; paste; date), returning `null` (the `when` is assigned to a nullable `InputTransaction`, so null = "handled, nothing more"). Anything else falls through to `inputLogic.onPickSuggestionManually`.
  - **Editor:** Settings → Typing → "Suggestion bar candidates" (`settings/pages/TopBarEditor.kt`, route `topbarEditor` in `SettingsNavigator.kt`, nav item in `Typing.kt`) — a multi-line `OutlinedTextField` bound live to `TopBarEntriesSetting` via `useDataStore`, with syntax help + Reset to default.
  - **Pick (combined-bar safe):** `handleTopBarPick` matches the picked `SuggestedWordInfo` by **identity** against `topBarInfos` (works whether the bar is topBar-only or predictions+topBar); a match inserts via the input connection and returns; anything else falls through to a normal predicted-word commit.
  - Synergises with the expandable suggestions panel (a long topBar is scrollable + expandable over the keyboard). Initial commit `432f70487` — 7 files (2 new): `TopBar.kt`, `Keyboard.kt`, `GeneralIME.kt`, `TopBarEditor.kt`, `SettingsNavigator.kt`, `Typing.kt`, `strings-uix.xml`. Combined-bar follow-up `f25c887eb` (`GeneralIME.kt`).
  - **Next-word prediction context (knowledge, no code):** FUTO's only contextual next-word predictor is the transformer LM, whose single model `ml4_q6_k` does **not** cover Czech/Russian (confirmed in the model manager). For no-transformer languages, next-word prediction falls to the legacy dict path (`Suggest`, `resultsArePredictions = !isComposingWord`) which needs n-gram data — the bundled cs/ru dicts are unigram-only, so it relies entirely on the **user-history** dictionary (learns bigrams from the user's own typing; "Personalised suggestions" on). So next-word prediction for cs/ru is **personalised-only and builds with use — by design, not broken**. The "Next-word suggestions" toggle is hidden whenever Transformer LM is on (`bigram_prediction.visibilityCheck = { !PREF_KEY_USE_TRANSFORMER_LM }`; `mBigramPredictionEnabled = bigramSetting || mTransformerPredictionEnabled`). General (day-one) next-word prediction would need an n-gram-rich dict or a covering transformer model (neither ships).

- **Special Keys docs: HIDE key + FUTO alt pages / return-to-letters (LIVE)** (commit `4b609ba9f`). Documentation-only — two new `CollapsibleSection`s on the Special Keys page (`SpecialKeys.kt`), same `Para`/`Yaml` inline-English style. (1) **Hide keyboard** — the `!icon/action_hide_keyboard|!code/action_hide_keyboard` key (standalone + compass-slot down-flick) plus the general "action on a key" pattern. (2) **Alt pages (extra layers) & return to letters** — documents FUTO's NATIVE alt-page system, which needs **no code**. Knowledge gained this session, worth keeping:
  - A layout declares `altPages: List<List<Row>>` (`Keyboard.kt`) — up to **4** pages (`KeyboardLayoutPage.Alt0..Alt3`, enum in `KeyboardState.kt`); only `$alt0/$alt1/$alt2` template keys exist (`= !code/key_to_alt_0/1/2_layout`, codes `CODE_TO_ALT_0..2 = -16..-18`), so a 4th page needs a hand-written `$alt3` key. Authored exactly like normal rows. The switch keys are dispatched in `KeyboardState.onEvent → switchToAltLayout` as a **sticky tap-toggle** (tap → page; tap the *same* alt key again → base) — **NOT** momentary: the alt keys never reach `onPressKey`/`onReleaseKey`, so there is no chord/slide-to-revert path like the symbol key's `symbolKeyState`+`finalizeChord`.
  - BUT alt pages are flagged `locked=false` in the enum, so `updateAlphabetShiftState` (the unshift-on-type path, run from `onEvent` when `Constants.isLetterCode(code)`, i.e. codepoint ≥ space) calls `toggleShift(false)` → **auto-reverts to base after ONE printable keystroke**. Net default feel: tap to enter, type one char → snaps back; functional keys (Del/arrows) don't trigger it; type nothing → stays until you tap a return key. To make a page **stay across a run** of chars, flip `Alt0..2` to `locked=true` (3-char enum change; self-consistent — also makes `shouldReleaseAllPointers` treat them like Base/ShiftLocked). The user tested and **kept the default** (type-one-and-return); the `locked=true` patch is on the shelf.
  - **Return-to-letters key:** `ABC|!code/key_to_alpha_0_layout` (`CODE_TO_ALPHA_0_LAYOUT = -19` → `switchToAlpha` → `setAlphabetLayout`) jumps to the base alphabet **unconditionally from any page**, so one identical key works on every alt page (label is free text). Do **NOT** use the `$alphabet` template for return — it fires `key_switch_alpha_symbol` → the **symbols** layer, not letters.
  - Alt-page rows are laid out **independently** — they need NOT match the host layout's column count (nothing in `LayoutEngine` ties them), and a page with no bottom row gets a default one auto-added (`getEffectiveRows` backfills number row + shift/delete + a `DefaultBottomRow`). FUTO's **only** stock layout using `altPages` is **IPA** (2 pages — vowels, then implosives/ejectives/clicks — full of `$gap` fillers; in the github submodule `futo-org/futo-keyboard-layouts`, reachable from the sandbox unlike the gitlab submodules). 1 file: `SpecialKeys.kt`.

- **Swipe-space layout switcher — phase 1: layouts column (LIVE)** (commit `796423b8c`). New `SPACEBAR_MODE_SWIPE_LAYOUT_MENU`; swiping the spacebar opens a compact `LayoutSwitcherView` (a `MoreKeysPanel` overlay) listing the current language's layouts, finger-tracked, release-to-switch in one motion; long-press+drag still moves the cursor. Full architecture, the `MoreKeysPanel`-as-overlay + identity-coordinate patterns, and the remaining phases (languages column + recency, then header row + shortcuts) are in the "Swipe-space layout switcher" section above. 8 files incl. the new `LayoutSwitcherView.java`.

- **Swipe-space layout switcher — phase 3: header row + configurable shortcuts (LIVE)** (commit `dfcd1002a`, completes the switcher). Adds a header cell atop each column on a shared top row ("Keyboard UI" → sizing screen, "Keyboards…" → system IME picker, "Custom Layouts" → Custom-layouts editor) and a configurable left column of settings shortcuts (toggled per-shortcut in the Keyboard UI screen). Introduces the **command-cell** type (`!nav/<route>` opens a SettingsActivity navDest, `!ime` opens the IME picker) and the `SwitcherShortcut` catalog (SharedPreferences-backed toggles). Columns now fit-to-width with text clipping. Architecture in the "Swipe-space layout switcher" section above. 4 files: `LayoutSwitcherView.java`, `MainKeyboardView.java`, `Subtypes.kt`, `Typing.kt`.

- **Swipe-space layout switcher — phase 2: languages column + recency (LIVE)** (commit `2e08b1cbf`). Adds the languages column (distinct other languages, each → its last-used layout) and `LayoutRecency` tracking updated from the central `LatinIME` `ActiveSubtype` observer (captures every switch). `LayoutSwitcherView` refactored to render N columns; both columns now order by recency. Architecture + the recency/observer/multi-column details in the "Swipe-space layout switcher" section above. 4 files: `LayoutSwitcherView.java`, `MainKeyboardView.java`, `LatinIME.kt`, `Subtypes.kt`.

- **4D keys (key sliding) + action-bar quick-access + Special Keys page + compact YAML key types (LIVE)** (commits `733df44b6` toggle → `1f7ca2ce0` action icons + live re-apply → `414affd43` at-rest labels + state icon + 4 position sliders → `439d911b2` Special Keys page → `fb7b3bba8` launcher icon + outlined close buttons → `807432777` Custom layouts launcher + close buttons → `0ccc3ab1f` compass + macro types (Phase 4a) → `5f0248ab4` chord type (Phase 4b-1) → `3b59ae18b` compass slot shorthands (Phase 4b-2) → `05388366e` cycle/multitap key (Phase 4c) → `16d9b57a1` cluster / predictive multi-key (Phase 4d)). Short directional slides off a key type one of eight surrounding characters, reusing FUTO's existing flick engine; the `KxkbActions.kt` action-bar plumbing (deep-link into settings sub-pages, the reactive state-aware icon, the `SharedPrefsCacheProvider` action-bar gotcha); the Special Keys reference page; and the compact polymorphic key types `compass` (slide string + per-direction overrides, with `{ mod }`/`{ chord }`/`{ macro }` slot shorthands via a custom `Slot` serializer), `macro` (literal text via `CODE_OUTPUT_TEXT`), `chord` (Emacs-notation key chords via a `\u0000`-sentinel `outputText`), `cycle` (multitap — repeated taps cycle a `taps` set via a `\u0001` sentinel + `performCycle` state machine, window is a settings slider) — all intercepted in `InputLogic.onTextInput` — and `cluster` (predictive multi-key / Multiling `3+2`: a band of N mains on one wide key, tap commits the centre while ALL letters become spatial candidates for the transformer, via sub-rect injection in `ProximityInfo.createNativeProximityInfo`; pure-Java, no `InputLogic` interception, no native change). Full architecture in the "4D keys (key sliding) + action-bar quick-access + Special Keys page" section above. **The locked special-key vocabulary is now complete** (compass / macro / chord / cycle / cluster); `type: flick` stays the low-level escape hatch.

## Maintenance

Whenever this skill is edited, deliver a refreshed zip in the same turn (per `skill-export`: `futo-keyboard-build_YYYY-MM-DD_HH-MM-SS_SHORTDESC.zip`, skill directory at top level, user's timezone Europe/Prague). The user keeps the zip as a portable backup and wants it current; do not wait to be asked.

**Standing rule — after EVERY push AND every pull/rebase, refresh and re-deliver this skill in the same flow (the user does not need to ask).**

- **After each push** (a feature commit landing on `origin/custom`): update this skill to record it — add the new hash to the commit chain in *4D keys… architecture map* + *Implemented features*, bump the commit count in *Customization commits*, fold in any new architecture/key-files notes the commit introduced, and move anything from "planned/NOT built" to "shipped" — then deliver the refreshed zip per `skill-export`. Treat the push as incomplete until the skill is updated and handed over. (The user re-uploads this zip to seed each new chat, so a stale skill loses the just-pushed work.)
- **After each pull/rebase:** a "Sync to a new upstream version" rebase changes the base tag, the `base_code`, and the commit hashes in the `custom` stack, and may shift file/line references the architecture notes depend on. Once it has landed (custom force-pushed, build verified), update the affected sections — at minimum the `target_tag`/version examples in *Project identity* + *Versioning* + the build block, the commit count and any moved commit hashes/line numbers in *Customization commits* / *Implemented features* / *Live look/size knobs*, and any rebase conflict learnings worth recording in *Sync to a new upstream version* — then deliver the refreshed zip. Treat the rebase as incomplete until the skill is updated and handed over.

## Related skills

- `shell-block-formatting` — the cyan-echo / red-stderr (`$'...'`) / pause-gate conventions every block here follows.
- `patch-naming` — naming for feature `.patch` files delivered for this fork.
- `multiling-futo-conversion` — converts the user's Multiling O layouts into FUTO v2 YAML layouts for this fork (compass/chord/cluster mappings, sym→alt0 / altGr→alt1, the GNU-layout output). Produces the layout files; this skill builds the engine they run on.
- `appmanager-fork`, `jami-build`, `arcanechat-fork` — the sibling commit/push/rebase Android forks this one mirrors (AppManager closest: native Android/Gradle, `custom` branch, force-with-lease; Jami closest on versioning: env-injected `+N` from a local counter).
- `skill-export` — zip convention for delivering this skill.
- `sandbox-environment` — why this skill lives in `/mnt/skills/user/` and is writable.
