# shiroikuma-futokxkb

Fork of [FUTO Keyboard](https://github.com/futo-org/futo-keyboard-android) — package `shiroikuma.futokxkb`, label "白い熊 FUTO kxkb", installable side-by-side with the official FUTO Keyboard from F-Droid.

The `custom` branch carries ~142 customisation commits (the two base commits plus the feature commits) over the FUTO `0.1.29` release tag, rebased onto each new upstream release.

## Skills

Four skills in `.claude/skills/` document this project's workflow, conventions, and architecture in dense reference prose. **Read the relevant SKILL.md before starting work** — they encode hard-won detail (build env quirks, the kxkb commit stack's architecture, gotchas hit during development) that you won't find by reading the source. Each is named by trigger; the descriptions are tuned for automatic invocation.

- **`futo-keyboard-build`** (primary, ~664 lines) — project identity, branch/remote model, commits 1–2 documented in full, build + sign + deploy pipeline, versioning (local counter + env injection), keystore convention, per-change delivery workflow, upstream-rebase procedure, and the **Implemented features** reference covering every kxkb feature commit in dense paragraphs. Read this any time the user asks to apply a change, rebuild, sync to a new FUTO release, or work on anything in the kxkb stack.
- **`upstream-new-version`** (orchestration over the above) — the `/upstream-new-version` flow: check whether upstream has a newer release — **stable or release-candidate (`-rcN`)** — and if so fast-forward `master`, rebase the `custom` stack onto the new tag (auto-reconcile small conflicts; **stop and plan with the user** when conflicts are significant), then build the new APK via `futo-keyboard-build`. Defers all pushing until the user says "Push". Read this when the user wants to sync/update to a new FUTO release or rc.
- **`multiling-futo-conversion`** (companion, ~271 lines) — porting the user's Multiling O `kxkb` layouts to FUTO v2 YAML. Deliverable is a `.yaml` for FUTO's Dev/Custom Layouts import, NOT fork code. Read this any time a Multiling layout file (with `[4D:…]`, `[MC:…]`, `[3+2:…]`, `[SYM]`, `[ALTGR]`, `[Lock]` codes) is the input.
- **`cluster-prediction-testing`** (verification, plugs into the sync flow) — test & preserve CLUSTER-layout prediction quality across upstream syncs. Two layers: a **pre-build static audit** (`audit.sh` — a one-command PASS/REVIEW/FAIL tripwire that diffs the prediction pipeline old-base→new-base and checks the seven integration points our hooks depend on) and an **on-device staged tracer** — `ClusterPredictionDebug`, a default-off dev toggle (Settings → Developer → "Cluster prediction debug") that writes a per-stage candidate trace to `cluster_pred_debug.log`, pulled via `adb pull /sdcard/Android/data/shiroikuma.futokxkb/files/cluster_pred_debug.log`. Read this whenever cluster prediction must be verified/has regressed, or you touch the cluster pipeline. `upstream-new-version` Steps 4.5 + 6 invoke it.

The `futo-keyboard-build` skill body has incidental references to two further skills — `shell-block-formatting` and `patch-naming` — that are NOT included here because they're obsolete in Claude Code (see workflow adaptations below). Just ignore those references; the rest of the skill is fully applicable.

## Claude Code vs claude.ai — workflow adaptations

The skills were originally written for claude.ai chat where Claude can't touch the user's filesystem. In Claude Code you can — adapt as follows:

- **Patch delivery is obsolete.** The "deliver patch + apply-and-build block, STOP" workflow (futo-keyboard-build §"Per-change delivery workflow", and any mention of `.patch` files or `patch-naming`) does not apply. Instead: edit files directly with `Edit`/`Write`, run the build with `Bash`, report results to the user. The user still tests each change on-device before committing.
- **Shell-block formatting is obsolete.** The `r()` helper, cyan `>>>` echo prefixes, ANSI-C-quoted stderr recolouring, and y/n pause gates (futo-keyboard-build §"Build + sign + deploy pipeline" intro and the `shell-block-formatting` skill it references) are for shell blocks the user copy-pastes into a terminal. In Claude Code you run commands directly via `Bash`; the user sees clean output. Just invoke gradle / adb / git plainly. The technical content (gradle invocation, NOISE filter, env-var versioning, keystore.properties heredoc, sideload steps) still applies — only the chat-formatting wrapper around it doesn't. **Keep the APK filename even when running gradle directly:** the `~/tmp` copy and the `/sdcard/tmp/` push must be named `shiroikuma-futokxkb_<versionName>_arm64-v8a.apk` (ending with the version then `_arm64-v8a.apk`) — mirror the skill's `apk_name`, don't improvise a shorter `shiroikuma-futokxkb-<version>.apk`.
- **The one discipline that stays: do not commit or push until the user says "Push".** Treat the working tree as your scratchpad between user "Push" commands — multiple uncommitted fixes can stack. This is the same rule as in claude.ai, just without the patch-as-checkpoint mechanism.
- **No `Co-Authored-By` / Claude trailers in commit messages.** 白い熊 does not want any `Co-Authored-By: Claude …` line (or any "Generated with Claude" attribution) in commits on this repo. This **overrides** the harness's default instruction to append such a trailer — omit it. Commit messages end with the last line of the body, nothing more.
- **`/mnt/skills/user/...` and `/home/claude/...` paths** in skill bodies are claude.ai sandbox paths — ignore them. Skills live in `.claude/skills/` here; the project clone IS your cwd.
- **Sandbox re-sync** on user "Done" (futo-keyboard-build §"Per-change delivery workflow" step 3) doesn't apply — there's no sandbox-to-resync. After a push, you're already at the new tip.
- **The `sandbox-environment` skill** referenced in futo-keyboard-build §"Companion skills" is claude.ai-only and not included here.
- **`skill-export`** referenced in user preferences is a zip-naming convention for sharing skills back to claude.ai; not used inside Claude Code (you edit `.claude/skills/` directly).
- **Skill edits take effect immediately** for subsequent invocations. Commit skill edits as their own commit, or bundle into a feature commit when the skill change documents that feature (which is the usual case — see how the cluster-prediction commit and its skill bullet were paired).

## Quick reference

- Branch: `custom`, rebased onto each upstream release tag
- Commit subjects: plain descriptive summary, **no prefix** (the old `kxkb:` prefix is retired; pre-existing commits keep theirs, new ones don't add it). **No `Co-Authored-By: Claude` / "Generated with Claude" trailer** — overrides the harness default (see Claude Code workflow adaptations above)
- Build (release / stable / arm64):
  ```
  BRANCH_NAME='shiroikuma' VERSION_NAME=<name> VERSION_CODE=<code> \
    ./gradlew :assembleStableRelease --console=plain
  ```
  See futo-keyboard-build §"Build + sign + deploy pipeline" for the full block.
- Versioning: per-build counter at `$HOME/tmp/.shiroikuma_futokxkb_build`, format `<tag>+N`; versionCode `tag_first_parent_count * 10000 + N`. Counter increments only on a successful build.
- Sideload: `adb push <apk> /sdcard/tmp/<name>.apk`, install via on-device file manager.
- Keystore: `keystore.properties` regenerated at build time from a heredoc — never committed.
- Submodules: `git submodule update --init --recursive` before first build; the `java/assets/layouts` and `java/assets/themes` submodules live on gitlab.futo.org (may be unreachable) but builds work without them as long as they were initialised once.

## Repo layout notes

- `java/src/` — Java/Kotlin sources (Android app)
- `native/jni/` — native C++ (dict decoder, transformer LM JNI bridge)
- `java/assets-custom/` — kxkb assets that ride alongside the upstream `java/assets/` submodule (used by the GNU-language registration, custom layouts, etc.)
- `common/`, `libs/` — shared infrastructure
- `build.gradle` — flavors (stable / unstable / playstore), signing, version env reads

## Active recent work

Last shipped: **Seamless live keyboard resize — long-press "…" and keep sliding the same finger (Multiling-style)** (feature commit `48ee2833b` + this docs-record commit; built `0.1.29+35`, versionCode `116900035`; pushed `origin/custom`; iterated on-device `+30`…`+35` — 白い熊's "Push"). **Keyboard feature** (it IS in the `futo-keyboard-build` "Implemented features" reference). **Replaces the stock dim+handles+Done/Reset resize overlay** (the `+19`…`+22` `LiveResizeAction`/`KxkbResizer` interaction — now superseded/inert, left in place for a follow-up cleanup) with the original Multiling vision: **long-press the `…` (more actions) button and KEEP sliding the SAME finger** → resizes **live**; **releasing it saves + returns to normal**. No buttons, no mode to dismiss. **Why the `…` button drives it:** for the same finger to flow from long-press into the slide, ONE gesture handler must own that pointer from the down — a separate overlay can't grab an already-held finger. So the primary finger is handled by the `…` button's own raw gesture (**`MoreActionsSeamlessButton`** in `ActionBar.kt`): a **tap** still opens the more-actions window; a **long-press** (`awaitLongPressOrCancellation` — the canonical primitive that fires reliably inside the action-bar `LazyRow`, after my manual `withTimeout`/`awaitFirstDown(requireUnconsumed=true)` version silently never fired) **buzzes** (`LocalHapticFeedback`), then that finger drives the keyboard via new manager methods `beginLiveResize` / `liveResizeMove(dx,dy)` / `endLiveResize` (`Action.kt` → `UixActionKeyboardManager` → `resizers`). **Zones / mapping** (`KeyboardResizers.applySeamless`, reusing `applyKxkbDrag` so the snap-to-dock dead-zone still applies): **top-left** (the `…` finger) vertical = height, horizontal = width (drag left = wider); **bottom-right** (a 2nd finger) vertical = padding/lift, horizontal = width (drag right = wider); **centre** (a 2nd finger) horizontal = split. **Multi-finger** — several at once, each extra finger applies live and buzzes on grab. **`KxkbSeamlessOverlay`** (always composed; shown while active): a faint dim that blocks typing + consumes touches, **three same-colour blue dots** — top-left **follows the primary finger** (accumulated `primaryDelta`, since the overlay can't see the `…`-owned finger), bottom-right deep in the corner, centre = split; a grabbed secondary dot follows its finger; catches the extra fingers by zone (`secondaryResizeTarget`), never double-counting the primary. 5 files (`Action.kt`, `ActionBar.kt`, `UixManager.kt`, `KeyboardResizers.kt`, `ResizerRect.kt`), no native change. **Iteration history:** `+30` multi-finger stock overlay + top-left-corner long-press hot-zone (the hot-zone never fired — `waitForUpOrCancellation` treats the underlying key/action *consuming* the touch as a cancellation); `+31` removed Done/Reset + auto-close; `+32` moved the trigger to the `…` button via `altPressImpl` (worked — but opened the *stock* overlay, NOT what 白い熊 wanted); `+33` the full seamless rewrite (custom `…` gesture + `KxkbSeamlessOverlay`, stock overlay unrendered); `+34` the `…` long-press wouldn't fire until I switched to `awaitLongPressOrCancellation` + added the activation haptic; `+35` same-colour dots, top-left dot follows the finger, bottom-right deep in the corner, and horizontal = width on top-left/bottom-right.

Prior: **Unified / grouped / phonetically-sorted layout-selection lists with a settable rounded-box look + keyboard-font option** (feature commit `4eaa26048` + this docs-record commit; built `0.1.29+29`, versionCode `116900029`; pushed `origin/custom`; iterated on-device through `+23`…`+29` — 白い熊's "Push"). **Keyboard feature** (it IS in the `futo-keyboard-build` "Implemented features" reference). Both layout-selection areas — Settings → **Custom layouts** (`DevLayoutEditor`) and the **Keyboard editor** (`KeyboardEditorScreen`) — now render through ONE shared **`LayoutSelectionList`** composable (in `EditorLabelFont.kt`), so they look identical and are styled from one screen (Settings → Keyboard UI → **"Layout list font"**, the renamed "Keyboard editor font"). **Grouping/order:** layouts grouped under **underlined language section headers** (the autonym via `SubtypeLocaleUtils.getSubtypeLanguageDisplayName`; the `zxx` locale labelled **"GNU"**), sections ordered **phonetically by the romanized autonym** (`phoneticSortKey`: a small map `ja→nihongo`, `ru→russkij`, `zxx→gnu`, plus `java.text.Normalizer` NFD diacritic-stripping for Latin names) so e.g. 日本語 sorts under **N** and Русский under **R** (→ Čeština, English, GNU, 日本語, Русский), **layouts within a section alphabetical by name**; **no bracketed `[i]` index / no "Custom Layout N" subtitle** (the editor's old `[$i]` removed). **Each name sits in a rounded box/pill** (`LayoutRow`), all **same width**, built from a **bare `Box` + `Modifier.clickable`** — NOT a Material `Button`/`Surface`, both of which enforce a 40–48 dp minimum touch-target height that kept earlier rows tall regardless of padding (the bug behind "padding = 0 still huge" across `+24`/`+25`; even a `Surface(onClick)` keeps the 48 dp min-interactive-size, so `+26` switched to `Box`+`clickable`); the editor's active layout is a solid `primary` pill. **All settable on the "Layout list font" screen** (new `SettingsKey`s, reset covers them): font **family/size/weight/colour** (size default bumped 12→16) **+ a "Keyboard font (theme)" family option** that uses the active theme's custom `Typeface` (`LocalKeyboardScheme.current.extended.advancedThemeOptions.font`, the same `withCustomFont` the keyboard uses, fallback to default if none — addresses "we only have default font options"); **"Padding between layouts"** (default 2 dp, the ONLY inter-row gap now that the min-height is gone); and a **"Layout box"** group — **Indent** (default 64 dp on top of the list's 16 dp margin = the previous "massive" indent, boxes land ~80 dp from the edge; range 0–240), **Box width** (same for all, **80–600 dp**, default 280), **Roundedness** (10), **Inner padding H/V** (14/4), **Box background** (RGBA, subtle grey default), **Box border colour** (RGBA, **alpha 0 / off by default**) and **Border width** (1.5). A live grouped preview (underlined "English" + a filled + an unfilled box) sits on the screen. 4 files (`EditorLabelFont.kt`, `KeyboardEditorScreen.kt`, `DevLayoutEditor.kt`, `Typing.kt`), no native change. **Iteration history:** `+23` unified + grouped + phonetic + GNU-rename; `+24`/`+25` left-align + indent but padding still looked huge (Material min-heights); `+26` `Box`+`clickable` killed the min-height; `+27` phonetic sort; `+28` the settable pill box + keyboard-font option; `+29` 600 max box width + the Indent restored as a setting defaulting to the previous massive value.

Prior: **Live on-keyboard resize — tap the "Resize" action to drag hot-points on the live keyboard (height / lift / width / progressive split)** (feature commit `932ce016d` + this docs-record commit; built `0.1.29+22`, versionCode `116900022`; pushed `origin/custom`; installed + tested on-device — 白い熊's "Push"). **Keyboard feature** (it IS in the `futo-keyboard-build` "Implemented features" reference). Repurposes the pinned **`LiveResizeAction`** (aspect-ratio icon; label "Keyboard UI" → "Resize") so a tap overlays draggable hot-points on the live keyboard instead of deep-linking to the Settings → Keyboard UI page (still reachable there). The overlay **reuses FUTO's resizer chrome** (dimmed keyboard, handle circles that light `primary` on grab / red `error` at a limit) via a new target-aware **`KxkbResizerRect`** (in `ResizerRect.kt`, modelled on FUTO's `ResizerRect` but the drag callback is told WHICH `CurrentDraggingTarget` is held), but drives **our own per-(geometry) `SavedKeyboardSizingSettings`** — the SAME fields the Keyboard UI sliders write — so it resizes/splits exactly like that page, **applied live**; **"Done"** dismisses, **"Reset"** restores the dimensions captured when the overlay opened (an **undo of this session**, snapshotted in `displayKxkbResizer()`, NOT factory defaults). **Hot-points** (`KeyboardResizers.applyKxkbDrag`, reading `latinIME.size.value` / view width fresh each delta): **Top** → `heightMultiplier` (drag up = taller); **Bottom** → `bottomLiftDp` (drag up = lift off the bottom); **Left/Right** → `widthFraction` (symmetric narrow/widen, inward = narrower); **Center** → **progressive split** (rightward opens the centre gap and switches Regular→Split on the first rightward move; leftward closes it and, past the minimum gap, switches Split→Regular; one-handed/floating = no-op). **Trigger plumbing:** new `KeyboardManagerForAction.showLiveResizer()` (`Action.kt`) → `UixActionKeyboardManager` (`UixManager.kt`) → `resizers.displayKxkbResizer()` (+ `closeActionWindow()`); the overlay renders beside FUTO's resizer in `NonFloatingKeyboardWindow`, gated on `kxkbResizing`, skipped for floating. **Snap-to-dock dead zone (the docking bug that cost `+20`/`+21`):** ANY side margin (`widthFraction < 1`) or bottom lift (`> 0`) flips the keyboard into see-through / floating insets (`LatinIME.onComputeInsets` `seeThrough = bottomLift>0 || sideOff>0 || split`), so the app draws BEHIND it instead of reserving its height — and the resizer made it trivial to leave a hair of margin, **un-docking the keyboard over Termux** (`+20`). Fixed in **`KeyboardSizingCalculator.calculate()`**: a **read-time coercion** snaps a near-docked blob fully to docked (`widthFraction ≥ 0.98 → 1.0`, `bottomLiftDp ≤ 5dp → 0`) before computing the size, so a slight margin docks normally (app pushed up, no gap) AND an already-nudged stored blob re-docks on next open; deliberate larger narrow/lift stay untouched and still show the app behind the margins (split keeps its real centre-gap see-through). **The dead zone is render/insets-only, NOT in the per-drag handle path:** the first attempt (`+21`) snapped each incremental delta too, which **pinned `widthFraction` at 1.0 so the Left/Right dots highlighted but never narrowed** (each small delta computed ~0.99 ≥ 0.98 → re-snapped to 1.0, never accumulating past the threshold); `+22` moved the snap out of `applyKxkbDrag` so the handle accumulates freely (a ~2% initial dead spot before narrowing begins is the cost). 7 files (`ResizerRect.kt`, `KeyboardResizers.kt`, `Action.kt`, `UixManager.kt`, `KxkbActions.kt`, `strings-uix.xml`, `KeyboardSizingCalculator.kt`), no native change. **Misdiagnosis aside:** an earlier read of the symptom as a 2dp side-padding gap on the key rows (and a `paddingDp` left/right → 0 edit) was reverted once 白い熊 clarified the real issue was docking — the side inset was never the cause and is NOT in this commit. **History:** `+19` (initial feature) + `+20` (Reset = restore snapshot) were folded into this single commit; `+20` exposed the docking bug, `+21` (dead-zone) broke Left/Right dragging, `+22` is the shipped state.

Prior: **Line-start literal space — at a line start, space stays a space and Tab engages the first prediction** (feature commit + this docs-record commit; built `0.1.29+18`, versionCode `116900018`; pushed `origin/custom`; installed + tested on-device — 白い熊's "Push"). **Keyboard feature** (recorded inside the `+17` Space/Tab bullet in the `futo-keyboard-build` "Implemented features" reference). Refinement of the `+17` Space/Tab candidate selection (below), fixing its one usability hole: space-commits-prediction made a plain space impossible on a fresh line. In the idle/next-word state **at a line start** (or the field start, or after only leading whitespace) the candidate selection now starts **unmarked** (sentinel `selectedCandidate = -1`): **space types a literal space** and **Tab engages the FIRST prediction** (the sentinel makes the first Tab land on candidate 0 instead of advancing to the second), which space then commits; mid-sentence behaviour unchanged (first prediction marked, space commits). The accent-box marker doubles as the mode indicator — boxed candidate → space commits it; no box → space is a space. **Line-start detection** (`GeneralIME.atLineStart()`): the text before the cursor with trailing spaces/tabs skipped is empty or ends with a newline (so multiple literal spaces chain), OR the last input was Enter via the new accessor `InputLogic.wasLastInputEnter()` — the editor-independent force-auto-caps signal, covering the Emacs Android port which hides the trailing newline from the text it reports. **`onPickSuggestionManually` now records the pick's last code point into `mLastInputCodePoint`** so a committed word (tap or Tab+space) ends the fresh-line state — incidentally fixing the latent force-auto-caps quirk where a word picked right after Enter still capitalized the next letter. Known imperfection (Emacs only): after Enter + one literal space a second space may commit a prediction (the hidden newline defeats the text check and the first space consumed the Enter signal). 2 files (`GeneralIME.kt`, `InputLogic.java`), no native change. **Build note:** the `~/tmp` per-build version counter had been cleaned away with `~/tmp`; restored it to `0.1.29 17` before building so `+18`/the versionCode stayed monotonic over the installed `+17`.

Prior: **Space / Tab candidate selection — Space commits the selected candidate, Tab moves the selection** (feature commit `d91cf534d` + this docs-record commit; built `0.1.29+17`, versionCode `116900017`; pushed `origin/custom` on top of the `+15` space-accept stack; installed + verified on-device). **Keyboard feature** (it IS in the `futo-keyboard-build` "Implemented features" reference). **Generalises the earlier `+15` "space accepts the first next-word prediction"** (commit `b39c25383`) into selectable candidate navigation under the **same setting** (`SpaceCommitsNextWordPrediction`, data key `kxkb_space_commits_next_word` kept for back-compat; UI title renamed "Space / Tab candidate selection", default **true**). When the bar shows real candidates: **SPACE commits the currently-selected candidate** (the first by default) and **TAB moves the selection to the next** (wraps) — so space / Tab+space / Tab+Tab+space commit the 1st / 2nd / 3rd; the selection is marked in the bar with a rounded accent box (`primary` @ 28% behind the word). **By state:** *next-word (idle)* — the first prediction is marked from the start, space commits it (the `+15` behaviour); *while composing* — the default selection (0) is **unmarked** and space runs the **normal autocorrect-on-space path unchanged** (so autocorrect-off still keeps the literal), and pressing Tab marks an alternate (e.g. the literal typed word, re-inserted as candidate 2) which space then commits. **Static topBar defaults are never navigated/committed** — they sit at the tail of `mSuggestedWords` and are filtered out of the selectable set by reference identity, so a bar showing only the pinned defaults still types a plain space/Tab. **(This is the fix for the regression in the never-committed `+16` experiment, where committing topBar defaults on space blocked all normal spaces — `+16` was rolled back before this.)** **Tab only intercepts with ≥2 selectable candidates** (with 0–1, e.g. GNU layout / no prediction, Tab passes through as a real `KEYCODE_TAB`, so Emacs Tab-completion is untouched — 白い熊 confirmed Emacs uses the GNU layout, no predictions). **Implementation:** `GeneralIME` holds `selectedCandidate` (reset to 0 on every input-driven suggestion update); the `onEventInternal` keypress branch handles SPACE/TAB and commits via `onPickSuggestionManually` (exactly like a tap). The **selectable list** = `displayCandidateList(words)` (extracted from `ActionBar.kt` so engine + UI index the SAME visual order — predictions with the literal typed word re-inserted as the 2nd entry) **minus** the topBar entries (by identity); `selectable[i]` is at displayed/wordList index `i`. The marker is carried in `SuggestedWords.mHighlightedCandidate` (a wordList index) and drawn by `CandidateItem` (matched by **identity**, so it survives the typed-word reordering); a Tab re-renders via a highlight-only **copy** of the current `words` (no re-prediction). **Gated on the expandable suggestion bar** (the default), whose order it indexes. 3 files (`GeneralIME.kt`, `ActionBar.kt`, `strings-uix.xml`), no native change. **History note:** `+16` (an interim build that also committed topBar defaults on space) was built and tested but **rolled back uncommitted** — only `+15` (space-accepts-first-prediction) and this `+17` (Space/Tab selection) are in the commit history.

Prior: **Per-app layout memory — restore each app's last-used layout on focus** (feature commit `c7e6b9aa6` + docs/convention commit `ca05f0e57` + a docs-record commit; built `0.1.29+14`, versionCode `116900014`; force-pushed `origin/custom` `84034b78d`→`ca05f0e57` then this commit on top; installed + verified on-device — GNU layout in Termux ↔ an English layout in the Emacs Android port, each restored on focus). **Keyboard feature** (it IS in the `futo-keyboard-build` "Implemented features" reference). Remembers the (language·layout) subtype last used in each app and switches to it automatically when that app's text field is focused; **last-used-wins** (any layout switch made while an app is focused (re)binds that app) and **on by default** (opt out under Settings → Keyboard & Typing → Keyboard → "Remember layout per app", plus a "Forget remembered layouts" action). Data (`Subtypes.kt`): `PerAppLayoutEnabled` (default true) + `PerAppLayoutMap` (the `package⇥subtype` bindings, tab-separated/newline-joined like `LayoutRecency`), with `getAppLayout`/`recordAppLayout`/`clearAppLayouts`. Behaviour (`LatinIME.kt`): **restore** in `onStartInput` (`maybeApplyPerAppLayout` captures `EditorInfo.packageName` into a `@Volatile currentInputPackage` — null for our own package so the settings/editor never bind — and, if on and the app's binding is still enabled and differs from the active subtype, writes `ActiveSubtype` so the existing observer does the live switch); **record** in that `ActiveSubtype` observer (`recordAppLayout(currentInputPackage, nowActive)` on every switch with an app focused, covering the swipe switcher / "Next layout" / system picker / editor Apply uniformly); **clear** the package in `onFinishInput`. **GOTCHA that cost a broken build (`0.1.29+13`):** the restore must write `ActiveSubtype` OFF the main thread (`lifecycleScope.launch { setSetting(ActiveSubtype.key, remembered) }`), NOT the blocking `switchToSubtypeString`/`setSettingBlocking` (`runBlocking`) — while off-by-default the blocking write only ran after init and was harmless, but once default-on it fired on the very first `onStartInput` at cold start and **hung the IME main thread, so the keyboard never drew in any app** (process alive, no crash/ANR — the tell of a hung UI thread); `+14` fixed it with the async write. 4 files (`Subtypes.kt`, `LatinIME.kt`, `Typing.kt`, `strings-uix.xml`), no native change. **Also this session (convention):** 白い熊 asked for NO `Co-Authored-By: Claude` / "Generated with Claude" trailer in commits or PR bodies — recorded in this CLAUDE.md (above) + the `futo-keyboard-build` skill push step + the `no-claude-coauthor-trailer` memory, and the existing stack was retroactively scrubbed of its ~79 trailers via `git filter-branch` over `0.1.29..HEAD` (trees verified byte-identical) then force-pushed. (This rewrite changed every kxkb commit hash — the point-in-time hashes in older "Active recent work"/"Implemented features" entries below are now historical, as they already are after any rebase.)

Prior: **App icon — Minchō Bold 不倒 wordmark (font swap over the black/yellow 不倒 mark)** (commit `1e162acad`, built `0.1.29+10`, versionCode `116900010`; pushed `origin/custom` `42f6f77bd`→`1e162acad`; installed + approved on-device). App-identity refinement only — **no keyboard/native change**. Swaps the **不倒 wordmark font from Gothic (Noto Sans CJK JP Black) to Minchō Bold (Noto Serif CJK JP Bold)**; everything else in the mark is unchanged from the prior icon (below): black (`#000000`) / yellow (`#FFFF00`), solid kanji over the traced-key block, foreground at **70%**, label and adaptive background untouched. Regenerated the 5 foreground PNGs (`drawable-{m,h,xh,xxh,xxxh}dpi/ic_launcher_foreground.png`), the 10 legacy `ic_launcher`/`ic_launcher_round` mipmaps (5 densities, square + circle-masked) and the 512 px `ic_launcher-playstore.png` — **16 files**. Render via the throwaway `~/tmp/render_mincho_bold.py` (PIL, `NotoSerifCJK-Bold.ttc`), not committed.

Prior: **App icon + label — black/yellow 不倒 launcher mark, label "白い熊 不倒 kxkb"** (commit `39955f1e4`, built `0.1.29+9`, versionCode `116900009`; pushed `origin/custom` `935f019b1`→`39955f1e4`; iterated on-device through several previews this session). App-identity change only — **no keyboard/native change**, so it is NOT in the `futo-keyboard-build` "Implemented features" reference. Replaces the FUTO-derived launcher icon with a **black (`#000000`) / yellow (`#FFFF00`)** mark: solid **Gothic** (Noto Sans CJK JP Black) **不倒** wordmark on top (replacing "FUTO"; 不倒 = "does not fall", the daruma/indomitable sense), over a **traced (yellow-outline) keyboard** on the bottom — the same key block as FUTO's icon (two rows of five keys + a centred spacebar), restyled to outlines — the whole foreground sized to **70%** of the icon. Adaptive **background colour → `#000000`** (`ic_launcher_background.xml`). The **foreground was switched from the FUTO vector to raster PNGs** (`drawable-{m,h,xh,xxh,xxxh}dpi/ic_launcher_foreground.png`; old `drawable/ic_launcher_foreground.xml` deleted) — raster deliberately, because faithfully reproducing the approved render as a vector would require stroking the CJK glyph contours, and Noto CJK Black glyphs are built from **overlapping components** whose internal seams a vector stroke would expose (the rasterised fill unions them cleanly). The foreground is **yellow art on transparency** (traced-key interiors and the space between keys are see-through to the black background); the adaptive `<monochrome>` themed-icon entry reuses the same art. Legacy `ic_launcher`/`ic_launcher_round` mipmaps (5 densities, square + circle-masked) and the 512 px `ic_launcher-playstore.png` regenerated; **app label `english_ime_name` "白い熊 FUTO kxkb" → "白い熊 不倒 kxkb"** (`strings-appname.xml`). 白い熊 iterated the design live (Gothic-solid → Minchō-solid → Minchō-traced → back to Gothic-solid; keys traced → filled → traced; size 75% → 70%), settling on **70% Gothic-solid 不倒 + traced keys** for that ship — then in the next session re-rendered the wordmark in **Minchō Bold** (see "Last shipped" above). Assets are reproducible via throwaway PIL/`fontTools` scripts kept in `~/tmp` (`gen_kxkb_assets.py`, `render_kxkb_icon.py`), not committed. 19 files (label + background colour + deleted vector + 5 new foreground PNGs + 10 legacy mipmaps + the Play-Store PNG).

Prior: **Keyboard editor — per-layout suggestion-bar candidates editor, preselect the active layout on entry, bottom Close button, configurable label font** (commits `af18f1610` candidates + `e048304c8` close button + `8a2adf4b7` preselect + `e5fbc77ad` label font + `b08f9d45c` skill doc; built `0.1.29+5`, versionCode `116900005`; pushed `origin/custom` `6a30697bb`→`b08f9d45c`; on-device test in progress). Four additions to the visual keyboard editor (Settings → Keyboard UI → "Keyboard editor"), no native change. **(1) Per-layout "Suggestion bar candidates" editor** — a section on the editor's main screen to edit a layout's own `topBar` list (the static default-prediction candidates shown when nothing is typed). The `Keyboard.topBar` field + YAML emitter already round-tripped it, but there was no UI — the only way to set a per-layout list was hand-authoring the YAML. `KeyboardEditorSession.setTopBar(entries)` writes it into the working DRAFT (dirty) so it persists only on Apply/Export like every other edit; an empty list stores `[]` (not `[""]`) so `resolveTopBarEntries` falls back to the global list. The box is **pre-filled with the effective list** (this layout's own entries if any, else the global "Suggestion bar candidates") for in-place editing; pre-fill is display-only — the model is written (forking from global) ONLY on an actual edit (`onValueChange`), so an untouched layout keeps tracking the global list, and clearing the box stores `[]` and tracks global again. Same entry syntax as the global `TopBarEditor` (plain text/emoji, `A…B` caret-pairs, `[Paste]`, `{{date`). 白い熊 corrected two things mid-build: editing must NOT auto-apply to the live/saved layout (it doesn't — draft only, Apply/Export persists), and the box must pre-fill with the global list for editability. **(2) Preselect the active layout** — on a FRESH entry into the editor it now loads the custom layout currently active in the keyboard (the one you entered from) instead of whatever was last edited; the active subtype's `KeyboardLayoutSet` name (`custom<N>` via `SubtypeLocaleUtils.getKeyboardLayoutSetName`) maps to the custom-layout index. Gated by a `rememberSaveable` flag so it fires only on a new nav back-stack entry, NOT on a pop-back from a key-edit/export sub-screen — so it never clobbers in-progress edits; only switches when the active layout is a custom layout that exists and isn't already selected. **(3) Bottom "Close Keyboard editor" button** — a third close button at the very bottom (under the candidates editor), mirroring the top + above-preview ones, reachable without scrolling back up. **(4) Configurable font for the editor's layout-name labels** — new Settings → Keyboard UI → "Keyboard editor font" screen (`EditorLabelFont.kt`) controlling family (Monospace/Default/Sans-serif/Serif), size, weight, and an optional colour override (toggle + RGBA; off = inherit theme), with a live preview + reset. One `editorLabelStyle()` composable resolves five new `SettingsKey`s and backs the layout-name labels EVERYWHERE in the editor (the layout list, Rows/alt-page lists, append-from-another-layout list, Export + alt-page-preview + per-key edit screens — replacing each screen's hardcoded `mono` style). Defaults reproduce the old look exactly (Monospace, 12 sp, normal, theme colour), so nothing changes until a knob is moved; where a label passes an explicit muted `color` (e.g. "(building preview…)") that stays muted even with the override on. 8 files (1 new `EditorLabelFont.kt`, + `SettingsNavigator.kt` route, `Typing.kt` nav item, the 4 editor screens' label-style, `KeyboardEditorSession.kt`). **Also (commit `b08f9d45c`, docs):** a `futo-keyboard-build` skill note that in Claude Code (gradle/adb run directly, no pasted-shell-block `read` gates) you must ALWAYS ask via `AskUserQuestion` whether to `adb push` after every successful build — a standing rule (the `ask-before-adb-push` memory), exception only for an explicit same-turn "build and push it".

Prior: **Sync to FUTO `0.1.29` (stable)** — rolled the `custom` stack forward off `0.1.29-rc1` onto the stable `0.1.29` release (`master` ff'd `8c85821e4`→`8ae14c400`; `custom` rebased onto the `0.1.29` tag, tip `2c911e286`; built `0.1.29+1`, versionCode `116900001`; static audit CLEAN). The rc1→stable delta is tiny — **4 upstream commits**, almost all additive: upstream threaded a new **`swipeLetter`/`swipeCodeOverride`** field through the v2keyboard model (`BaseKey`, `KeyData`, `Key`, `LayoutEngine`) to fix an AZERTY swipe bug, plus de-"alpha"'d strings and reshuffled the default action categories. The 124-commit replay produced **6 conflicts, all "not huge"/mechanical**, resolved in place: (1) **`Registry.kt` ×3** — upstream's stable build simplified `DefaultActionSettings.PinnedKey` to just `listOf(VoiceInputAction)` and moved `MemoryDebugAction` into `Favorites` for debug builds; took upstream's new base and re-appended our pinned actions (`KeySlidingToggleAction, LiveResizeAction, SpecialKeysAction, CustomLayoutsAction`), keeping upstream's new Favorites line verbatim (our commits never intended to touch it). (2) **`KeyData.kt` / `Key.kt` / `BaseKey.kt` / `LayoutEngine.kt`** — upstream's `swipeLetter`/`swipeCodeOverride` field additions landed right next to our per-key override (`color`/`fontScale`/`hintScale` → `colorOverride`/`fontScaleOverride`/`hintScaleOverride`), `clusterMains`, and `heightRows`→`rowSpan` fields in the same data classes / `ComputedKeyData` constructions; each was a clean **"keep both fields"** merge (named args, so order is irrelevant). No code re-derivation needed — purely additive reconciliations. **Cluster-prediction static audit CLEAN** (all seven hooks intact, no decision-logic drift — the rc1→stable delta doesn't touch the cluster pipeline). On-device Layer B trace + smoke test were offered but the user said "Push" before "done"; the trace toggles remain available on-device for ad-hoc verification (no rebuild needed). No native change.

Prior: **Emacs/typing fixes — Tab as a key event, Force-auto-capitalization toggle (incl. after newlines), ellipsis auto-spacing** (commits `19c921b8e` force-caps + `7dacd5161` Tab, built `0.1.29-rc1+27` versionCode `116860027`; `3e13557fc` ellipsis, built `0.1.29-rc1+28` versionCode `116860028`; on-device verified — Tab completion in the Emacs minibuffer, newline-caps, ellipsis spacing). Three typing improvements, mostly for the **Emacs Android port** (a rich `text-conversion-style t` field — prediction works there, unlike Termux/`TYPE_NULL`). **(1) Tab → `KEYCODE_TAB`** (`InputLogic.sendKeyCodePoint`): a Tab key (`CODE_TAB`=`'\t'`, e.g. `!code/key_tab` in the GNU/`kxkb.yaml` layout) used to fall through to `commitText("\t")` — a literal tab character; now it flushes pending edits then `sendDownUpKeyEvent(KEYCODE_TAB, 0)` (falling back to commitText only if the connection can't take a key event, mirroring the digit path just above), so the field *acts* on Tab — Emacs minibuffer completion / `indent-for-tab-command` — like every other soft keyboard. **(2) Force auto-capitalization** — new off-by-default bar-pinnable toggle "Force auto-capitalization on/off" (`ForceAutoCapsToggleAction`, icon `shift`, pref `PREF_FORCE_AUTO_CAPS` → `SettingsValues.mForceAutoCaps`; mirrors the Terminal-prediction toggle — lands in *More*, highlights while on, rebuilds `SettingsValues` live). `getCurrentAutoCapsState` ORs `TYPE_TEXT_FLAG_CAP_SENTENCES` (== `CAP_MODE_SENTENCES`, the bit `getCursorCapsMode` masks against) into the field's inputType when on, so `.`/`?`/`!` capitalize even though Emacs reports an inputType without that flag (which is *why* no keyboard auto-caps there — it honours the field). Still gated by the global Auto-capitalization master switch (`mAutoCap`). **Newlines also capitalize:** Emacs reports its text-before-cursor *without the trailing newline* (our committed-text cache and even a fresh `getTextBeforeCursor` come back without it — confirmed because caps worked after a period but a newline only when the prior line had one), so a text lookup can never see the line start. The editor-independent signal is that the last thing *we* input was Enter: `InputLogic` tracks the last non-functional code point (`mLastInputCodePoint`, set at the top of `handleNonFunctionalEvent`, which both Enter and ordinary characters route through) and force-caps returns `CAP_MODE_SENTENCES` when it's `CODE_ENTER`; the next typed letter overwrites it, so only the new line's first letter capitalizes. **(3) Ellipsis "…" auto-spacing** — added the single ellipsis char (U+2026) to `symbols_followed_by_space` in `java/assets/spacing-and-punctuations/default.xml` so it swaps the preceding auto-space away and adds a space after (`hello …` → `hello… `), exactly like `.`; the three-dot `...` already did this (each `.` is a swapper via `isUsuallyFollowedBySpace`→`trySwapSwapperAndSpace`), only the single character was missing. NOT added to `symbols_sentence_terminators`, so it doesn't auto-capitalize the next word; `default.xml` only (covers en + the cs/ru fallbacks; the fr/ar/hy/fa overrides keep their own punctuation-spacing rules). 7 files (`InputLogic.java`, `Settings.java`, `SettingsValues.java`, `KxkbActions.kt`, `Registry.kt`, `strings-uix.xml`, `default.xml`), no native change.

Prior: **Keyboard editor — swap a key up/down between rows** (commit `300891473`, built `0.1.29-rc1+25`, versionCode `116860025`; built clean, on-device test pending). The visual layout editor (Settings → Keyboard UI → "Keyboard editor" → tap a key) could move a key only left/right within its row; the key Edit screen's "Position in row" section now also has **▲ Move up / Move down ▼**, the vertical analogue of the existing ◀ Move / Move ▶. New session op `KeyboardEditorSession.swapKeyVertical(page, row, col, delta)` **swaps** the key with the one directly above/below it — same column index, clamped to the adjacent row's key count — so both rows keep their key counts (a pure exchange, no row overflow; `withKeys` preserves each row's kind, so a key can swap across letters/bottom rows). Up disabled on the top row, down on the last; works per page (base + alt pages); pops back to the preview after the swap, like the horizontal Move. **Swap, not relocate** (matching the horizontal Move precedent — which is itself a swap-with-neighbour). 2 files (`KeyboardEditorSession.kt`, `KeyEditScreen.kt`), no native change.

Prior: **Colon and semicolon get period-like auto-spacing** (commit `18fa9a032`, built `0.1.29-rc1+24`, versionCode `116860024`; built clean, on-device test pending). When prediction/auto-spacing was on, typing `:` or `;` left an auto-space *before* them (`hello :`) and no reliable space after — unlike `.` `,` `?` `!`. Root: `:` and `;` were the entire `symbols_optionally_preceded_by_space` set (`;:`) in `java/assets/spacing-and-punctuations/default.xml` (only file defining it; no locale overrides), which marked them emoticon-style. That set's **only** two consumers are the two `:/;` special-cases in `InputLogic.handleSeparatorEvent`: the `// Don't swap space for : or ; ever, see #1436` guard (`tryStripSpaceAndReturnWhetherShouldSwapInstead` ~L1996) that disables the preceding-auto-space **swap**, and the conditional trailing-space branch (~L1468, `codeShouldBeFollowedBySpace = !spacePrecedesCursor`). **Fix: emptied the set** (one declarative line, no code change) → `isOptionallyPrecededBySpace` is always false → `:`/`;` follow the ordinary followed-by-space path exactly like `.` `,` `?` `!`: the preceding auto-space is swapped away and a space is always added after, so `hello :` → `hello: `. **Emoticons mostly survive** because `)` is itself a swapper (`hello` → `hello ` → `:` → `hello: ` → `)` → `hello:) `); a *manually* typed space before `:` is preserved unless the most aggressive auto-space mode is on (same nuance `.` has). Times like `12:30` unaffected (digit context already suppresses auto-spacing). Prediction-off contexts (GNU/`zxx`) unaffected — `current_language_has_spaces=false` disables the whole auto-space machinery there. 1 file (`default.xml`), no code/native change.

Prior: **Bar-pinnable "Personal dictionary" action + App-language picker fixes (offer English (US), persist the selection)** (commits `7c83dfa91` action + `3073e993e` app-language; built `0.1.29-rc1+23`, versionCode `116860023`; the action verified on-device — reaches the Japanese 辞書に追加 add-word dialog). **Personal dictionary action** (`PersonalDictionaryAction` in `KxkbActions.kt`, icon `icon_spellcheck`): opens the Personal Dictionary editor (Settings → Predictive Text → "Edit Personal Dictionary") directly via the `SettingsActivity` "navDest" extra → "pdict" route — same launcher pattern as Live sizing / Special keys / Custom layouts. Registered in `Registry.AllActionsMap` as `"personal_dictionary"`, in no default category → auto-lands in *More* so the user pins it. This is the entry point for adding custom words, incl. **Japanese kanji entered with a hiragana reading + part-of-speech (品詞)** which Mozc then offers as a conversion candidate (the in-app `JapaneseWordPopupDialog`; encoded into Android's `UserDictionary` as word=kanji, shortcut=`"furigana\tposIndex"`, re-injected into Mozc via `refreshMozcDictionaries()` on the `ReloadPersonalDict` message). 3 files (`KxkbActions.kt`, `Registry.kt`, `strings-uix.xml`). **App-language picker — two fixes** (the in-app UI display locale, Settings → "App language"; NOT keyboard typing subtypes): (1) **English (US) was missing.** The offered list is built at build time by `build.gradle`'s `generateAppLocales` task, which scans translation dirs for `values-*` qualifiers and emits BCP-47 tags; the base `values/` (the project's English source) is NOT a `values-*` dir, so en/en-US was never emitted — only en-GB/en-AU/en-CA/en-IN/en-XC. The task now adds `en-US` explicitly (build log: 92 locales, incl. en-US); selecting it resolves through values-en-rUS → values-en (neither exist) → base `values/`. (2) **The selection didn't persist** — after the process was killed it reverted to the system language (Japanese here). Root cause: on **API < 33** AppCompat's per-app-locale auto-storage only re-reads the saved locale when an `AppCompatActivity` is created, but our Settings UI (`SettingsActivity`) is a plain `ComponentActivity`, so nothing repopulated `AppCompatDelegate.getApplicationLocales()` on a cold start — which the existing `SettingsActivity.attachBaseContext` override reads to apply the locale — and it fell back to system default. Fix in `AppLanguage.kt`: mirror the chosen tag into our own pref (`setAppLocale` writing `kxkb_app_locale`) and re-apply it at app startup from `CrashLoggingApplication.onCreate` (`restoreAppLocale`, guarded, **both stable + playstore flavors**) — a no-op on API 33+ (framework already restored it, `getApplicationLocales()` non-empty) and on "System default" (empty tag). 4 files (`build.gradle`, `AppLanguage.kt`, both `CrashLoggingApplication.kt`). No native change. **Aside (read-only, no change this session):** confirmed the whole Settings-UI colour scheme (amber accent / near-black bg / grey field fills) comes from the **active keyboard theme** shared via `UixThemeAuto`/`THEME_KEY` — `primary`/`surface`/`surfaceContainer*` roles — and is **whole-theme-only** (Settings → Themes presets or image-import; no per-role colour pickers). The amber-on-black is the dynamic Material You theme deriving from the device wallpaper. The kxkb caps-lock colour + per-key layout-editor colour overrides affect only the keyboard, not these Settings dialogs.

Prior: **Terminal prediction — top-bar toggle to force composing/prediction in raw-input (Termux) fields** (commit `787c01bf5`, built `0.1.29-rc1+22`, versionCode `116860022`; verified on-device in Termux → SSH → `emacsclient -t`). Terminal apps (Termux) report `inputType=TYPE_NULL`, so the IME runs in **send-key-events mode** with suggestions off — every tap is a raw key event, there is no composing region, hence no prediction (it's the terminal field's input mode, not SSH filtering). New opt-in **top-bar action "Terminal prediction on/off"** (`TerminalPredictionToggleAction` in `KxkbActions.kt`, icon `text_prediction`, registered in `Registry` → lands in *More* so the user pins it themselves; highlights via `activeStateProvider` while on, like the swipe toggle). It flips a new pref **`PREF_TERMINAL_PREDICTION`** (off by default); writing it rebuilds `SettingsValues` live (Settings' pref listener, no keyboard restart). `SettingsValues.needsToLookupSuggestions()` then returns true for raw-input fields (`mInputAttributes.mSendKeyEventsMode`) when the toggle is on → the word composer composes and the full prediction pipeline runs (cluster prediction included), so candidates appear and a **space-committed word is sent to the remote** over SSH; `shouldInsertSpacesAutomatically()` is likewise forced on so a space follows the committed/picked word (the missing-trailing-space fix). Gated AFTER the GNU (`zxx`) guard so GNU stays prediction-free even in a terminal; `mSendKeyEventsMode` is left intact (its ONLY consumer is the ENTER handler) so **ENTER stays a raw newline**, correct for terminals. **Trade-off:** while on, interactive raw-keystroke behaviour (control keys, shell tab-completion, immediate input) is traded for prose prediction — flip it off for shell work. Implemented via `SettingsValues` overrides (rebuilt on pref change) rather than touching `InputAttributes`, so it's live with no `InputAttributes` plumbing; validated first with a throwaway `InputAttributes` force-on hack, since reverted. 5 files (`Settings.java`, `SettingsValues.java`, `KxkbActions.kt`, `Registry.kt`, `strings-uix.xml`), no native change.

Prior: **Keyboard UI live-sizing knobs — bottom-padding lift, see-through narrowed/lifted/split margins, landscape width, keyboard-mode picker** (commit `921a63ef3`, built `0.1.29-rc1+17`, versionCode `116860017`; verified on-device incl. foldable portrait & landscape). All per-(language·layout·geometry), in Settings → Keyboard UI (`KxkbSizingScreen` = `Typing.kt`). **Bottom padding (lift):** new Size → Height slider (0–640 dp) that lifts the whole keyboard off the bottom edge in every docked mode (Regular/Split/One-handed) — `bottomOffset` promoted onto the `ComputedKeyboardSize` base class, rendered via `OffsetPositioner`'s transparent bottom spacer; 0 = unchanged docked behaviour. **See-through margins:** when a keyboard has empty margins — narrowed (`sideOffset` > 0), lifted (`bottomOffset` > 0) or **split** (always has a centre gap) — `LatinIME.onComputeInsets` borrows the floating keyboard's inset treatment (`contentTopInsets`/`visibleTopInsets` at the navbar so the app draws behind the whole window; only the keyboard rect marked `TOUCHABLE_INSETS_REGION`) so the app shows THROUGH the sides / bottom gap instead of an opaque black void; for split, `UixManager.KeyboardBackground` additionally **punches the centre channel** (computed from the live keyboard's key geometry via `computeSplitChannel`, the key-free central band down to the top of the full-width bottom bar) out of the solid/gradient background brush so that channel is see-through too. **Landscape width:** the "Keyboard width" slider now narrows + centres **split AND non-split** layouts (`sideOffset` promoted to the base class; both the Split branch and the `Split && !supportsSplit` Regular-fallback in `calculate()` apply `widthFraction`, scaling `splitLayoutWidth` by the same fraction) — it was Regular-only, so it silently did nothing in landscape where Split is the default (and every layout supports split, `supportsSplit` defaulting true). **Keyboard-mode picker:** a Size → "Keyboard mode" selector (Standard / Split / One-handed / Floating; English string literals on purpose — the upstream `action_keyboard_modes_*` strings are translated and render as Kanji under a CJK UI locale, whereas this kxkb screen is English-only) writes `currentMode` + `prefersSplit` per geometry, so **split (etc.) can be chosen in ANY orientation — portrait included** — not only where it is the default. "Split width" slider label de-scoped from "(landscape / unfolded)". **Bottom-row split deliberately NOT done:** the generic `LayoutEngine` splitter mangles the action row's `Grow` spacebar (it collapses to nothing in a narrow half), so the bottom bar stays full-width and the channel runs the letter rows down to it; `LayoutEngine` left net-unchanged. 5 files (`KeyboardSizingCalculator.kt`, `UixManager.kt`, `LatinIME.kt`, `Typing.kt`, `strings-uix.xml`), no native change. **Trade-off:** any narrowed/lifted/split keyboard adopts the floating-style insets, so the app draws behind it and does NOT auto-scroll content above the keyboard (same as the floating keyboard) — that is what makes "display what's under it" possible.

Prior: **GNU layouts: disable automatic spacing (no auto-space after punctuation)** (commit `645371888`). GNU layouts (locale `zxx`, ISO 639 "no linguistic content") are used for code input, where the engine's automatic spacing gets in the way. New declarative override `java/assets-custom/spacing-and-punctuations/zxx.xml` — rides alongside the upstream `java/assets/spacing-and-punctuations/` config via `build.gradle`'s `assets-custom` merge, resolved by locale with fallback (`zxx` → `default`, `zxx` winning per key) — sets `current_language_has_spaces=false` (the master gate on every automatic-spacing path in `InputLogic`: auto-space after `. , ; : ! ? ) ] }`, the weak-space swap, double-space→". ", auto-space before/after symbols) and additionally empties the followed-by-space symbol lists. The literal spacebar is unaffected; real languages never read `zxx.xml`, so their auto-spacing is intact. No code change (assets-only). **Follow-up** (commit `7a710a755`): GNU also no longer composes words or runs prediction/next-word — `SettingsValues.needsToLookupSuggestions()` returns false for `zxx`, so the word composer stays idle (InputLogic only composes when that is true) and GeneralIME shows only the static topBar default candidates (still tappable), never next-word. This ALSO fixed a double-space the `current_language_has_spaces=false` override had introduced: a pending COMPOSING word made the first space commit it with no separator and swallow the space (`InputLogic.shouldAvoidSendingCode`), so only the second space typed — with no composing word the first space is a real space again. 1 file (`SettingsValues.java`).

Prior: **Cluster-layout prediction restored after the 0.1.29-rc1 rebase, + a permanent cluster-prediction testing harness** (working tree, verified on-device, pending the user's "Push" — not yet committed; built as `0.1.29-rc1+4`, versionCode `116860004`). **Regression:** the 0.1.29-rc1 rebase replayed cleanly but cluster prediction silently broke — upstream added a gate in `LanguageModelFacilitator.makePredictionInputValues` that returns null while composing on any non-QWERTY layout unless the new default-off `AllowTransformerOnNonQWERTYLayouts` dev setting is on. Every cluster layout is non-QWERTY, so this null skipped the transformer path (English) AND made `maybeApplyClusterConstraint`'s `makePredictionInputValues(ignorePassthrough=true)` call return null (Czech/Russian) — the whole cluster pipeline (`processAndMergeSuggestions` filter + `clusterTrieWalk` enumeration + `clusterBaseFold`) never ran, collapsing cluster layouts to the raw dict tap-decoder. Our `ignorePassthrough` bypassed only the OLD passthrough guard, not the new gate. Everything else the feature needs (`getValidNextCodePoints` Java + native, the GeneralIME legacy-branch wiring, `Key.clusterMains`) was verified unchanged. **Fix:** the gate now also accepts any layout carrying a cluster key (`kb.sortedKeys.any { !it.clusterMains.isNullOrEmpty() }` — the same `clusterMains` signal `clusterAllowedSets` keys on), so cluster layouts run the full pipeline again; ONE change fixes both English (transformer-merge) and cz/ru (legacy). Also assigned upstream's never-set `prevAcceptedLayout` cache so the scan runs once per layout, not per keystroke. **Tooling:** new `ClusterPredictionDebug` (default-off dev toggles at Settings → Developer → "Cluster prediction debug": *Trace to file*, *Isolate next-word/topBar*, *Clear log*) writes a staged per-update candidate trace (gate decision, per-tap cluster sets, dict / LM / trie-walk / selected / FINAL) to `cluster_pred_debug.log`; the new **`cluster-prediction-testing`** skill documents the pre-build static audit (seven integration points) + the on-device trace cycle + the en/cz/ru corpus, and `upstream-new-version` Steps 4.5 (audit) + 6 (verify) invoke it. **Verified on-device:** 0 QWERTY-gate refusals; 29 transformer-merge (English) + 36 legacy-cluster (cz/ru) blocks; e.g. `thih→this` (English) and `pravděpodobně` (long Czech word) resolved top with confident autocorrect, FINAL strictly in-set + accent-correct. 4 files edited (`LanguageModelFacilitator.kt` fix+trace, `GeneralIME.kt` trace+isolate-toggle, `DevSettings.kt` toggles, `upstream-new-version` SKILL) + 1 new source (`ClusterPredictionDebug.kt`) + 1 new skill (`cluster-prediction-testing`). No native change. Reconciliation recorded in futo-keyboard-build §"Sync to a new upstream version → 0.1.29-rc1 sync learnings". **Follow-up — commit behaviour (commit `787e6081b`):** on a cluster layout the composing "typed word" is the meaningless centre-letter literal, so SPACE now commits the top in-cluster prediction (autocorrect-on-space). `processAndMergeSuggestions` forces `isCorrectionEnabled` for cluster contexts (so it works even with the global Auto-correction setting off; non-cluster keeps the user's setting) and lowers the confident-autocorrect gate from ≥4 taps to ≥2 (single tap stays literal; still guarded by `clusterEnumerated.isNotEmpty()`, so a non-dict word produces no enumeration and keeps its literal). To keep the literal reachable for out-of-dictionary words, `ActionBar.ActionBarWithExpandableCandidates` re-inserts the `KIND_TYPED` literal (previously filtered out) as the **2nd** candidate in the expandable bar, deduped against the top prediction (candidate taps pick by identity, so position never mis-targets a commit). 2 files (`LanguageModelFacilitator.kt`, `ActionBar.kt`).

Prior: **Sync to FUTO `0.1.29-rc1`** (rebased `custom` onto the `0.1.29-rc1` tag, tip `6ecf23f43`; `master` ff'd to `8c85821e4`). The first non-trivial rebase — upstream reworked six subsystems our features touch (12 conflicts over the 97-commit replay), resolved conflict-by-conflict with the user. Key reconciliations: **upstream's native `rowSpan`** vs our `heightRows` → kept `heightRows` as the author-facing `KeyAttributes` alias but routed it through upstream's `rowSpan` engine (dropped our `ComputedKeyData.heightRows` + `effHeight`/`finalRowHeights`; upstream's `rowSpanProcessed` auto-gaps the covered cell); **upstream split the spacebar setting** into `mSpacebarSwipeMode`/`mSpacebarHoldMode` → re-homed our swipe-space layout switcher as a new swipe-mode value `SPACEBAR_MODE_LAYOUT_MENU=3`, long-press now via `mSpacebarHoldMode`; **kept** the `mergeDuplicates`-disable, the appended kxkb action-map entries, and the per-key colour/cluster fields alongside upstream's additions. New submodule **`java/assets/futo-swipe`** (swipe-typing model, HuggingFace). Build-env fix: Claude Code's shell needs an explicit `export ANDROID_HOME=$HOME/android-sdk` (now in both skills' build steps). **Follow-up fix** (`6ecf23f43`, `spacebar: carry the legacy swipe=layout-menu setting…`): upstream's legacy migration only maps old spacebar values 0/1/2, so our old `pref_spacebar_mode=3` fell back to "move cursor" and the swipe menu silently broke — added `SPACEBAR_MODE_SWIPE_LAYOUT_MENU_LEGACY=3` mapping to the new `LAYOUT_MENU` swipe default so it carries over. Reconciliation details in futo-keyboard-build §"Sync to a new upstream version → 0.1.28 → 0.1.29-rc1 sync learnings". **NOTE: `0.1.29-rc1` is a release candidate, not a stable release.** A `0.1.28+rollback` APK (versionCode `116860002`) was also built so the old 0.1.28 stack can be reinstalled over an rc1 build if needed.

Prior: **Phase 3 (completion) — per-key cluster spacing / character position / secondary-label offsets, + cluster/column auto-capitalise** (`kxkb: layout editor Phase 3 — per-key cluster spacing, character position & secondary-label offsets`, commit `255811886`, + `kxkb: cluster/column auto-capitalise when shifted (+ Special Keys "case" docs)`, commit `21e1da0f2`). **Geometry overrides** finish Phase 3's per-key plumbing (same model→render→editor path as the colour/size overrides): `KeyAttributes` gains `clusterLeftOffset`/`clusterRightOffset` (cluster band insets), `labelOffsetX`/`labelOffsetY` (character position, ±keyWidth/keyHeight) and `flickTopOffset`/`flickBottomOffset`/`flickLeftOffset`/`flickRightOffset` (the secondary 4D / cluster side-label band) — all nullable (null = theme), resolved in getEffectiveAttributes/plus, carried via `ComputedKeyData` → runtime `Key` overrides → `KeyboardView` (`drawClusterMains` reads the cluster insets; `onDrawKeyTopVisuals` shifts label+icon+cluster-mains by the char offsets; `drawAtRestFlickLabels` positions the side labels), each falling back to the theme `sCluster*Off`/`sFlick*Off` defaults; emitter round-trips them. **Editor** (key Edit → "Appearance (live)"): `OverrideSliderRow` controls for Cluster left/right spacing (default 0.333), Horizontal/Vertical position (default 0) and Secondary top/bottom/left/right (defaults 0.30/0.40/0.34/0.34), each Switch-off = theme, shown live on the single-key REAL-size crop preview (secondary band gated to Compass/Cluster/Column/Flick). **Cluster auto-capitalise** (`21e1da0f2`): a cluster/column now upper-cases its WHOLE predictive band when the layout is shifted, so it needn't be wrapped in a `case` purely to capitalise — `ClusterKey`/`ColumnKey.computeData` detect the shifted element (gated on the resolved `shiftable`, default true) and build `effMain = main.uppercase(locale)`, which drives the tap-commit primary, the West/East flicks, the drawn label and the `clusterMains` codepoints; prediction is unaffected because `clusterBaseFold()` lowercases before matching the dict. `attributes: { shiftable: false }` opts out — matching how base/compass keys already behave. **Special Keys** settings page gains a "Case (per-shift-state key)" explainer (what `case` is for; that clusters/columns no longer need it just to capitalise). 9 files, no native change. **Phase 3 complete.** Documented in full in futo-keyboard-build §"Implemented features".

Prior: **Phase 3 (Stage 2) — per-key background + border colour** (`kxkb: layout editor Phase 3 Stage 2 — per-key background + border colour`, commit `13491a707`). `KeyAttributes.backgroundColor`/`borderColor` (ARGB, null = theme) plumbed model→render like the text overrides; `KeyboardView.onDrawKey` builds a `GradientDrawable` rounded-rect matching the theme — corner radius + existing fill read from the key's current `GradientDrawable`, theme border colour + stroke width pushed per-geometry via `setKeyDrawDefaults` from `withPerKindLook`. Override only-background keeps the existing border; only-border keeps the existing fill (synthesising a ~2dp border on a borderless theme). Editor "Appearance" gains Background / Border RGBA controls (clear buttons), shown live in the single-key preview. **Remaining Phase 3:** per-key cluster spacing. Documented in full in futo-keyboard-build §"Implemented features".

Prior: **Phase 3 (Stage 1) — per-key text appearance** — `kxkb: per-key text colour / font size / hint size overrides` (engine `29de03ec3`) + `kxkb: layout editor — appearance controls + single-key live preview` (editor `6e2652da3`). Per-key visual overrides plumbed model→render: `KeyAttributes` gains `color` (label/icon ARGB), `fontScale`/`hintScale` (multipliers on the computed primary/hint text size, null = theme); resolved in getEffectiveAttributes, carried via `ComputedKeyData` → runtime `Key` (`colorOverride`/`fontScaleOverride`/`hintScaleOverride`) → `KeyboardView.onDrawKey` rebuilds the `KeyDrawingConfiguration` with the overridden textColor / textSize×scale / hintSize×scale; emitter round-trips them. **Editor UI** (key Edit → "Appearance (live)"): an RGBA colour control (reuses the Keyboard UI `ColorSetting`, now internal) + "Use theme colour" to clear, and **percentage** size sliders (Switch off = theme; stored as a multiplier, shown as % since px differs per geometry); above them a **single-key live preview at REAL size** — `SingleKeyCrop` draws the full page keyboard to a `Bitmap` and crops to the key's exact pixel rect (`drawX,y,drawWidth,height`), deterministic 1:1, rebuilt as sliders move. A `case`/shift key writes the appearance to all shift-state branches. **Stage 2 (next):** per-key background + border colour, then per-key cluster spacing. No native change. Documented in full in futo-keyboard-build §"Implemented features".

Prior: **`kxkb: layout editor — duplicate key / duplicate row`** (commit `0d71fd01d`) + **`kxkb: adjacent identical keys render separately`** (engine commit `e049a4ea6`). Completes Phase 2's structural editing. **Duplicate:** session `duplicateKey(page,row,col)` (copy right after the key) + `duplicateRow(page,row)` (copy the whole row after it); UI = a "Duplicate key" button in the per-key Edit screen's Position-in-row section (inserts + opens the copy for editing) and a "dup" button per row in the Rows section; works on base + alt pages. **Engine fix it depends on:** upstream FUTO's `LayoutEngine.mergeDuplicates` merged any two ADJACENT keys with identical `ComputedKeyData` into one combined-width key (on the real keyboard, not just previews) — so two identical neighbours always collapsed into one double-width key, which is why "duplicate key" looked broken. Disabled (`mergeDuplicates` returns the row unchanged) so adjacent identical keys render as separate keys; wide keys are made with width tokens (`Custom*`/`Grow`), not duplication; split-layout middle duplication is unaffected (it happens later in `splitRow`). Note: duplicating a key in an already-full fixed-width row overflows the row (expected — delete/replace another key to rebalance). 4 files (3 editor + 1 engine), Documented in full in futo-keyboard-build §"Implemented features".

Prior: **`kxkb: layout editor — alt-page editing, cross-layout copy/preview, Apply-as-new`** (commit `320bc8074`). Extends the editor to the alt pages and adds cross-layout management. **Per-page editing:** `EditPath` gained a `page` field (−1 = base, 0..N = `altPages[page]`); session `keyAt`/`replaceKey` + all structural ops are page-aware; a **page selector** (Base / alt0 (sym) / alt1 (altGr) / …) drives the preview, Rows section and tap-to-edit (an alt page is previewed by building a standalone keyboard whose base rows ARE that page's rows). **Alt-page management ("Alt pages (max 3)"** — the engine only switches `key_to_alt_0/1/2` → `Alphabet1/2/3`): each page has ◀/▶ (reorder) + ✕ (delete, selected page clamped); a single page can be **copied (appended) from another layout** — the "Append a page from another layout" list shows one row per (layout · altN) with **Preview** (a screen rendering that page, with an Append button) and **Append** (`appendAltPageFrom`, capped at 3). **"Apply as a new layout":** a dialog (Language + name prefilled) saves the working model as a NEW custom layout (same path as "Create new layout", registered as a subtype) and reopens the editor on that copy; the action row is now Apply | Export YAML over Apply-as-new | Revert. 5 files (1 new: `AltPagePreviewScreen.kt`) + the `AltPreview` route, no native change. Documented in full in futo-keyboard-build §"Implemented features".

Prior: **`kxkb: layout editor Phase 2 — add/move/remove keys & rows`** (commit `7796125ce`). Structural editing on top of the visual editor (below). **Per-key** (a key's Edit screen, "Position in row" section, top-level keys only): **◀ Move / Move ▶** (swap with neighbour), **Insert ◀ / Insert ▶** (insert a key and jump into editing it), **Delete key** (disabled on a 1-key row). **Per-row** (a new "Rows" section on the main screen): **▲/▼** reorder, **+key** (append + edit), **✕** delete, **Add row** (single-key letters row after the last letters row). Session ops `insertKey`/`removeKey`/`moveKey`/`addRow`/`removeRow`/`moveRow` (all via a `mutateRows` copy-and-rebuild helper) with keep-≥1 guards (≥1 key/row, ≥1 letters row). Also fixed this round: a **`case` key (e.g. shift) now shows a unified Attributes editor** — the selector has no attributes of its own (they live on its per-state branches), so the editor reads `normal`'s attributes and writes any change to every attribute-bearing branch at once. Remaining Phase 2: alt-page (sym/altGr) editing + duplicate. No native change, 3 files. Documented in futo-keyboard-build §"Implemented features".

Prior: **`kxkb: visual keyboard-layout editor (Phase 1 — per-key editing)`** (editor commit `cf4b89047`, on the YAML-emitter foundation `683f47083`). An in-app layout editor at **Settings → Keyboard UI → "Keyboard editor"** (also the space-swipe menu's Keyboard UI column). Pick a custom layout → a live **full-width preview** built 1:1 from the working model via the new emitter; **tap any key** → a dedicated Edit-key sub-screen. Edit-key: a **Type** picker (convert base/compass/cluster/column/macro/chord/cycle/case/gap), the type-specific scalar fields inline (spec/label/code/moreKeys/text/keys/taps/main/slide/icon), and an **Attributes** section (width/style/moreKeyMode/heightRows + boolean flags as tri-state inherit/true/false). **Nested keys drill in** (a `case` key's shift states, a compass `primary`, slide-direction KeySlots get their own Edit-key screen; `{mod}` slots get inline mod/on/label fields). Width tokens read meaningfully ("Regular (1 key)", "Custom1 — 9% of keyboard") with a **"Custom key widths"** section to set the Custom1–4 `overrideWidths` percentages. **Apply** writes the layout back as the custom layout + re-switches the active subtype (live keyboard reloads); **Export** opens a screen with **Copy** + **Save-to-file** (system picker, prefilled with the UTF-8 layout name); two **"Close Keyboard editor"** buttons finish back to the keyboard. **Foundation** (`683f47083`): `emitKeyboardYaml(Keyboard)` in `v2keyboard/KeyboardYamlEmitter.kt` — a hand-written YAML emitter that round-trips the model (kaml only parses; the custom serializers throw on encode), emitting explicit `{type:…}` form + `$template` reverse-map + bare-scalar pure BaseKey + default-omission; plus `Keyboard.rows` made public and `GapKey` a data class. **Editor** (`cf4b89047`): `KeyboardEditorSession` (shared working `V2Keyboard` across the editor's nav; path-addressed key/slot get-replace; preview via the `ForceCustomLayoutYamlB64` seam; **tap→source mapping = runtime `key.row`/`key.column` index the authored `kb.rows` directly** — the number row is disabled so it is not laid out, no offset) + `KeyboardEditorScreen` / `KeyEditScreen` / `KeyboardEditorExportScreen` + routes; also reworked the space-swipe switcher's Keyboard UI column (`Subtypes.kt`: removed "Themes", added "Keyboard editor", moved "All settings"/"Special keys" nearer the finger). Phase 1 = per-key editing; Phase 2 = structural (add/move/remove keys & rows, alt-page editing); Phase 3 = engine-backed per-key colour/spacing. No native change. Documented in full in futo-keyboard-build §"Implemented features".

Prior: `kxkb: Caps-lock via Shift tap-cycle + blue Shift-glyph indicator` (commit `f99c2f2f1`; preceded by a one-line `.gitignore` housekeeping fix `030bb4bb8`). Tapping Shift now **cycles** unshifted → shift (one-shot) → caps-lock → unshifted, timing-free (replaces double-tap-for-caps-lock; two quick taps still reach caps-lock via the cycle). A *manual* shift advances to caps-lock; auto-caps (`Shifted`) and caps-lock both unshift on tap (so a tap still cancels an auto-capitalised start). Driven by `KeyboardState.cycleShift()` (the `CODE_SHIFT` press now calls it instead of `toggleShift`+`onShiftTapForShiftLockTimer`). When caps-lock is engaged the **Shift glyph** on both Shift keys recolours (default pure blue `#0000FF`) so the locked state is obvious; the key *background* is untouched — done by overriding `textColor` in the `KeyDrawingConfiguration` for `CODE_SHIFT` keys when the keyboard is in a `SHIFT_LOCKED`/`SHIFT_LOCK_SHIFTED` element (the icon tint and label paint both honour `textColor`), detected at draw time since caps-lock toggling is a full element reload. The colour is a new per-(language·layout·geometry) Keyboard UI setting: `SavedKeyboardSizingSettings.capsLockColor`, pushed via `KeyboardView.setCapsLockColor` in `LatinIME.withPerKindLook` (null ⇒ default blue), `lookFieldsDiffer` re-looks on change, and a "Caps lock → Caps-lock Shift colour" ColorSetting row in Settings → Keyboard UI. 6 files, no native change. Documented in full in futo-keyboard-build §"Implemented features".

Prior: `kxkb: backspace auto-repeats when held in any layout` (commit `bfe244fbb`) — `BaseKey.computeData` now treats `CODE_DELETE` as repeatable (alongside the arrow actions), so longhand delete keys repeat when held without needing `repeatableEnabled` in the layout (the `$delete` template already did). 1 file.

Prior: `kxkb: append topBar defaults after next-word predictions (combined bar)` (commit `f25c887eb`), on top of the Multiling-style "topBar" feature (`432f70487`). The topBar static entries (emoji/snippets, `A…B` bracket-pairs with caret-between, `[Paste]`, `{{date` stamps) show in the suggestion bar's idle/next-word state; now they're **appended after any next-word predictions** (so both are selectable) instead of only when the bar is empty. While composing a word the bar stays pure (no defaults). Global list editable in Settings → Typing → "Suggestion bar candidates"; a layout's own `topBar:` overrides it. Engine: `GeneralIME.showPredictionsWithTopBar()` + `engine/general/TopBar.kt` parser + identity-matched pick interception in `onEventInternal`. No native change. Also confirmed this session: next-word prediction for cs/ru is **user-history-only by design** (no transformer model covers them; bundled dicts are unigram-only) — not a bug. Documented in full in futo-keyboard-build §"Implemented features".

Prior: `kxkb: expandable suggestions panel (Multiling-style) default-on + 128 candidates` (commit `2cfffdc45`). FUTO already ships an expandable candidates UI (`ActionBarWithExpandableCandidates` — scrollable inline row + a ▲ toggle that overlays the keyboard with the full list) but used it only for CJK, gated behind a default-off dev setting (`UseExpandableSuggestionsForGeneralIME`). Defaulted that to true, surfaced it as a Settings → Typing toggle ("Expandable suggestions panel"), and raised the merge candidate pool from 14 to 128 (`SuggestionResults` capacity + the dict `take`) so cluster layouts can flip through many candidates. 4 files, no native change. Documented in full in futo-keyboard-build §"Implemented features".

Prior: `kxkb: software dead keys for diacritic composition` (engine commit `45bba2f80`, cz/gnu 1.3.5 layouts `bfb0a429c`). Diacritic accent keys (`´ ˇ ¨ ¯ ˚` …) now compose with the next letter (`ˇ`+`r`→`ř`) instead of typing literally. FUTO's `DeadKeyCombiner` is already in every layout's combiner chain but only fires on `FLAG_DEAD` events (which AOSP only set for hardware dead keys); the fix re-dispatches an accent keypress as a dead event in `InputLogic.onCodeInput`, gated by `DeadKeyCombiner.isDeadAccentCodePoint` (dedicated accent display-forms `≥ U+0080` only, so literal ASCII `` ` `` `^` `~` `'` stay literal). 2 engine files, no native change. The cz/gnu layouts were bumped to 1.3.5 with macron/diaeresis/ring dead-key slots. Documented in full in futo-keyboard-build §"Implemented features".

Prior: `kxkb: cluster prediction for accented / no-LM languages (Czech, Russian)` (commit `bc1cd9e7c`). Made the Multiling-style cluster prediction work beyond English. Two root causes fixed, Kotlin-only (2 files), language-agnostic: (1) the cluster pipeline was transformer-gated — Czech/Russian have a dict but no transformer model, so they fell through to the legacy path with no cluster handling; now `maybeApplyClusterConstraint` runs it on the legacy dict path too (`makePredictionInputValues(ignorePassthrough=true)`). (2) accent-blindness — `clusterBaseFold` (NFD) folds accented dictionary letters onto the base-Latin/Cyrillic cluster sets. The cartesian enumeration was also replaced by a **dictionary trie-walk** (`clusterTrieWalk` via the existing `getValidNextCodePoints`) that handles any length (incl. `jednoduché`) and reads accents straight from the trie; ≥4-tap clusters now auto-commit the predicted word. Verified on Czech + Russian. Documented in full in futo-keyboard-build §"Implemented features". Prior five-layer base feature was `kxkb: Multiling-style cluster prediction` (commit `3bb85d1a1`).
